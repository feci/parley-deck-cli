/parley
/parley.exe
/dist/
/build/
coverage.out
parley-deck/agents.local.toml
parley-deck/meta/headless-agents.local.json
parley-deck/meta/runtime-probes/
.antigravitycli/
tmp-test-plugin/
# agy (Antigravity CLI) writes a project->UUID map to ./cache/projects.json
# in its working directory when launched; it is a runtime artifact, not ours.
/cache/
# Local Go build/module caches (used by sandboxed agent runs with
# GOCACHE=$PWD/.gocache). Build artifacts, not source.
/.gocache/
/.gomodcache/
