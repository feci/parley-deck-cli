---
agent: codex-1
idea: meta-protocol-change-evidence-first-efficiency
date: 2026-09-16
status: candidate-controls-only
---

# Fresh-cycle and grouped-launcher controls

Kimi's fresh-cycle fix invocation `ecd4331a-1a10-477a-9de7-ddacb38e0781` exited0 with five owned Go files and its own correction note. Source-only authorship is not an executed test claim. The new immutable 461-file candidate compiled in1.872s, then its driver-focused run had15 passing events and one failure: the fresh fixup fixture expected a step binding while leaving the step ceiling at its unlimited default.

Codex added only `MaxDriverSteps: 5` to that test's driver configuration, matching the existing finite-step control and retaining every outcome assertion. The corrected immutable snapshot compiled in1.901s and passed16 focused events in2.423s. Its existing-history refusal behavior is not waived. A counterfactual restored only the three old production driver files while retaining the new tests: compilation passed1.691s, and both fresh-scope tests failed behaviorally in0.804s. These controls support the admission-order change; broad new-source checks and independent acceptance remain outstanding.

Evidence: `n1-major-validation/`, `n1-major-finite-step-validation/`, `n1-major-before-control/` under the managed continuation runtime. All failed and passing snapshots remain retained. The finite-step correction was also copied into the idle editable candidate before Kimi received the separate known-exhaustion allocation. N1 MINOR-1 remains unresolved until that candidate is checked; no N1 Go source is integrated.

Zcode's launcher correction invocation `8b6206ca-1bed-4102-b232-eb165de25d56` exited0 with two owned Go files and its own note. An immutable recovery-plus-launcher snapshot compiled in1.559s and passed8 local fake tests in1.102s. This verifies the covered grouped-launch and refusal cases; it is not actual amendment execution.

Coordinator source review then found four additional gaps: the preview inspected the raw cycle ledger instead of the binding's complete migration-charge checks; an ambient agent-config layer was not hashed when the explicit option was omitted; the shared900s deadline began after discovery; and organizer-preservation reporting reused the aggregate success boolean. Zcode owns a bounded correction and targeted tests for those paths. No actual launcher execution, count, declaration apply or treatment occurred.

Concurrent source attribution: Claude's history-design invocation observed Kimi's driver drift; the launcher correction observed the same allocated driver work. The owner-specific terminal snapshots, rather than whole dirty trees, are imported into validation sources. These observations are not attributed to the read-only design author or to Zcode.

Claude's unscoped-run design correctly distinguishes absent from conflicting identity and readable unknown-scope bytes from unavailable roots. Codex accepts those constraints and the digest-bound preservation approach. Replacing existing persisted field types and adding a separate product discovery command are unnecessary for the immediate candidate; the assigned implementation keeps existing signatures/records compatible and adds an optional request-scoped unscoped-run declaration. The design's four listed prior-candidate “flaws” are extension constraints, not demonstrated failures of the declared-worktree-only behavior. No real operator count or migration decision is inferred.

Accounting at 2026-09-16 03:38 UTC:80 stopped normalized terminals plus one stopped invocation missing its terminal;50 terminal costs plus the incomplete attempt are unknown. Known CLI estimates total USD109.2053935. The three subsequent active correction/candidate tasks are excluded. This is implementation/source-review accounting; prospective experiment spending has not begun.



## Known-exhaustion candidate terminal

Kimi invocation `49ae62b4-3755-4882-a1f3-3236a425d01e` exited0 with seven owned
Go files and its new note. The immutable 452-Go/module candidate (463 total
manifested files) compiled in3.685s and passed46 focused test/subtest events
in17.171s. A counterfactual restored only the three driver/runner wiring files,
retaining the new helpers and tests: it compiled in2.986s, then the three known
exhaustion behavior tests failed in2.371s. Both sources remained unchanged.
These controls cover known cross-resource exhaustion, charged-session reuse and
the documented late-race boundary; full/race/vet and independent acceptance
remain pending. Broad validation will use the final combined candidate, avoiding
duplicate long runs while the disjoint declared-history source is still active.

The launch observed concurrent Claude-owned launch_migration_history.go drift;
only Kimi's exact allocated terminal hashes were imported. No history change is
attributed to Kimi. Accounting at03:48UTC is81 normalized stopped terminals plus
one missing-terminal attempt,51 unknown terminal costs plus that incomplete
attempt; known CLI estimates remainUSD109.2053935.
