---
agent: codex-1
idea: meta-protocol-change-evidence-first-efficiency
date: 2026-09-16
status: preparation-only
---

## Observed blocking authority

The read-only registry observation contains 27 registered worktrees, exactly two
unavailable at the observation time: the original `.../scratchpad/f2repo` and
`/private/tmp/revert-test2`. Full paths, retained HEAD/gitdir/commondir bytes and
hashes are in private runtime `history-unavailability-preparation/inventory.json`.
Observation SHA256: `2ff966bf2684cd8ec2aab3ce10ebab99c3f09c239d768899262ead1e92c70537`.
No registration was removed, directory recreated, missing history reconstructed,
or policy/counter modified. Registry HEAD is not evidence of missing runtime
history. Backup availability has been asked; no answer or attestation is inferred.

The actual read-only `budget migrate inspect --kind cross-review` refuses the
unavailable f2repo registration. All three v2 round-02 requests were refused
before child start and remain preserved. No failed request is promoted to a
participant artifact. Their normalized usage stays null; source control flow
shows refusal before cycle/step charge, rather than evidence of unknown provider
work. Distinguish these records from the three currently active implementation/
review CLIs and from older stopped real processes.

## Supported recovery gap and bounded next implementation

Claude's own supporting source review identifies why no present attended flag
can satisfy this state: migration inspect/apply share the unavailable-root scan,
while extension requires an existing policy. Its MRW-1 proposal records exact
unavailability as unknown, keeps ordinary bootstrap refused, binds explicit
migration to the incomplete inventory and leaves the frozen cap unchanged.
That proposal is not an adopted attestation, a proven history bound or permission
to assign an unknown region a zero count.

The currently allocated implementation is only the read-only inventory API/CLI:
`budget worktree inspect`. It prepares a reviewable stable digest covering actual
registration and availability without exposing apply/attest or changing existing
launch admission. An unavailable row must remain visible as unknown. The
candidate and tests will be independently inspected before integration. A future
writer/reconciliation path still needs its complete design, checks and explicit
operator decision; implementation of this reader does not silently authorize it.

## Group the real cross-review operation

Accept Claude's accounting correction: use the existing grouped runner for the
remaining amendment round. `runner.RunRound` delegates to `RunRoundOne`, which
opens one cross-review and one step session for its selected participants;
`runAgent` preserves pre-existing owned artifacts through the normal skip path.
Keep all four canonical members; do not rewrite the historical roster or reduce
quorum to the three missing files. Preserve Codex's published round-02 and author
only missing peer files. Existing runner tests cover skipping an existing file.

This changes the future orchestration plan, not the three retained refused
records. It does not solve missing history, refund a past count or assert that a
binding already exists. Freeze/apply an actual migration only after the missing
history disposition and exact counts are reviewable and authorized. No repeated
independent per-participant round-02 launches, alternate phase names, independent
clone scope or uncharged direct CLI is used to evade the current refusal.

No experimental treatment has started. USD 15 total prospective experiment
spending and 900 seconds per task-arm remain separate from software work.
