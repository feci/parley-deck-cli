# Concrete amendment continuation proposal — 2026-09-16

Status: prepared and read-only; **not an operator authorization**. The user approved
prospective Codex/Claude/Kimi/Zcode membership and a four-person pilot amendment.
That direction did not select an unknown historical action count or apply a migration.

## Verified preview

The actual candidate product CLI, built locally without installation, passed
`budget migrate inspect --kind cross-review` against the amendment worktree on
2026-09-16 at04:28UTC. Its history digest is
`31950020aa1c253ed7bd68c7c42049b1cf1f496f437380d0776b8d712e8cae34`; it retains325 sources. The previous322-source
snapshot remains preserved with its old digest; the current325-source inventory is a
fresh observation, not an edit of the old evidence. Raw27-registration bytes and the
empty shared budget inventory were unchanged. No migration or provider call occurred.

The visible lower bound is **1**. Its basis is
`surviving-visible-worktrees-and-scoped-run-history-only`. This is not a verified exact total.
Two registrations remain missing/unknown:

- `/private/tmp/claude-501/-Volumes-My-Shared-Files-AI-WORKSPACE-parley-deck/5dc331bd-5ddf-45e0-b6c2-d519d8c05128/scratchpad/f2repo`
- `/private/tmp/revert-test2`

One run lacks an idea identity. Its25copies are copies of one unknown-scope file set,
not25actions; its readable bytes remain retained:
`parley-deck/runs/20260510T194003Z=7283348e38b86004c304e55befa95949d276d247cfd0166c4b98ed759333c6b6`.
Three refused-before-start requests remain ungrouped; their IDs are in the companion
JSON. They are not erased, silently assigned a cycle, or inferred to be zero history.

## Proposed operator choice, pending explicit approval

Choose **N=1** as the imported historical cross-review accounting total for this
amendment only, while explicitly acknowledging the incomplete coverage above.
Choose `2026-09-15T23:51:55.566717Z` (earliest observed scoped history) as the
accounting epoch. These are proposed operator bookkeeping choices, not recovered
facts about unavailable or unscoped histories. An operator with evidence of another
count/earlier epoch must replace them. No selected value is written to accounting yet.

Preserve the existing maximum **3**. With N=1, at most two subsequent cross-review
attempts remain. Execute exactly one grouped round-02 after guarded migration:
Codex's existing artifact stays intact; Claude, Kimi and Zcode each write only their
own missing file. All preparation/discovery/children share900seconds. One group spends
one cycle; failures stay spent. No launcher retry, driver loop or automatic extension.
This is amendment deliberation, not pilot/packet treatment.

The already tested organizer launcher currently refuses before discovery because no
saved cycle binding exists. It hashes exact round/protocol/config input bytes. Its
current plan digest is `376afb6340454bda51ef30901aa5389c1a23837246598f36a8b1ab062f8531c2` and its process-local config
SHA256 is `35d6706284b22ad772dc16e8b6b8fa14e705ff1d710394b34961e6b31fb4b560`. Config changes only per-invocation settings (900s
and existing exact-file sandboxes); global roster/configuration is unchanged.

## Guarded application sequence after approval

1. Finish outstanding source validation and publish the reviewable draft checkpoint.
2. Record the user's exact count/epoch/unknown-coverage decision. Stop/check all writers
   of this scope; attendance is an execution requirement, not supplied by frontmatter.
3. Re-inspect the same declaration inventory. Refuse changed bytes, additional unknowns,
   changed coverage or a different visible lower bound; do not reuse a stale digest.
4. Use the existing attended `budget migrate apply` with the exact declarations,
   chosen total/epoch, original max3, reviewed history digest, unique decision/reason,
   writers-stopped attestation and explicit yes. Re-inspect its retained decision and
   imported charges. Never call a product API directly to bypass attended CLI checks.
5. Re-preview the launcher/config/artifacts; execute once with the exact approved
   `-expect-digest`. Verify all peer artifacts and unchanged Codex bytes; retain full
   telemetry and failed results. A failed round does not imply consensus or retry grant.

No migration apply, selected count, epoch attestation, treatment, protocol publication,
release, installation, PR merge or closure is included in the current observation.
The pilot and28-call packet trial still need demonstrably enforced prospective USD15
campaign spending, and their frozen inputs/independent grading remain outstanding.
