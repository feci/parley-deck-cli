---
agent: codex-1
idea: meta-protocol-change-evidence-first-efficiency
date: 2026-09-16
status: candidate-controls-only
---

## Retained timed-out implementation

Zcode's N1 implementation invocation `feb014c6-d06a-4d04-b74e-f872656a818b` reached its 30-minute deadline: normalized exit 143/timeout, wrapper exit 1 after 1803.310s. Eight Go files and its incomplete owned note were retained byte-exact; `internal/app/driver_precheck_test.go` was absent. Cost/usage are unknown. No author completion or test claim is inferred. The original failed process remains a spent attempt. Kimi now owns the two app precheck paths and its own new note in a separate 15-minute implementation allocation.

## Executed fixture corrections

The first combined candidate compiled, then failed focused checks: grouped test agents were not marked Found, and the driver test dereferenced a step binding with no finite step budget configured. A separate corrected snapshot retained all outcome assertions, set the actual discovery precondition and a five-step fixture ceiling. That run had 14 passing events but the driver restoration then dereferenced an absent cycle binding. A diagnostic-only overlay showed the actual refusal: `historical cycle requests require explicit accounting migration before first binding`. This refusal is preserved; restoring protocol bytes is not permission to forget the earlier unstarted request.

The final driver witness explicitly admits a fresh cycle scope BEFORE introducing malformed cached context. It therefore exercises an already-bound operation, with zero charges/starts at known refusal, one grouped failed real child after restoration, and no re-charge while awaiting an incomplete round. Sixteen focused test/subtest events passed in 4.961s. Unbound restoration still needs migration; this witness does not claim otherwise. All prior failures and their exact source/configuration are retained under `n1-candidate-validation`, `n1-fixture-correction`, `n1-driver-diagnosis` and `n1-bound-fixture-correction`.

## Real artifact-skip correction

A separate actual-behavior control demonstrated that Zcode's `precheckFirstSelected` inspected an agent whose existing owned artifact the real runner would skip. Four subcases plus their parent failed before correction (1.809s): first completed round participant, fully completed round, completed first reviewer and completed implementation. Overwrite controls passed.

Codex's replacement mirrors each entry point's options, uses the existing `protocolLaunchPhase`, preserves the single implementer/drafter selection, and checks the first PENDING output under the same `os.Stat`/Overwrite predicate as `runAgent`. Review consensus retains its forced overwrite. Existing owned bytes are asserted unchanged. The fully skipped control also invokes the actual grouped runner and observes two skips with no invocation. The new six-case control plus existing N1/precheck tests passed 23 test/subtest events in 8.421s, exact source unchanged. This is candidate execution evidence; independent acceptance is still required for this coordinator change.

## Counterfactual charge control

A two-file overlay restored ONLY the published driver step wrappers and runner monetary precharge boundary while keeping the candidate tests and helper interfaces. It failed in 3.817s with eight test/subtest failure events: optional prechecks ignored, a step spent at the real round's known protocol refusal, and steps spent for missing monetary binding and malformed defaults. These are behavioral failures, not missing-symbol/compile failures. The corrected source's 23-event run above passes those checks. Late refusal still retains previously spent work; no refund or atomic multi-budget transaction is claimed.

## Remaining scope

No candidate Go file is integrated by this note. The newest source is the `n1-skip-control/after-config.json` snapshot (450 Go/module files). Kimi's pending app changes are outside it. Whole current-source full/race/vet and independent acceptance are still required after assembly. No missing-history declaration writer, operator attestation/count, amendment round retry, experiment treatment, release or global change is performed.


## Kimi app correction and independent skip review

Kimi's completed app slice compiled in the combined snapshot. The first focused
run failed only its implementation-refusal fixture (26 passing events): the
shared fixture already contained IMPLEMENTATION.md, so the corrected real skip
predicate properly returned nil. The failed snapshot/log remains retained at
`n1-kimi-app-validation/`. Kimi's own correction removes that existing artifact
only in the pending-launch case, retains every refusal assertion, and adds the
inverse existing-artifact app case. Its new note independently supports the
coordinator runner skip/overwrite correction with located source evidence and
corrects its prior reviewer-versus-builder description. Both owned notes remain
unchanged.

The corrected immutable snapshot has 451 Go/module files (460 total manifested
files). Compile passed in 3.369 s and the combined focused command passed 28
test/subtest events in 15.577 s, with no source mutation. Full affected driver/runner/app tests passed 1,049 test/subtest events in
630.903 s under `n1-complete-validation/`. The coordinator deliberately stopped
the subsequent race command after 125.348 s (SIGTERM, exit -15, 428 passing events)
when independent review required a new source revision. This is neither a race
pass nor a test failure. Vet was not run in that batch; exact source stayed unchanged. Other packages' Go sources equal the validated recovery
checkpoint; these controls do not include Claude's active declared-history work.

The Kimi correction launch observed tracked drift in budget/binding.go from the
concurrent authorized Claude allocation. Only Kimi's exact terminal-owned test
and note hashes were imported into the new snapshot, excluding that budget
mutation. This is not attributed to Kimi. Whole N1 source review subsequently found the fresh-scope MAJOR below; no
whole-audit acceptance, source integration, provider treatment or signoff follows
from this scoped passing command.


## Independent fresh-scope finding and failed correction attempt

Kimi's source review invocation `3dabe3df-3e10-4781-8904-3711eae619ca` identifies
MAJOR-1: a free refused protocol precheck writes requested/terminal receipts
before a fresh driver's cycle binding exists; the later ordinary binding then
correctly refuses the unmigrated history. Repeated restoration attempts can spend
steps before that refusal. The earlier explicitly prebound witness does not cover
production order and must not be offered as proof that this finding is fixed.
MINOR-1 also remains: known exhaustion of one resource can consume another before
its refusal. Late-race failures remain spent; no refund or atomicity is promised.

The review's opening incorrectly attributes a broad full/race/vet PASS to the
coordinator's focused 28-event report. Its original bytes remain preserved; an
author-owned correction is requested. Original N1 production code is Zcode's;
Codex owns the listed fixture and runner skip/overwrite corrections. Kimi's first
correction attempt `41520f00-819c-4fb7-bb0b-033d9a462058` failed after 365.499 s
with explicit `loop.max_steps_exceeded` (30 steps), no owned note and no tracked
source drift. It is not a successful implementation or provider quota failure.
A narrower MAJOR-only correction is in progress with a per-invocation 60-step
limit and 15-minute deadline; no global Kimi configuration was changed.

No N1 Go source is integrated or accepted. All original failures, the passing
old-source tests, and the deliberately cancelled race evidence are retained.
