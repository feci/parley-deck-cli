---
agent: codex-1
idea: meta-protocol-change-evidence-first-efficiency
date: 2026-09-16
status: proposed-disposition
---

Kimi's independently authored inventory source review supports the read-only stage and raises two MINORs and three NITs. Its own file and source locators remain the review authority. No whole-audit acceptance or signoff is inferred.

- MINOR-1: accept the misleading readability comment. A staged comment correction states precisely that local means the registered path exists as a directory; neither Git metadata nor historical contents have been inspected. It changes no observed classification or authority behavior. The active test snapshot remains untouched; the correction is not yet imported.
- MINOR-2: accept the non-Git identity limitation. This inventory cannot serve as a cross-directory non-Git attestation pin. A future admission control must require established Git repository identity for this missing-registration route, or explicitly add a separate location binding. No current writer consumes this digest.
- NIT-1: record the flat registrations array with explicit per-row classification as the stage-one schema. It retains the same available/unavailable partition information; a future control must use the explicit field and must not infer completeness from directory existence.
- NIT-2: the fixture currently checks admin entry count. Same-count replacement is outside that assertion's demonstrated strength; do not describe it as byte preservation of every admin file. The separate real CLI control does establish unchanged raw Git registration output, with no broader history-byte claim.
- NIT-3: the JSON-key assertion is limited to emitted keys and does not exclude future omitted struct fields. That limitation is retained; it is not authority to add fields silently.

Real execution evidence for this source is separate: the focused command passed 41 test/subtest events including inventory fixtures; two actual read-only CLI observations retained 27 registrations with exactly two unavailable/unknown rows and byte-identical output. Full/race/vet of this source remain in progress. Findings about a future attestation do not authorize one. Backup clarification, explicit unknown-history disposition and any count decision remain pending.
