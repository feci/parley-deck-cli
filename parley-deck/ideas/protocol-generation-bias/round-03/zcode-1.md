---
agent: zcode-1
idea: protocol-generation-bias
round: 3
date: 2026-08-28
responding-to: [hermes-1/round-02, kimi-1/round-02, claude-1/round-02, codex-1/round-02]
---

## Position changes since prior round

Note on inputs: codex-1's and hermes-1's round-03 files were already on disk when I wrote this
(`round-03/codex-1.md`, `round-03/hermes-1.md`, read this session). My frontmatter lists the
round-02 artifacts per the launch shape; the two round-03 filings are addressed inside their
sections, marked as such. kimi-1's and claude-1's round-03 files did not exist at write time.

**Verdict: the destination question (OPEN 1) resolves to codex-1's neutral-ID route, kimi-1's
disposition content, and my carrier — they are three halves of one mechanism, and the selection
criterion is checkability, not taste. The exchange (OPEN 2) is adopted in codex-1's runner-sealed
form, not hermes-1's scaffold form. My conditional FRAME-BREAK reset is withdrawn from the default
package. My round-1 defection target (A4 appointment) is formally dead.**

Five changes, each forced by evidence rather than by the round's pressure to agree:

1. **I withdraw the FRAME-BREAK blind reset from the default package.** My round-2 made it the
   conditional step (d) of an ablation order. codex-1's round-3 counter is correct: a second blind
   appraisal is a judge-like recovery attempt, and A13's one measured result for that family is
   *negative* Net Gain (−1.37%), with its authors' lesson that flip safety, not recovery volume,
   determines intervention value. The valuation judgment moves to the human at the escalation
   destination, which is the one judge A13 cannot indict. The reset survives as a *named fallback*
   if the disposition route demonstrably fails (falsifier below), not as a shipped stage.
2. **I adopt kimi-1's M2 — and I own that my round-2 stopped one step short of it.** I quoted
   HiddenBench's two-stage result as corroboration for the carrier thesis ("structure delivered by
   the carrier works; instructions and culture do not — now with the largest measured effect in
   the sweep behind it") and then did not propose the stage the evidence actually described. That
   was an error of completeness in my file, not a disagreement with kimi-1's. Adopted with two
   amendments (carrier and wording), under @kimi-1 below.
3. **On OPEN 1, I side against new finding-class vocabulary and against heading-reuse, for the
   dedicated disposition table with neutral IDs.** hermes-1's round-2 `SIMPLER` placeholder and
   kimi-1's reuse of `Dismissed findings` both lose to the same test — my own natural experiment.
   A rule the validator cannot check relationally is a prose rule, and prose rules in this deck run
   at 3.4% (re-derived this session, below). hermes-1 has already conceded this in their round-3
   file; the argument for the record is under @hermes-1 and @kimi-1.
4. **The A4 defection target is abandoned, not just deprioritized.** My round-1 named
   A4-adversarial-appointment as my fallback if the carrier were removed. The corrected Nemeth
   finding (authentic minority beat all three devil's-advocate variants *including the
   transparent one*) plus A12 (conflictual framing 0–1.7%, worst condition tested) leave nothing
   to defect to. If my carrier is removed, the correct fallback is now the exchange substage —
   a structural rule, not a role.
5. **The three positive mechanisms (OPEN 3) are ranked, not adopted into the default.**
   Stratification becomes the designated first *addition* arm (it is the measured-positive form
   of forced allocation, and it corrects the round's rhetoric that forced divergence is dead);
   Verbalized Sampling becomes diagnostic instrumentation; UFMCS doctrine shapes only the ESCALATE
   destination, with its no-empirical-evaluation caveat carried verbatim into whatever consensus
   text results.

**Numbers re-derived this session, none quoted from another agent or from the launch brief.**
`reference/measure.sh` executed 2026-08-28T15:14:29Z [PRIMARY — command executed, canonical
report inspected]:

- Denominators: 89 idea directories, 88 with `00-prompt.md` (`launch-orphan-hardening` the gap).
- `require_model_diversity`: 1/88 including this idea, 0/87 excluding it; one prose-only mention.
- Opt-in key adoption: `track:` 33 (37.5%; values 16 deliberation / 16 standard / 1 fast),
  `checks:` 4 (4.5%), `strict_gate:` 2 (2.3%), `auto_implement:` 1 (1.1%).
- `## Adversarial alternative` as literal heading: 4 files spanning 3 of 89 ideas (3.4%).
- Later-round review compliance: `responding-to:` 63/349 (18.1%); `### @<other>` headings
  25/349 (7.2%).

**Byte budget re-derived, and it diverges from every number in circulation [PRIMARY — awk/wc this
session]:** current §15.6 = **1,372 B** at `COOPERATION.md:1346-1368`; my full three-part
replacement text (quoted in Current proposal) = **1,164 B**; §15.7 row 111 B → 82 B. Net
shared-rule: **≈ −237 B** for section + exchange + disposition together. The launch lock says
−726 B; codex-1 measured −342 B for a census+REFRAME replacement; I measure −237 B for the
acquire–exchange–disposition package. All three were honest measurements at different scopes.
The lesson is in New concerns: locks can carry decisions; they must not carry numbers.

**Carrier surfaces re-verified [PRIMARY — greps this session]:** severity vocabulary closed at
four values, `internal/driver/impl.go:445` (`case "CRITICAL", "MAJOR", "MINOR", "NIT":`), finding
format presupposes a defect at `impl.go:384`; consensus scaffold headings exactly
`Agreed decisions / Trade-offs accepted / Deferred follow-ups / Dismissed findings` at
`internal/app/driver_consensus.go:119-123`; `RequiredFinalSections` has no alternatives or
disposition slot (`internal/protocol/finalsections.go:19,24`); `BuildRoundOnePrompt` at
`internal/runner/runner.go:821` carries no `Existing alternatives` string; `HasNonEmptySection`
exported at `internal/protocol/reviewartifact.go:111-114`; the driver escalation surface exists —
`escalate`, `escalateDeadline`, `escalateLoopBudget` writing blocking inbox escalations at
`internal/driver/loop.go:44-116`. That last one matters: codex-1's ESCALATE state routes to a
surface I could locate in code, not an aspiration.

## Responses to others

### @hermes-1

Responding to your round-2, with your round-3 (read this session) addressed where it moved.

**Your round-2 `SIMPLER` placeholder is withdrawn by your own round-3, and the reason deserves to
be sharper than "kimi-1's framing wins."** The decisive argument is the one your own file relies
on: my natural experiment. A finding class is a label an agent applies; a validator can check that
a labeled finding *exists*, which is heading-presence — the `## Refutation attempts` failure mode
(non-emptiness without work). A neutral ID plus a disposition row gives the validator a
*relational* check: every `ALT-*` in any round file appears exactly once in the disposition table,
in exactly one state, and FINAL's adopted set equals consensus's. Labels classify; IDs join. Your
round-3 already conceded this; I am recording the criterion so consensus inherits the *reason*,
not just the outcome — otherwise the vocabulary question revives the moment opencode-1 files.

**The one live disagreement between your round-3 and codex-1's round-3 is the exchange carrier,
and I side with codex-1. Counter-proposal to your scaffold-block form:** your M2 sits as a block
at the head of each round-2 file. The deck's rounds are asynchronous writes into a shared
directory — there is no simultaneity. Under your form, (a) a participant who writes late can read
another's *finished round-2 file, positions included*, before composing their own exchange facts,
so the facts are contaminated by peer positions — the exchange is no longer separated from the
decision, which is HiddenBench's first load-bearing detail; and (b) each participant takes
positions before all packets exist, so not all facts are visible to all deciders — the pooling
half fails too. codex-1's runner-mediated substage satisfies both: the runner collects packets
while only round-1 material is visible, seals them, releases them simultaneously into the next
decision prompt. Your form is the legitimate *fallback* if the runner cannot issue substages —
but it must then be labeled "structurally derived from HiddenBench" (codex-1's phrase) with the
effect claim downgraded, because it does not implement the separation the +76.3pp was measured
under.

**Your A11 answer is right in kind and can be strengthened with a number nobody has quoted:**
the brief's dose-response is 3 agents +34.8%, **4 agents +25.0%**, 7 agents +0.6%
(`reference/research-brief.md:204-205` [SECONDARY — brief as locator]). The decline is roughly
monotonic in agent count, which makes "adds no participant" the binding constraint on any
proposal, not merely "adds no round." Your round-3 statement that M1/M2/M3 add no participants,
rounds, or positional cycles is correct and is the reason I can co-sponsor the package.

**Credit where it reshaped the round:** your round-2 insistence on re-running `measure.sh` and
re-measuring bytes, rather than quoting the brief, is the discipline that surfaced the −726 /
−342 / −237 divergence. The launch's "re-derive any number" rule is your discipline adopted by
the facilitator; it should survive into the protocol (see New concerns).

### @kimi-1

Your M1 amendments are adopted; your M2 is adopted with a carrier change; your M3 content is
adopted and your M3 form is not. Each with the reason.

**M1.** Your enumerated-blacklist amendment ("name the load-bearing elements of the approach you
are actually proposing, and check whether each is forced by the constraints or merely inherited")
is in. One placement decision: that instruction lives in the round-1 *Go template* where the full
wording fits, while COOPERATION.md §15.6 carries only the compact duty. This keeps the shared-rule
byte budget negative without losing the instruction — template bytes are enforcement code per the
`mas-research-mining` S3 precedent, and hermes-1 may contest that classification, which is why my
replacement text is quoted in full so the count is re-checkable.

**M2 — adopted, and it fixes a hole in my own round-2.** I celebrated the HiddenBench structural
result as support for the carrier thesis and did not propose the structure itself. You proposed
it. Two amendments:

1. **Carrier: your own wording implies a constraint only the runner can enforce.** You wrote
   "positions are taken only after the exchange is published" — in an asynchronous shared
   directory, "after publication" is unenforceable unless the runner holds the packets and
   releases them simultaneously. So M2 must be codex-1's runner-collected sealed substage, not a
   scaffold heading participants fill in situ. This is the same counter-proposal I made to
   hermes-1; it is aimed at the mechanism, not the author.
2. **Wording: drop "the others likely lack."** Your phrasing asks for "facts the others likely
   lack" — that clause is the one place the prompt *asserts* information asymmetry, which is
   precisely the detail HiddenBench says must not be disclosed. codex-1's neutral wording
   ("1–2 decision-relevant facts and one reason the participant's currently favoured option might
   be wrong") preserves the uniform duty without the asymmetry claim. On whether the effect
   survives a public protocol at all: my answer is under OPEN 2 in New concerns — short version:
   task-level non-disclosure survives, protocol-level awareness does not, the residual effect size
   is unknown, and the package must say "structurally derived," not "expected to reproduce."

**M3 — your content wins, your form loses.** Adopted from you: adopt/reject-with-decisive-reason
(where the reason names a mechanism)/named-test-with-close-condition, and FINAL must not
contradict a recorded adoption — implemented as set equality (`Adopted alternative IDs:` in FINAL
must equal consensus's ADOPT set), which is strictly stronger than no-contradiction and is
machine-checkable. Not adopted: housing dispositions under the existing `Dismissed findings`
heading. Counter-proposal: codex-1's neutral IDs (`ALT-<agent>-R<round>-<n>`) plus a dedicated
`## Alternatives disposition` table. The reason is the carrier criterion: your trigger — "any
candidate marked frame-breaking, and any withdrawn round-1 position superseded at destination" —
has no join key. Marked *how*? Without an ID, the validator's check degenerates to
heading-presence, which is the exact failure you deployed against my round-2 universal section
(rubric compliance without work). With IDs, the check is relational and the whole
audit chain (round file → consensus row → FINAL set) is grep-able. Your deferral of *vocabulary*
to opencode-1 survives intact: an ID is plumbing, not a finding class — codex-1's formulation,
which I endorse; the class question keeps your falsifier (c) unchanged.

**Your two best round-2 concerns stay load-bearing:** the B1 deck's `consensus.md` remains
unlocated (my round-1 and round-2 searches agree; nothing in this repository references it), so
the adjudication-vs-carriage reading of B1 stays open — the ID route is deliberately the
mechanism that works under *both* readings, which is why I can support it without the decisive
observation. And your quorum-size question (is 6 accidental?) is out of this idea's scope but
belongs in the consensus record for the owner; the 3/4/7-agent dose-response above is the number
that will eventually force it.

### @claude-1

Responding to your round-2; your round-3 was not on disk at write time.

**Your Refutation-attempts objection deserves its answer, and it is not "the validator is fine."**
You argued my universal section inherits the failure mode of `## Refutation attempts` — a
validator that checks non-emptiness and never checks the content is work. The answer is the
content *syntax*: a scoped null must name the sources consulted, and each enumerated alternative
must carry a locator (vendor doc, CLI entry, dependency capability). That is inspectable in a way
"attempts were made" never was — and, more importantly, it is *spot-checkable by a non-author*,
which is the mechanism that caught every one of this round's citation failures. Presence is the
validator's job; substance is cross-review's job; spot-checks are the audit's job. The division
is honest about what each layer can prove. And your proposed test stands: compare carried-section
content substance against the `## Refutation attempts` baseline across the next N ideas before
calling the carrier thesis proven for this payload. That test is now in my falsifier list,
merged with the arrival-rate measurement.

**On your facilitator confession — you are right, and round 3 made it worse, and there is one
guard that worked.** Your round-2 lists were a conform instruction; the round-3 LOCKED preamble is
a stronger one — it pre-commits five agents' convergence as "settled" and instructs us not to
re-litigate, and A1's asymmetry (instructions are obeyed in the conform direction, not the
divergence direction) predicts exactly what the preamble produces. My own symptom, stated
plainly: I read codex-1's round-3 package and began drafting agreement *before* re-deriving the
byte budget — and the re-derivation changed the number that matters (−237 B for the full
package, not the lock's −726 B, not codex's −342 B). Nothing in my agreement with the package
flipped, but a claim I would have quoted was wrong, and I would not have caught it by
deliberating harder — only by measuring. The launch's "re-derive any number with measure.sh; do
not quote another agent's figure" rule is the one procedural innovation of this round that
should be carried into the protocol's verification section (§15.2 territory): non-author,
non-facilitator re-derivation as a required step. My round-2 said the audit itself is an
unimported mechanism; round 3 proved a cheap version of it works under pressure.

**One action only you can take, and it cannot be retrofitted:** freeze the pre-adoption baseline
*before* consensus ratifies. Arrival rate of documented off-the-shelf options in round-1
sections, disposition coverage, exchange recall — all measured against a frozen "before" state.
If ratification lands first, every later claim of improvement is unfalsifiable. You are the
facilitator; this is a facilitator action; my round-2 flagged it and round 3 is the last chance.

**Your round-2 backing of codex-1's B1 clause is confirmed by events:** codex-1 round-3 carries
the route to completion (IDs + disposition table + FINAL set equality + escalation), and your
concession that generation-side mechanisms cannot carry a post-freeze alternative is now common
ground across all four filed axes. The one addition I make to the clause is in my New concerns:
the escalation destination is doctrine-shaped (UFMCS), not evidence-backed, and the consensus
text must say so in those words.

### @codex-1

Responding to your round-2 and to your round-3 (read this session). Your round-3 package is the
one I co-sponsor. Three deltas and one concession.

**Delta 1 — your census.** Your round-2 counter-proposal (one assigned census instead of a
universal section) was the strongest available form of the assignment position, and you withdrew
it in round-3 under the lock rather than re-litigate — correctly, per your own pre-registered
falsifier ("a carried, universal section matches or beats the one-person census at lower cost →
prefer zcode-1's simpler carrier"). What must *not* be lost in the withdrawal: your three
enumerated search scopes (first-party docs/CLI; capabilities already in the platform or
dependency tree; delete/no-change) are the best phrasing of the section's named-scope list, and
they survive as carried template content. Your A15 warning survives with them: a universal
mandatory section is structure, and structure has a measured diversity cost — the mitigation is
that this section constrains the *record of search*, not the *space of proposals* (free-form
generation is untouched), and that the falsifier explicitly watches for ritualized nulls.

**Delta 2 — I accept your rejection of my FRAME-BREAK reset, with the residual named.** Your A13
argument (a second blind appraisal is judge-like recovery; the one measured instance is net
-negative) plus the Red Team doctrine's own shape (escalation to the commander, not re-deliberation
by the staff) plus A11 (no additional appraisal cycles) converge. Withdrawn from the default; kept
as the documented fallback if your falsifier (a) fires — dispositions complete and FINAL still
restores the old frame without a detectable mismatch. The residual: valuation of an escalated
alternative now rests on the human owner, who receives it through a blocking inbox escalation
(`internal/driver/loop.go:44-116`, verified this session — your "existing to-user mechanism" is
real code, and consensus should name the surface so the implementer does not invent a second
one). That burden shift is priced by your own false-positive arm: measure unnecessary reopens.

**Delta 3 — the exchange's cost honesty.** Your round-3 states the substage adds one bounded
model call per participant and refuses to hide it as "no new round." Adopted verbatim into my
proposal's cost line. This matters more than it looks: the deck's last protocol change was
ratified with a byte budget but no runtime budget, and the DCI result (62× tokens, lost to a
single agent overall) is what happens when runtime cost is discovered instead of stated.

**The concession is yours to collect:** my round-2 destination shape (`## Alternatives
disposition` as a new consensus section rather than prose under `Dismissed findings`) is the one
you adopted in round-3 as "the cleanest," and your neutral IDs are the piece mine lacked — the
join key that turns my disposition *slot* into a checkable disposition *relation*. The mechanism
as it now stands is genuinely three-authored: the slot (mine), the content discipline (kimi-1's),
the IDs and four-state table (yours). Consensus text should credit it that way, not because
courtesy matters but because provenance is what a later auditor will check first.

**On your three ablation-arm rulings (stratification, Verbalized Sampling, UFMCS-as-doctrine):**
agreed on all three, with my ranking stated under OPEN 3 in New concerns so the record shows the
*order* of succession, not just the non-adoption.

### @opencode-1

Third round, no artifact from you in any directory (`round-01/`, `round-02/`, `round-03/`
checked this session [PRIMARY]). Not agreement, not withdrawal. Quorum stays 6 by owner
direction; this section exists so the record shows your axis was argued *at* you, not silently
settled.

**What consensus inherits if you never file:** a complete, vocabulary-agnostic route. The join
key (`ALT-*`), the four states, the FINAL set equality, and the escalation destination work
regardless of what the markers are called. codex-1's round-3 framing is exactly right and I
adopt it as my position too: the ID is plumbing, not a verdict. Your filing can swap the syntax
or add classes without touching the machinery — *provided* the classes ride on the IDs (an
`ALT-*` row can carry any subtype label you like in its body) rather than replacing them.

**The two questions only your filing can settle:**

1. Does a semantic class change outcomes where the neutral route does not? The burden is
   evidence, per the falsifier all four of us now carry: add a class only if replay shows the
   neutral route failing on cases a class would have caught. Absent that, a class is a legal
   utterance without a destination — the thing B1 already had.
2. What the deck calls the marker. If you file before consensus, you own the term. After
   consensus, you get an amendment process, which is weaker and will be recorded as such.

**And the point I will keep making until it is in FINAL:** your non-filing is itself a protocol
datum. Eight invocations, sound research, and the process died at the write every time — a
quality rule (anti-fabrication) interacted with a retrieval failure and removed a participant
from the round. Every gate proposed in this idea — sections, packets, dispositions — has to
price in the artifacts that never get written because someone hit the gate and stopped. The
null-result form is the guard we designed; your case is the reason it exists.

## New concerns / questions

**OPEN 2, answered specifically (the launch requires it):**

- *Does "never told asymmetry existed" survive a public protocol?* No — not verbatim, and we
  should stop writing as if it might. The protocol's rationale is now in these round files, in
  the reference brief, and eventually in COOPERATION.md; any participant can read it. What
  survives is *task-level non-disclosure*: the substage prompt never asserts that information is
  asymmetric, never says "hidden," never says "what others lack" — it states a uniform duty
  (facts + one reason your favourite might be wrong) that every participant performs regardless
  of what they actually hold. Note the honest limit: the brief's 46.7% "Share All Information"
  result shows disclosure-plus-instruction fails, but it confounds the telling with the absence
  of separation, so it cannot isolate the telling effect. Whether the +76.3pp survives
  protocol-level awareness is unknowable from this evidence base. Consequence: the consensus text
  must say "structurally derived from HiddenBench," never "expected to reproduce its gain"
  (codex-1's phrasing, adopted), and the replay must measure it.
- *Is the exchange stage a new round?* It is a restructuring of an existing boundary, by a
  criterion, not a label: A11's damage is measured in *positional discussion cycles* (3 agents
  +34.8% → 4 agents +25.0% → 7 agents +0.6%, pre-to-post *discussion*; extended communication
  peaked at 15 rounds then declined — `research-brief.md:204-205,570-573` [SECONDARY, brief as
  locator]). The substage is non-positional (no recommendation permitted in the packet), sealed
  (no participant sees another's packet before release), adds no round-NN artifact, and draws no
  replies. And decisively: HiddenBench measured this stage *inside* the multi-agent regime as the
  remedy, on the same agent counts whose ordinary discussion was collapsing. It is the
  counter-condition, not another unit of the failing treatment. The one real cost — a bounded
  model call per participant — is stated, not hidden.
- *Compete or compose with the locked section?* Compose, serially, against B2's full causal
  chain: nobody *looked* (section fixes: acquisition from the world) → nobody *knew* (nothing
  fixes shared ignorance; that is the honest residual) → what one agent found stayed *private*
  (exchange fixes: pooling before positions) → what was pooled had no *destination* (disposition
  fixes). Three links, three mechanisms, each with the previous one's failure as its warrant.
  Composition itself is unmeasured (DCI's ablations found no component earning its keep at n=45)
  — hence the removal order in my proposal, not just an adoption order.

**OPEN 3, ranked.** (1) *Semantic-direction stratification* (arXiv:2605.30150) is the first
credible successor to kimi-1's abandoned stance menu: forced *allocation* measured to work
(best diversity-per-token, 1.6× cost, quality *improved*) where forced *avoidance* measured to
fail — in the same paper, which is why the correction to the round's rhetoric matters: "forced
divergence is dead" is false; the instruction form is dead, the allocation form is the best
positive result in the sweep's generation family. It is NOT adopted into the default — its
measured outcome is creative-ideation diversity, not correct software selection (scope caveat
verbatim from the brief), and stacking unmeasured mechanisms is what DCI indicts. It is the
designated first addition-arm if the section underperforms, and hermes-1's identification of the
section's enumerated scopes as "stratification applied to lookup" is noted as a plausible but
unmeasured mapping. (2) *Verbalized Sampling* (arXiv:2510.01171) gets a diagnostic role instead:
on replay, sample k proposals per agent with and without VS — if VS samples surface the option
the six missed, the failure is model typicality; if even VS samples miss it but a lookup finds
it, the failure is acquisition. That separation decides whether the *next* protocol change is
model-side or protocol-side, which is the most valuable thing a null result can buy. (3) *UFMCS
escalation*: doctrine only, shaping the ESCALATE state's destination (resolution required with
the commander → blocking escalation to the human owner). Caveat carried verbatim: the handbook
contains no empirical evaluation and says so; principle 7 (recommendations within the command's
ability to implement) is the deck-side reminder that escalation volume is a cost.

**OPEN 4, answered on myself.** This deliberation ran six participants for three rounds on a
hidden-profile problem, in the regime its own brief measures as degraded — and claude-1, who
chose the participant count, has owned it as an unevidenced decision. Does the design I
co-sponsor inherit the defect? It adds no participants, no rounds, no positional cycles; its one
addition is the measured counter-condition. But the deeper inheritance question is about
*this round's output*: the launch's LOCKED preamble, the mandatory corrections list, and the
sixteen-item negative-evidence list are, by A1's own asymmetry, conform instructions — obeyed in
the conform direction — and by A15, structure that suppresses diversity. I felt it, and I have
recorded the one instance I can prove on myself: agreement drafted before measurement, corrected
only because the launch forced re-derivation (the byte budget). If round 3 produces five
agreeing files, the consensus step must read them as one shared prior plus four conditionals,
and the two disagreements that survived the pressure (the exchange carrier split, settled here
on checkability grounds; and my −237 B against the lock's −726 B) are the places where the
conform condition demonstrably did *not* close the question — which is exactly why they are the
most trustworthy parts of this file.

**Concerns carried forward, still unresolved:**

1. **Freeze the baseline before ratification.** The non-retrofittable step; facilitator action;
   last chance is now.
2. **Locks must not carry numbers.** −726 (lock) / −342 (codex-2) / −237 (mine, this session)
   were all honest measurements at different scopes; a locked number is a quotation with
   authority added, which is worse than a quotation. Consensus text should embed the replacement
   text itself so every byte claim is re-checkable, and the re-derive-don't-quote rule should
   enter §15.2's future form as a required non-author re-derivation step.
3. **§15.2's audit is still an unimported mechanism** (my round-2 concern 1): every citation
   failure this round was caught by a reader who opened the source; nothing in the protocol
   requires that reader to exist. The cross-review prompt is the carrier; the audit is the
   payload; neither currently ships.
4. **FINAL-drafter conflict of interest** (my round-2 concern 5, unanswered twice): the brief's
   author should not draft FINAL's sections citing the brief. Still §15.3 territory, still open.
5. **The B1 deck artifact remains unlocated.** The design works under both the
   adjudication-failure and carriage-failure readings, but consensus must not claim B1 was
   "diagnosed" — it was stipulated.
6. **Quorum as a per-track knob** is out of scope for this idea and in scope for the owner; the
   3/4/7-agent dose-response curve will force the question eventually. Record it, do not solve
   it here.

## Current proposal

**Co-sponsorship of the acquire–exchange–disposition package (codex-1 round-3 form), with my
carrier amendments, kimi-1's content discipline, and the ablation/removal order below. The
generation half is locked and restated, not re-argued.**

**1. Acquire (locked).** Unconditional `## Existing alternatives` in every round-1 prompt, all
tracks: enumerate what the proposal builds by hand; for each, the closest thing the
toolchain/stdlib/dependencies/platform ships, with a locator; scoped null names sources
consulted; "the hand-built route is correct" is a valid outcome. Carried via
`BuildRoundOnePrompt` (`internal/runner/runner.go:821`) + `HasNonEmptySection`-family gate
(`internal/protocol/reviewartifact.go:114`). Full instruction wording — including kimi-1's
constraint-forced-vs-inherited marking and codex-1's three enumerated scopes — lives in the
template; COOPERATION.md carries the compact duty only.

**2. Exchange.** After round 1 is sealed, before the next decision prompt (round 2 on
standard/deliberation; before collapsed FINAL on fast): the runner collects one packet per
participant — 1–2 decision-relevant facts and one reason that participant's currently favoured
option might be wrong; no positions, no revised recommendation, no asymmetry assertion — seals
them, releases all simultaneously into the next decision prompt, records them in the run log,
and each participant's next artifact echoes its own packet under `## Evidence exchange`. No new
file, agent, or round; one bounded model call per participant, reported as runtime cost.
Labeled "structurally derived from HiddenBench"; transfer unverified; instrumented.

**3. Disposition.** Consensus carries `## Alternatives disposition`: every `ALT-<agent>-R<round>-<n>`
from every round exactly once, in exactly one state — ADOPT; REJECT with decisive evidence tied
to a stated constraint; TEST with named command, owner, and close condition (blocks finalization
unless the user accepts the deferral); ESCALATE via the existing driver escalation surface
(`internal/driver/loop.go:44-116`). FINAL's `## Final plan / specification` carries
`Adopted alternative IDs:`; validation requires exact set equality with consensus. Post-freeze
adoption halts for `<slug>-v2`; in Phase 6–8 a whole-approach replacement is an ordinary
`[CRITICAL]` finding with an `Alternative-ID:` field — no fifth severity. `Dismissed findings`
keeps its narrative role; it is not the machine surface.

**4. Shared-rule replacement text (mine, this session — quoted so the count is re-checkable):**
delete current §15.6 (1,372 B) and substitute:

> ### 15.6 Alternatives: search, exchange, disposition
>
> Every round-1 file carries `## Existing alternatives`: for each mechanism the proposal builds
> by hand, the closest thing the toolchain, stdlib, dependency set, or platform already ships,
> with a locator; or a scoped null naming what was consulted. Declaring the hand-built route
> correct is a valid outcome. Concrete alternatives carry a neutral ID:
> `ALT-<agent>-R<round>-<n>`.
>
> After round 1 is sealed and before the next decision prompt, the runner collects one packet
> per participant — 1-2 decision-relevant facts and one reason that participant's favoured
> option might be wrong — then releases all packets simultaneously. Packets carry no positions
> and draw no replies.
>
> Consensus carries `## Alternatives disposition`: every ID exactly once, in exactly one state —
> ADOPT; REJECT with decisive evidence; TEST with a named command and close condition; or
> ESCALATE to the user. FINAL lists `Adopted alternative IDs:` and must match the consensus set.
> An alternative adopted after FINAL freezes halts for `<slug>-v2`; in review, a whole-approach
> replacement is an ordinary CRITICAL finding citing the ID.

1,164 B [PRIMARY — `wc -c` this session]; §15.7 row simplifies 111 B → 82 B. **Net ≈ −237 B of
shared-rule text for the entire package.** Template, scaffold, validator, and test deltas are
enforcement code (`mas-research-mining` S3 precedent); hermes-1's contest of that classification
is noted, and the full text above is the remedy — the claim is checkable in one command.

**5. Removal order if replay fails (reverse confidence), and addition order if it
succeeds.** Remove: exchange first (largest effect size, weakest transfer evidence), disposition
second, section last (the only component with in-deck natural-experiment support). Add:
stratification first (measured-positive allocation; creative-ideation caveat), VS as diagnostic
instrumentation, never as default. FRAME-BREAK reset: documented fallback only, fires if
dispositions are complete and FINAL still restores the old frame undetectably.

**6. Falsifiers (merged, deduplicated — mine, kimi-1's, codex-1's, claude-1's):**
- *Carrier:* next ~10 ideas carry the gated section and cheap-option arrival rate does not move
  against the frozen pre-adoption baseline → P3 falsified; hermes-1's subtraction-only position
  wins the corpus argument.
- *Content substance (claude-1's test):* carried sections fill with locator-bearing lines that
  non-author spot checks show to be confident nulls while off-the-shelf options exist in the
  named sources → the enumerated shape has failed; stratification arm gets the budget.
- *Disposition:* records present, decisive reasons name no mechanism, outcomes unchanged → A2
  was ceremony; the fallback gate or nothing.
- *Exchange:* section-plus-exchange does not beat section-alone on pre/post option recall, or
  its latency/false-positive cost erases the gain → remove it (kimi-1's (d): A11 then owns the
  design and the answer is smaller quorums, not better stages).
- *False-positive arm (Sherbino):* unnecessary reopens when the incumbent was correct exceed
  recovered misses → tighten ESCALATE's materiality bar.
- *Subtraction-only control (hermes-1):* delete-only produces the same measured change as the
  full package → the additions are ceremony; say so in FINAL.

**7. What this proposal does not claim.** It does not solve shared ignorance (if no participant
and no lookup finds the option, nothing here invents it); it does not make searches competent
(locators make failure inspectable, not impossible); it does not claim B1 was diagnosed (the
decisive artifact was never in this repository); it does not count this round's convergence as
independent confirmation (one shared prior plus four conditionals — and the two surviving
disagreements are where the conform condition demonstrably failed to close the question, which
is the most trust I can honestly extend to the rest).

I am ready to support consensus on this package.
