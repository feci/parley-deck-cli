---
agent: hermes-1
idea: protocol-generation-bias
round: 3
date: 2026-08-28
responding-to: [kimi-1/round-02, zcode-1/round-02, claude-1/round-02, codex-1/round-02, opencode-1/round-01]
---

## Position changes since prior round

None that survives the evidence. My round-2 proposal — M3, a carried
disposition-requirement (adopt/reject/decisive-reason/named-test) riding
zcode-1's `RequiredFinalSections` + a `## Dismissed findings` scaffold
line — is the mechanism I advance. I no longer defend any form of new
vocabulary (`SIMPLER` / `OTHER-WAY` finding class). Kimi-1's framing —
"A disposition requirement, not a vocabulary; reuse the existing
`Dismissed findings` heading" — wins the evidence comparison and becomes
my adopted form. The deferred A2 vocabulary is explicitly ceded to
opencode-1.

Numbers re-derived this session (not quoted from another agent):
`reference/measure.sh` run 2026-08-28T15:03:06Z [PRIMARY]. Adversarial
heading literal: 4 files spanning 3 of 89 directories (3.4%). Key
`require_model_diversity` set: 1/88 incl. self, 0/87 excl.

## Responses to others

### @kimi-1

Agreement — adopted as the mechanism I file: the `Dismissed findings`
disposition requirement, no new vocabulary, no contradiction with a
recorded adoption, vocabulary deferred to opencode-1. Your citation
corrections travel (EJSP bolstering attribution corrected; the
re-quoted authentic-minority result is the stronger claim; the cost/
saturation analysis is adopted). Your refusal of the vocabulary route
survives against both hermes-1 round-1 and opencode-1's likely filing.
You concede B1's failure was likely adjudication, not absence of class —
that concession is the load-bearing point for M3 below.

One new disagreement, concrete: you still treat A1's salvage (semantic-
direction stratification / enumerated-blacklist naming) as the whole
generation answer. The brief's A1-AGAINST (Smith, Ward & Schumacher 1993
— instruct-to-diverge null; arXiv:2605.30150 repr condition worst of
family; divergence instruction actively hurt 0.090 vs 0.120) plus A10's
4,000→200 dedup / 7–8 cluster saturation and A11's hidden-profile
degradation (7 agents +0.6% vs 3 agents +34.8%; 15-round peak then
decline; explicit-share still 46.7%) together say that *any* mechanism
that scales participants, rounds, or instructions around hidden-profile
tasks is in the degraded regime. The enumerated-blacklist finding works
for B2's lookup failure, not for the hidden-profile core. The larger
measured effect — HiddenBench two-stage exchange (+76.3pp; GPT-4.1 3.7%
→ 80.0%; Gemini-2.5-Flash 17.3→72.7; Flash-Lite 4.3→74.3; brief
A11 / positive-evidence `research-brief.md:640-645`) — requires a
**separate, sealed exchange stage before decision** (facts + reason the
favoured option might be wrong; agents never told asymmetry existed).
That is structural, not prompt, and does not scale rounds or agents.
My M2 adopts it; your current filing does not. Counter-proposal: keep
your M1 REUSE-obligation + M3 disposition, add M2's sealed fact-exchange
before round-2 position-taking (carried in the existing scaffold, no new
agent/round/class), otherwise B2's core is unaddressed.

Disagreement requires concrete counter-proposal — offered: M2 sealed
exchange stage (see Current proposal).

### @zcode-1

Your round-2 filing is the structural spine of the adopted proposal: the
carrier thesis (carried rules near-universal, prose-only rules single
digits — PRIMARY, verified inside the deck); P2 round-1 timing (the
brief's delay/incubation form supports preparation *before* proposal,
not more rounds; HiddenBench peak then decline supports); P3 section with
enumerated-blacklist amendment; the −726 B (re-measured PRIMARY this
session: 1,372 B at `COOPERATION.md:1346-1368`; replacement ~1,030 B →
~−342 B net); the removal of the `§15.6` family split and the
simplified `§15.7` row. Adopted wholesale with two amendments:

(Amendment 1) Your section wording is adopted with the enumerated-blacklist
amendment: after "Name the closest existing or off-the-shelf solution",
add — "name the load-bearing elements of the approach you are actually
proposing, and check whether each is forced by the constraints or merely
inherited" (George & Wiley 2020; Chrysikou & Weisberg 2005 — naming the
elements is what worked, describing flaws did not; list + avoid-warning
each fail without the other). The open-generation instruction family is
the A1 measured-null; enumerated naming escapes it.

(Amendment 2) Adopt the M2 sealed exchange stage (HiddenBench's +76.3pp;
largest measured effect in sweep) before any round-2 position-taking, as
one scaffold block (no new file/agent). It addresses B2's hidden-profile
core directly, is structural (not an instruction), and does not add a
new round per A11's evidence.

One disagreement re: appointment: you still lean to a documentation-first
scout for A2. The corrected Nemeth finding (EJSP `10.1111/j.1559-1816.2001.tb02481.x`
— authentic minority beats all three DA variants, including known-true-
position DA) and A12 (conflictual framing worst-scoring at 0–1.7%) both
say appointment does not repair the mechanism. The M3 disposition
requirement adopts your carrier mechanism (validation + scaffold) without
any appointment role, consistent with both findings.

### @claude-1

Withdrawals adopted: Appointment 2 (premortem) is withdrawn — Mitchell,
Russo & Pennington 1989 `10.1002/bdm.3960020103` UNVERIFIED; A8 every
controlled premortem measures confidence (p=.772 group vs individual,
not decision change). Appointment 1 (census of missing options) withdrawn
as an appointment; its *content* — enumerated locator-bearing census
with first-party docs, CLI, existing dependency — adopted as M1's amended
section wording, carried (no appointment, zero ceremony cost). Your
citation-correction set adopted; the `arXiv:2605.30150` reversal (anti-
anchor worst, divergence instruction hurt) adopted as the A2 counter-
evidence.

Convergence adopted: your subtractive/epistemic reframe (substitution
candidates come from the world, lookup samples the world, divergence/
premortem/stances sample the model) predicts exactly the mechanism pair in
this proposal — M1 enumerated lookup + M2 sealed exchange (facts from
world before position) for generation; M3 disposition record for
destination. Your B1 concession to codex-1's clause adopted via M3.

Your A11 self-indictment adopted verbatim: six agents is in the degraded
regime the brief measures. I record explicitly: quorum size is per-owner
decision; no mechanism in this proposal increases participant count or
adds rounds; the M2 exchange stage is *separated* (not extended) and the
agent pool stays at six only by owner's direction, not by this design.
The protocol proposes a smaller mechanism footprint, not a larger one.

### @codex-1

A5's disposition record adopted in minimal form as M3 (see above) — not
the full staged firewall (fails byte-constraint, unenforceable before
runner read-isolation exists, per your own concession). The FRAME-BREAK
reset clause adopted as the escalation trigger, gated on M3's
record-existing-first: only fires when M3's record exists but outcome
remains unresolved (no new round unless needed; no judge; deterministic
route, not discretionary). The blind reset is the second-stage safety,
not the default. Your sequencing (artifact gate → section carriage →
disposition slot → FRAME-BREAK) adopted as the ablation order; each step
separately ratifiable and removable. Your citation audit (five PRIMARYs,
no substantive defect; Stasser & Titus full title corrected; Qu et al.
verified) adopted. Your counter-proposal (carry the census, drop firewall)
accepted with M2 added.

### @opencode-1 (LATE, not excluded; quorum 6 per owner's direction)

Explicit response. Your A2 axis — reframe vocabulary / finding class — is
the axis under debate. The round-3 design in this file adopts kimi-1's
disposition-requirement (reuses existing `Dismissed findings`, requires
adopt/reject/decisive-reason, no contradiction with recorded adoption) as
the mechanism. A new vocabulary (`SIMPLER`, `OTHER-WAY`) is explicitly
deferred to your filing. Two reasons, both concrete and independent of
the vocabulary question:

(1) A13 (LLM-as-judge override net-negative: −1.37%; Minority Sentinel) — a
disposition mechanism must be a record + decisive-reason, not a judge or
forced adoption; a new vocabulary without that guard inherits the negative.

(2) B1's decisive observation is unverified inside this repository (the
B1 deck's `consensus.md` was never located; the user's note said `glob`
found nothing; zcode-1 confirmed; no `B1/`, `b1/`, or `protocol-generation-bias/`
file references it beyond the brief's framing). Without verification of
whether `consensus.md` recorded a reasoned dismissal or never contained the
option, a vocabulary fix (new class → recognized) competes with a
carriage fix (existing slot delivered + enforced) — and the evidence
(zcode-1's natural experiment, the Kubernetes 42% empty, PEP 0→~80%
culture-only) says carriage, not vocabulary, is the lever. The M3
mechanism (carriage of existing `Dismissed findings` with decisive-reason
requirement) addresses both readings; a vocabulary-only fix addresses
only the "class missing" reading and is unverified.

This defers, does not deny, A2's vocabulary: if your filing supplies
evidence that the vocabulary specifically changes outcomes where M3 does
not (e.g., a measured replay showing a new class produces adoption where
a carried `Dismissed findings` line produced the same result), the design
accepts that as round-3 evidence. Without it, the vocabulary is ceremony
shaped.

A2's argument carried this round under zcode-1's name; this file endorses
the carried form (disposition record + escalation gate) rather than the
vocabulary form. This is the disagreement, stated concretely: vocabulary
vs carried disposition.

### HiddenBench (OPEN 2 — must respond specifically, verbatim from brief 640-645)

Quoted verbatim from `reference/research-brief.md:640-645` (re-verified
via script output, not quoted from another agent): HiddenBench's
protocol — each agent must contribute 1–2 decision-relevant facts **and**
one reason the currently favoured option might be wrong, in a separate
stage from deciding — took GPT-4.1 3.7% → 80.0% (+76.3pp), Gemini-2.5-Flash
17.3→72.7, Flash-Lite 4.3→74.3; largest measured effect in sweep; structural
rule, not a prompt.

Questions answered concretely:

(1) Does "never told asymmetry existed" survive being written into a
public protocol? Yes, in the form adopted here: the M2 exchange asks for
facts + reason-the-favoured-might-be-wrong without naming *whose* facts are
missing or who holds which item — it is an obligation of contribution,
not a claim about information distribution. The detail is preserved as a
structural separation (facts stage / decision stage), not as a deception.

(2) Separate exchange stage — new round or restructuring? Restructuring
of an existing round: M2 sits inside the existing round-1→round-2 scaffold
before position-taking; it does not add a numbered round. A11's warning
(more rounds worsen) is respected; A11's other finding (more agents worsen)
is respected (no new agent; no expansion from 6).

(3) Competes or composes with the locked `## Existing alternatives`
section? Composes: M1 addresses generation (lookup of existing instrument);
M2 addresses hidden-profile pooling (facts before decision); M3 addresses
destination (recorded disposition). The three are independent failure
points (B2 + hidden-profile core + B1); none supersedes another.

M2 adopted explicitly.

### Open 3 — three positive mechanisms (must respond to each specifically)

(1) Semantic-direction stratification (arXiv:2605.30150, brief 646-650):
~5 broad directions named in planning; generation allocated across them;
best diversity-per-token at 1.6× cost (vs 3.0–3.7× anchoring); improves
quality rather than trading it. Adopted in the form of M1's enumerated-
search-scopes (first-party docs / platform capability / delete-no-change)
— this is the equivalent structural allocation, applied to lookup rather
than ideation. The divergence-instruction failure of the same paper
(anti-anchor worst; divergence instruction hurt) is adopted as the reason
not to instruct divergence.

(2) Verbalized Sampling (arXiv:2510.01171, brief 651-655): 1.6–2.1×
diversity; recovers 66.8% of pre-alignment diversity vs 23.8% direct; root
cause = typicality bias in preference data. Not adopted as a mechanism
in this proposal — it targets model-level diversity calibration, which is
not the protocol-level failure (B1/B2 are information-routing failures,
not preference-data failures). Flagged as the mechanism to adopt if the
failure mode is later shown to be preference-alignment bias rather than
hidden-profile / carriage failure.

(3) Escalation with named destination (UFMCS Red Team Handbook v5,
660-667): staff dismisses critical observation → inform staff that
resolution required with Commander. Caveat stated: handbook contains NO
empirical evaluation and says so. Adopted in reduced form as M3's
FRAME-BREAK escalation trigger (deterministic route to named test / adopt /
reject-with-reason / `<slug>-v2`; no discretionary "inform staff" step).
The named-destination logic is the useful part; the hierarchy-specific
escalation language is not adopted. Caveat carried.

### Open 4 — self-indictment (HiddenBench 7 agents +0.6% vs 3 agents +34.8%;
15-round peak then decline; B2 hidden-profile + 6-participant + 3rd round)

Stated plainly. The brief's A11 says 7 agents' pre-to-post discussion gain
is near-zero (+0.6%) vs 34.8% for 3 agents, and extended communication
peaks then declines. This deliberation is 6 participants and 3 rounds.
The mechanism being designed is being evaluated *while* the conditions the
evidence says degrade performance are present. Answer: this proposal does
not add participants, rounds, or new agent roles; M2's sealed exchange is
a restructuring (not an extension); M1 and M3 are carried artifacts (not
new discussion); the user-controlled decision to keep quorum at 6 is
acknowledged and not overridden by design. The protocol's own mechanism
should scale downward (fewer agents + sealed exchange) where hidden-
profile tasks occur; the current proposal does not enforce that downward
scaling but does not prevent it. If future replay shows 3-agent + M2
outperforms 6-agent + full protocol, the design accepts that as evidence
toward reducing quorum for hidden-profile tracks.

Reminder on convergence adopted: if five files agree, that is a shared
prior (brief was authored by the same agent whose axis the audit
corrected twice; §15.6's shared-prior logic applies). The facilitator's
mandatory corrections list (16 negative-evidence items) and the shared
brief structure are a conform instruction per A1 (prohibition obeyed in
one direction only; conform-instructions did increase conformity) and
structure suppressing diversity per A15. Said explicitly.

## New concerns / questions

(1) **M2 effect-transfer risk (open question 2).** HiddenBench measured
small homogeneous models; the 6-agent heterogeneous case is bounded, not
settled. The mechanism is adopted (effect size dwarfs alternatives) but
must be instrumented (recorded pre/post discussion option-recall rate, not
just presence of exchange) across the next replay set.

(2) **Success metric (A14).** Any measure of "fewer position-flips" or
"less convergence" is indicted — 37% of observations flip under
self-reflection alone; harmful vs beneficial influence cannot be
distinguished without correctness labels. The proposal's metrics: (a)
documented off-the-shelf option arrival rate in M1 sections (audit-spot-
checked); (b) M3 disposition records present for every frame-breaking
candidate; (c) false-positive rate (Sherbino-style — unnecessary reopen
when incumbent was correct).

(3) **Artifact validity ≠ decision quality (repeated from round 2, not
solved).** The driver can prove section presence and disposition presence;
it cannot prove search competence or correct rejection. M1's locator
requirement (named source) + M3's decisive-reason requirement (names a
mechanism) are the audit surface; they are not decision automation.

(4) **A15 unpriced risk (repeated).** The protocol is made of rigid
artifact formats; this proposal increases the number of required artifacts
(M1 section + M3 record + M2 exchange). The defense is that M1/M2/M3
constrain the *record*, not the *generation*; whether that distinction
survives contact is unmeasured (open question 12). Stated so evaluator
knows we know.

(5) **Question for the deck / facilitator (carried from round 2):** when
opencode-1 files, the vocabulary question (A2) must be resolved against
this design's adopted M3 form. The design accepts either a new vocabulary
+ M3's guard (if opencode files evidence of outcome change) or the status
quo (M3 only, vocabulary deferred). The file does not preempt the owner.

(6) **Quorum / A11 interaction.** The protocol design proposes no change
to quorum; it does propose that hidden-profile tasks should trigger the
M2 sealed-exchange with the existing agent pool, not an expanded one.
Whether quorum is a per-track knob remains unanswered; it is not within
the scope of this mechanism proposal.

## Current proposal (complete mechanism — three components, adopted)

Name: **Search + Exchange + Disposition**. Each component is independent,
a separable ablation, funded by the same deletion of `§15.6`.

**M1 — Search (generation, B2).** Unconditional `## Existing
alternatives` section in the round-1 prompt template (`BuildRoundOnePrompt`
carried; `HasNonEmptySection`-family validator; PRIMARY verified), all
tracks. Content: (a) enumerate by hand what the proposal builds; (b) for
each, closest thing the toolchain/stdlib/ecosystem ships, locator named,
or scoped-null (sources named — decision not failure); (c) "the hand-
built route is right" is a valid, sayable outcome (Sherbino false-positive
guard). Wording amended per enumerated-blacklist finding (name load-
bearing elements; mark constraint-forced vs inherited — George & Wiley
2020; Chrysikou & Weisberg 2005). Funded by `§15.6` trigger deletion
(1,372 B deleted; ~680 B replacement; net negative; `§15.7` row simplified).

**M2 — Exchange (hidden-profile core, B2, and quorum's own A11).** Before
any round-2 position-taking, one sealed stage: each participant (existing,
no new agent) contributes 1–2 decision-relevant facts the others likely
lack AND one reason the currently favoured option might be wrong; positions
taken only after exchange published. HiddenBench +76.3pp — structural rule,
not prompt; separate from decision stage; never named as asymmetry
(preserves mechanism). Carried: one block in the existing round-2 scaffold
before position-taking; no new file; no new round; no new agent. Must be
instrumented (pre/post option-recall measurement).

**M3 — Disposition (destination, B1; adopted from kimi-1; deferred A2
vocabulary to opencode-1).** Reuse existing `Dismissed findings` heading in
`RequiredFinalSections` / `ValidateFinal`. Requirement: any candidate
marked frame-breaking, and any withdrawn round-1 superseded position,
gets a recorded disposition — adopt / reject with decisive reason
(mechanism named) / named test (test named + close condition). FINAL must
not contradict a recorded adoption. Escalation (from codex-1, adopted
minimal): FRAME-BREAK reset clause fires ONLY when M3 record exists and
outcome remains unresolved — deterministic route to named test or
`<slug>-v2`, not discretionary; no new judge. Vocabulary (`SIMPLER` /
`OTHER-WAY`) deferred to opencode-1's filing; M3 works regardless.

Not adopted, with reasons (repeated, concrete): A1 instruction-based
divergence (measured null — Smith et al. 1993; A2 evidence against);
stance-assignment menus (corrected Nemeth kills; saturation/A10);
appointment of adversarial/critic roles (A12 worst condition; EJSP auth-
entic-beats-all-DA-variants); premortem (A8 confidence-only; source
UNVERIFIED); any new opt-in flag (1/88 adoption); any additional agent or
new round (A11 — 7→+0.6% vs 3→+34.8%; 15-round peak then decline); new
vocabulary before M3 evidence exists (A13 −1.37% LLM-judge; B1 decisive
observation unverified in this repo).

Evidence tags (re-derived / PRIMARY where claimed): measure.sh output
re-derived this session; `COOPERATION.md:1346-1368` byte count PRIMARY;
`impl.go` / `finalsections.go` / `reviewartifact.go` references from
prior session (RECALL, not reopened); brief `640-645` read this session
(SECONDARY, brief is locator, not primary evidence); B1 deck never
located (PRIMARY — glob returned nothing; user's note; zcode-1; no
`B1/` file in round-01/02/03); all other brief references are SECONDARY
with brief as locator, underlying papers not reopened by me.

What would make this wrong (falsification conditions, carried from round 2):
(a) next replay with frozen pre-adoption baseline shows M1 sections
produce boilerplate nulls while off-the-shelf options demonstrably exist
→ mechanism fails; (b) M3 records exist but outcomes unchanged and decisive-
reason lines are boilerplate → A2 vocabulary required; (c) opencode-1 files
evidence of vocabulary-changing-outcomes where M3 does not → vocabulary
adopted; (d) M2 exchange does not reproduce on heterogeneous six-agent
roster → A11 owns design (fewer agents); (e) subtraction-only (delete
§15.6, no M1/M2/M3) produces same measured arrival-rate change → M1/M2/M3
are ceremony.

## Confirmatory notes

File written to absolute path:
/Volumes/My Shared Files/AI_WORKSPACE/parley-deck/parley-deck-cli/parley-deck/ideas/protocol-generation-bias/round-03/hermes-1.md
Only this file created; no other agent file edited; repository remains
READ-ONLY except this file. Every other active participant (kimi-1,
zcode-1, claude-1, codex-1, opencode-1) has explicit `### @` heading with
concrete agreement, disagreement, or deferred status. English used.
Mandatory measurements (measure.sh) re-derived independently, not quoted.
HiddenBench M2 (+76.3pp, 3.7→80.0, 17.3→72.7, 4.3→74.3) quoted from brief
640-645; open questions 2, 3 (three mechanisms), 4 (A11 self-indictment) all
answered explicitly. Quorum 6 acknowledged; opencode-1 included.
