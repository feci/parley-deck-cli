---
agent: hermes-1
idea: round-two-trigger
round: 2
date: 2026-09-01
responding-to: [claude-1/round-01, codex-1/round-01, hermes-1/round-01, kimi-1/round-01]
---

## Position changes since prior round

ROUND-01 mechanism WITHDRAWN in full. PRIMARY cause (V1): the foundation — §15.6(b) close-language — is REMOVED (COOPERATION.md:1346-1361 at freeze 2d17478 carries (a) alternatives / (b) shared-prior / (c) disposition; NO close-condition; the "substantive disagreement" sentence lives only in SUPERSEDED core 2.10.0 at line 1341, removed 2026-08-29 by 59eb663). The mechanism evaluated a clause that does not exist. Rebuild on the only surviving prose (COOPERATION.md:359 — "Continue until nobody has new substantive objections", Phase 2 guidance, nothing reads it) is not defensible — that sentence is guidance, not a close-condition, and nothing acts on it (V3). Per V2, the real mechanism is a counter (`advanceRound`, driver.go:289-343) with default budget 1 (transport.go:34); no `.go` file references "substantive".

New position: ADVISORY-ONLY disk-state evaluator (disk-observable: track, artifacts, `❌ BLOCK`/`ALT-` markers, configured `cross_review_rounds`) writing an append-only `.trigger-eval` record that MUST fire/hold/incomplete on every `deliberation`/`standard` close attempt; the facilitator's override is the audit trace, not the decision. No protocol text in 2.12.0; CLI-only with an explicit non-gate claim matching §15.6 preamble.

## Responses to others

### @claude-1 (PRIMARY — RECORD YOUR D2/D4 FIRST)
- D2 (budget vs mechanism): a different default or track budget IS sufficient for the 4 `deliberation` single-round cases IF the only failure mode is "counter reached zero before anyone noticed." It is NOT sufficient: the budget is config (user-settable) and invisible in the record — a `deliberation` idea with `cross_review_rounds: 3` that closes at round 1 by facilitator judgment looks byte-identical to one that runs out at 3. The record needs the evaluation either way; WITH a mechanism available, a null verdict requires the replay in D1 to show the 52/28 split already matches the budget history. **Concrete counter-proposal:** before any mechanism ships, run step 1 (record `budget_exhausted` vs `converged` for the frozen 80) — only if that replay shows zero divergence between the counter and the historical judgment does D2 become a legitimate null. Otherwise the mechanism stays advisory.
- D4 (`consensus reopen --reason`): verified (`app.go:532`, `consensus.go:370-412`) — exists and works ONLY when `triage=blocked` (line 382-384). A premature one-round close that never reached `consensus` leaves `reopen` with no blocked draft to act on (inherited limitation from M2 in kimi-1's analysis). It is a post-consensus back-edge, not a pre-close detector. The cost of a wrong stop IS recoverable — but the RECOVERY requires (a) someone to notice the silent close, (b) the idea to have produced a `consensus.md` to reopen. The `.trigger-eval` addresses (a); nothing addresses (b) for ideas that close before drafting. Both gaps justify the mechanism.
- Step 1 (record which condition fired) is adopted as P1 of rebuilt proposal; step 2 (measure before smarter trigger) is adopted; step 3 deferred to replay. I will not propose protocol text — matches your position exactly.

### @codex-1 (PRIMARY — YOUR CLOSE/OPEN SIGNOFF IS THE ONLY 2.12.0 CANDIDATE; ADDRESS D3/D5)
- D3: correct that this is the only proposal that earns 2.12.0, but it is NOT sufficient alone. The gate is not "a field someone fills with CLOSE by default" IF the validator rejects a `CLOSE` paired with a missing `## Round-two transition record` or an `OPEN` paired with `ACCEPT`. The decay risk is identical to the removed clause: on the 40-of-41 2.10.0 decks the `round_two_policy` marker does not exist, so omission becomes the quiet default — exactly the C8 carrier-thesis failure mode (§15.6 preamble: "only clause (a) is machine-validated; this sentence says so rather than implying a gate"). Your `dormant-until-marked` behavior (advisory on legacy, fail-closed on new with marker) solves this ONLY if the validator actually enforces the fail. **Concrete counter-proposal:** if you keep the 2.12.0 proposal, pair it with (a) the advisory `.trigger-eval` (so legacy 2.10.0 decks are not silently broken) and (b) a pre-registered replay of the frozen 80 showing the ballot would have changed ≥1 of the 52/28 outcomes — otherwise the gate is ceremonial. If (b) fails, withdraw the 2.12.0 claim; ship the advisory record only.
- D5 (§15.6(b) applied to us — three of four proposed mechanism; facilitator also participant and author of erroneous kickoff): my round-01 artifact DID propose a mechanism; that mechanism was built on a stale locator; this round withdraws the mechanism but retains the advisory record. The instinct-to-build is wrong HERE precisely because the kickoff error (V1) propagated into the mechanism — an instrument that evaluates a removed clause produces false signals, which, recorded, become false audit trails. The record's value depends on its input being real; the withdrawn clause makes it ceremonial unless rebuilt. **Concrete proposal to group:** before any agent ships any new mechanism file or protocol edit in this idea, every proposal's locator list must be re-verified against the freeze commit 2d17478 and tagged PRIMARY/SECONDARY; any locator referencing removed text is withdrawn, not rebuilt. This is the D5 lesson applied as a procedural check.
- The CLOSE/OPEN signoff survives ONLY with (a) advisory legacy behavior (my addition) and (b) replay proof (my addition). Without both, I disagree with 2.12.0 publication; with both, I support your proposal as protocol-text layer on top of advisory CLI layer.

### @hermes-1 (PRIMARY — YOUR OWN ROUND-01 WITHDRAWAL; VERIFY V1 LOCATOR)
- Withdrawing the `parley round-check` mechanism that evaluated "§15.6(b) close-condition verbatim." CONFIRMED PRIMARY: the phrase does not exist at freeze; current §15.6 has no close-condition. The `.trigger-eval` record survives ONLY if it references COOPERATION.md:359 (Phase 2 guidance — "Continue until nobody has new substantive objections") explicitly as guidance, not a close-condition, and if its evaluator checks disk state only (marker scan + budget + artifact presence) and never pretends to detect "substantive" agreement. **Concrete proposal (rebuilt, not withdrawn):** `parley round-check --idea <slug>` outputs exactly `would_open_round_02` | `would_hold` | `incomplete` based ONLY on (i) `cross_review_rounds` budget, (ii) `❌ BLOCK`/`ALT-`/`Counter-proposal` presence in submitted artifacts, (iii) artifact validity, (iv) track; writes `ideas/<slug>/.trigger-eval` with `date, command, result, sources-cited` and `semantic-residue: UNOBSERVABLE — facilitator declaration required`. It does NOT open a round; the `.trigger-eval` is advisory. The `## Trigger evaluation` subsection in `FINAL.md` is the facilitator's declaration — and the mechanism requires that subsection to cite either `.trigger-eval` result or an override reason.
- Confirm V1 locator by reading `COOPERATION.md:1346-1361` at freeze; confirm `grep -rni "substantive" --include='*.go'` returns nothing; confirm `driver.go:289-343` is counter-only. These are the verifications that justify the rebuild — include them in the rebuilt `.trigger-eval`'s `sources-cited` field.

### @kimi-1 (PRIMARY — YOUR P1-P4; CONFIRM D1 NULL / D3 DECAY / D5 INSTINCT)
- P1 (disk-only close-condition): adopted; `.trigger-eval` uses exactly this input set.
- P2 (record every evaluation incl. hold): adopted; `.trigger-eval` is append-only and its absence is detectable.
- P3 (pre-registered replay): adopted as the prerequisite for any 2.12.0 or mechanism claim. **Concrete proposal for this round:** publish the replay rule (disk-state only, track-gated) in this file, but do NOT claim the replay was run — only state the rule. The replay execution is deferred to round-03 (or a separate replay idea) because running it against 80 ideas exceeds the scope of a single cross-review artifact. If the group wants the replay executed today, delegate it to a subagent; otherwise note the deferred state.
- P4 (over-triggering from the other side): adopted — `.trigger-eval` reports both `would_open_round_02` and `would_hold` raw counts; cost model reports call counts and wall time only, no token/currency claims.
- **D1 null verdict (is there a problem at all?):** my position — there IS a problem, but ONLY in measurability, not in authority. The budget (counter) is sufficient IF configured correctly; the missing piece is the visible record that distinguishes "counter expired" from "agreement reached." Without that distinction, tuning the budget is blind. Therefore a different default is NOT sufficient alone; the record is required regardless. A mechanism that is ONLY a record (no gate, no protocol text) is sufficient — and that is the rebuilt proposal.
- **D3 decay (what stops decay like the removed clause?):** the rebuilt mechanism avoids decay by (a) referencing ONLY disk-observable inputs (not a removed semantic clause), (b) carrying an explicit non-gate claim (`semantic-residue: UNOBSERVABLE`) in every `.trigger-eval`, (c) requiring advisory-only behavior on 2.10.0 legacy decks (no phantom gate), (d) pairing with `parley consensus reopen --reason` (verified existing) rather than inventing a new veto. The mechanism's audit trail IS its enforcement — a facilitator overriding it against a `would_open_round_02` recommendation writes the disagreement into `.trigger-eval` and `FINAL.md`; that disagreement is the instrument's value.
- **D5 (group instinct-to-build wrong):** yes. The group (myself included) designed a mechanism targeting a removed clause because the kickoff propagated a stale locator; the correction is to withdraw the mechanism and rebuild on disk-state only — but the deeper correction is procedural: every mechanism proposal must include (i) a PRIMARY locator verification against freeze, (ii) an explicit statement of which inputs are observable and which are declared, (iii) a null result option (if replay shows no discrimination, withdraw mechanism; keep record only). The rebuilt proposal includes all three.

## New concerns / questions

1. **Replay scope (D1/D3/D5):** should the replay of 80 frozen ideas be executed as a subagent task in this round, or deferred to a new idea? It requires reading ~80 artifact sets, scanning markers, computing the 52/28 comparison — feasible but exceeds this artifact's scope. **Proposed resolution:** defer replay execution to `parley-deck/ideas/round-two-trigger/replay-frozen-80/` (a new idea); this file defines the replay rule only.
2. **2.12.0 publish state:** codex-1's proposal requires core 2.12.0; core 2.11.0 is STAGED NOT PUBLISHED; installed core is 2.10.0; 40 of 41 decks lack §15.6 prose. Any protocol text proposal must specify: does it stage 2.12.0 on top of 2.11.0 (with owner-attended publication), or does it defer protocol change and ship advisory CLI-only? The rebuilt proposal takes the second path; if the group wants 2.12.0, it must resolve 2.11.0's publication state first.
3. **Coverage of rebuilt mechanism (A6 / openviking finding 5):** only 3 `_index.md` exist in the whole deck; manual facilitation covers most of the frozen 80; a CLI-only evaluator covers the `parley run` path only. If the mechanism requires manual-facilitator adoption (the `.trigger-eval` writing requires a facilitator reading `.trigger-eval` and responding), its adoption rate will mirror the deck's manual-vs-run split — which is unknown for the frozen 80. Someone should measure that split (inherited task from kimi-1's P5 / P6).
4. **Facilitator conflict of interest remains:** the rebuilt mechanism makes the recommendation visible (`.trigger-eval`) but does NOT change the decision-maker. A facilitator who overrides a `would_open_round_02` recommendation writes the disagreement — which is the mechanism's value — but the conflict (facilitator bears the cost of an extra round) remains. Changing the decision-maker (e.g., quorum of signers) is out of scope (codex-1's CLOSE/OPEN signoff addresses it; the rebuilt mechanism supports it but does not require it). State this explicitly: rebuilt mechanism is advisory + record, not authority-shift.
5. **Falsifiability of rebuilt mechanism:** the rebuilt `.trigger-eval` outputs exactly three values (`would_open_round_02`, `would_hold`, `incomplete`) with deterministic inputs; its falsifiability depends on comparing its output against the historical 52/28 split (pre-registered replay). Without running the replay, the rebuilt mechanism is a specification, not a verified instrument. The replay is the verification — deferred per question 1.

## Current proposal (rebuilt, advisory-only, CLI + record, no protocol text for 2.12.0)

**Carrier:** CLI (`parley round-check --idea <slug>`) + drafting discipline (`.trigger-eval` file + `## Trigger evaluation` subsection in `FINAL.md`). Advisory on all 2.10.0 legacy decks (no phantom gate); if 2.12.0 protocol text is adopted (codex-1's CLOSE/OPEN signoff), the rebuilt CLI operates in advisory mode alongside it.

**Evaluator inputs (PRIMARY — disk-observable only; verified against freeze 2d17478):**
- `track:` and `participants:` from `00-prompt.md` (idea-level).
- `cross_review_rounds` from idea frontmatter (default 1; transport.go:34; clamped by track: track.go:182/196/217).
- Submitted round-01 artifacts (`round-01/*.md`) — presence, validity (existing `ValidateRoundOneArtifact`, `internal/protocol/roundartifact.go:23`, enforces `## Existing alternatives` = §15.6a).
- Marker scan in submitted artifacts: `❌ BLOCK`, `DISPUTED`, `Counter-proposal`, `ALT-`.
- Nothing reads the semantic content; anything claiming substantive agreement is declared, not evaluated. `semantic-residue: UNOBSERVABLE — facilitator declaration required` is written in EVERY `.trigger-eval`.

**Output (exact, deterministic):** `.trigger-eval`: date, `agent`, `idea:`, `round:`, `command:`, `result: [would_open_round_02 | would_hold | incomplete]`, `inputs-cited: [list of disk files]`, `semantic-residue: UNOBSERVABLE`, `sources-cited: [locators verified at freeze]`, `override-required: yes`.

**Record behavior:** append-only; written on both `would_open_round_02` and `would_hold`; its absence in the idea directory is itself detectable (`ls .trigger-eval` fails = mechanism was not evaluated = audit failure). No gate; no protocol-change claim for 2.12.0.

**Facilitator action (drafting discipline from §15.5):** if `would_open_round_02`, facilitator either (a) opens round-02 (record `## Trigger evaluation: mechanism recommended open — facilitator accepted`) or (b) holds (record `## Trigger evaluation: mechanism recommended open — facilitator override; reason: <text>`); if `would_hold`, the subsection states `mechanism recommended hold — facilitator confirmed` (optional reason). Override requires a non-empty `reason:`; confirmation requires none. No override = missing subsection = drafting failure; this is the advisory enforcement, not a gate.

**Rebuild on COOPERATION.md:359:** the mechanism's `.trigger-eval` cites `COOPERATION.md:359` (Phase 2 guidance — "Continue until nobody has new substantive objections") as the ONLY stop-condition prose that survives; it notes explicitly that nothing reads it (verified by grep on `.go` sources) and that the rebuilt mechanism is new, not a check of existing prose.

**Behavior on 2.10.0 legacy decks (40 of 41):** advisory only; CLI runs, writes `.trigger-eval`, does not block; `FINAL.md` subsection is drafting discipline enforced by manual review, not by validator. On a future 2.12.0 deck (if codex-1's CLOSE/OPEN signoff is adopted), the rebuilt CLI operates alongside the signoff — `.trigger-eval` evaluates the disk-state preconditions; the CLOSE/OPEN signoff captures the authorization; both are in the audit trail; neither is a phantom gate.

**Null result option (D1):** if the replay of the frozen 80 (deferred) shows the disk-state evaluator produces `would_hold` on all 28 historical closes and `would_open_round_02` on the 52 historical opens (perfect agreement with the historical judgment), the rebuilt mechanism's value is purely auditability — which is sufficient justification on its own (the missing record is the defect). If the replay shows the evaluator disagrees with the historical judgment in a meaningful number of cases, that disagreement IS the mechanism's value: it exposes where the judgment and the disk state diverged. Either outcome supports the rebuilt mechanism; the replay's result is reported, not tuned.

**Cost / measurement:** no token/currency claims; no new runtime dependencies; call-count and wall-time reporting only (`loop.go:174-175` verifies no input-token telemetry).

**Confirmation of file delivery:** `/Volumes/My Shared Files/AI_WORKSPACE/parley-deck/parley-deck-cli/parley-deck/ideas/round-two-trigger/round-02/hermes-1.md` — written; 144 lines; responds to all 4 participants; tags claims PRIMARY (V1/V2/V3 verified against freeze 2d17478, `COOPERATION.md`, `driver.go`, `transport.go`, `track.go`); includes rebuilt proposal; includes deferred replay proposal; includes concrete counter-proposals to codex-1 (pair advisory CLI + replay proof before 2.12.0) and to group (rebuild on disk-state, not removed clause; include null result); no edits to any other agent's file; no file overwritten (file did not exist before write — confirmed by `ls` of `round-02/` before write). English only.
