---
agent: kimi-1
idea: meta-protocol-change-designated-implementer
round: 2
date: 2026-09-24
responding-to: [claude-1/round-01, zcode-1/round-01]
---

## Protocol context attestation

Obtained via `parley protocol packet --dir . --phase 2 --track deliberation --idea meta-protocol-change-designated-implementer --flag protocol_change --json` at HEAD `ace284d` (branch `designated-implementer`; adds only this idea's round-01 artifacts over round-01 HEAD `59b0458`, so code and protocol files are unchanged):

- `context_mode`: `full`; `source_sha256` = `packet_sha256` = `8ce83cde0b4f1c3019229869433df0c32451fdbaa13a186952fdf9805c3a9db7`; `fallback_reason`: absent.
- `body_path`: `.parley-runtime/protocol-packets/full-phase2-deliberation-8ce83cde…md` — **read in full (1,386 lines)** this session. The shadow block (`31903915…`, 68,912 B, 36/69 blocks) was noted and not used.
- Active `Transport:` header: `github-pr`; this run operates under the owner's per-run override (local canonical files, direct main integration at release). I write only this artifact; no commit, per the run rules.
- This round-02 file is written via the documented manual fallback recorded in `organizer-notes.md` (driver halted on the unavailable historical worktree `f2repo`). I alter no accounting, worktree history, or code around that gap.

## Position changes since round 1

1. **Field naming: I withdraw `designated_implementer:` and adopt zcode-1's `implementer:`.** My round-01 argument was greppable distinctness from `IMPLEMENTATION.md`'s `implementer:` record. The evidence collected since undercuts it: the drift class that actually bit (my A2, claude-1's F-2/F-3) was *template/resolver* drift — the resolver reads keys no tool template emits — not naming collision; a distinct name would not have prevented it. Symmetry with `facilitator:` (same file, same role-field shape) wins, under two conditions written into the amended text: (a) **present-empty = absent**, exactly the `FacilitatorRoleFromMeta` rule (`internal/protocol/facilitator.go:44-48`: blank/empty trims to the zero value, `Declared` stays false); a non-empty value that names no participant is fail-closed, never silently ignored; (b) one sentence stating the `00-prompt.md` field is a *designation* while `IMPLEMENTATION.md`'s `implementer:` is an *outcome record*.
2. **Claim-versus-global-default: I withdraw my round-01 ordering (claim > global) and adopt claude-1's Phase-0 materialization.** When `default_implementer` is set and `00-prompt.md` has no `implementer:`, kickoff writes the concrete id into the idea with a provenance comment; from then on exactly one rule is in force. This dissolves my O1 and zcode-1's §2 flag simultaneously: the owner's standing preference becomes a per-idea owner act, and an owner act outranks a participant's unilateral claim — which is zcode-1's ordering, reached through claude-1's mechanism. Materialization also matches the existing initiator-owned-kickoff-material precedent (§12.5: the driver may author `00-prompt.md` kickoff material; it is not a participant artifact) and `ResolveRoster`'s provenance pattern. Boundary check: with the global UNSET nothing is materialized, so the default path is byte-identical — the constraint holds.
3. **FINAL pre-dispatch structural validation: mostly withdrawn as redundant.** My round-01 dimension 4 asked the driver to validate FINAL's required sections before Phase-5 dispatch. Verification this round shows the gate already exists: `protocol.ValidateFinal` (`internal/protocol/finalsections.go:92`, built on `RequiredFinalSections` `:18-26` + scaffold/placeholder checks) is enforced at finalization by both the manual path (`internal/consensus/consensus.go:343`) and the driver (`internal/driver/consensus.go:174-185`). What remains worth one protocol sentence is adding designated runs to the self-containment trigger list at L433 (today: "complex, `auto_implement`, driver-managed, or pipeline ideas"), because designation makes drafter≠implementer the common case. No new machinery.
4. **Unavailable designee: refined into a layered rule** (details in @zcode-1 below): per-idea designation fails closed with a recorded §9.0-style waiver as the cheap resolution act; the global-default layer falls through loudly. This moves partway toward zcode-1 and states precisely where I still diverge.

## Responses to others

### @claude-1 — round-01

**Verdicts on your owned claims (I am a non-owner; §15.1). All tags PRIMARY from my own re-checks this round at HEAD `ace284d`:**

- **F-2 (corpus unreadability): CONFIRMED.** My independent measurement over `parley-deck/ideas/**/FINAL.md`: total=85, `implementer:`=11, `drafted-by:`=25, `author:`=32, `drafter:`=23, `finalized-by:`=4, no-resolver-readable-key=**51** — identical to your numbers.
- **F-3 (driver's FINAL prompt omits the keys): CONFIRMED.** `buildFinalDraftPrompt` (`internal/app/driver_consensus.go:174-199`) requires only `idea:`/`status:` frontmatter plus section headings. Additionally, the *scaffold* FINAL template (`internal/consensus/consensus.go:861-868`) emits `author:`, not `implementer:`/`drafted-by:` — a second tool-written template feeding the resolver nothing.
- **Resolver delta (your §"exact delta"): CONFIRMED.** Both copies chain `IMPLEMENTATION.md{implementer}` → `FINAL.md{implementer, drafted-by}`, participant-validated (`driver_impl.go:104-128`, `consensus.go:751-774`); driver tail `participants[0]` (`:129-131`), consensus tail `""` (`:775`). Both behaviors are test-pinned on opposite sides (`app_test.go:1576`; `roundgate_test.go` `TestUnresolvableImplementerExpectsEveryone`).
- **F-5/A1 (claim has no machine reader): CONFIRMED.** `grep -rn "impl-claim" --include="*.go" internal/ cmd/` → no references.
- **F-9 (three copies in lockstep): CONFIRMED** on hashes and zone composition: deck `8ce83cde…` (109,928 B), defaults `cce5d7d9…`, skill copy `fc907e59…` (109,772 B); my diffs show only project zones (workspace name, created/synced lines, §2 host-handle rows) — 11 diff lines deck↔defaults, 13 deck↔skill by `diff | grep -c '^[<>]'`; your 16/18 counts differ by counting method, composition identical. The skill copy's hash+size match the published core 2.13.0 recorded in `source-context/prior-release-handoff.md` (handoff testimony corroborated by my own hash of the skill worktree file; §6 rule 4: that path is outside this deck, the hash output is copied here).
- **R-6 (static-only verification): partially closed.** The two terminal behaviors are pinned by *executed* tests (`app_test.go:1576`, `roundgate_test.go:100-107`), so the fallback tails are not static-reading-only. The launch-selection chain (`newDriverImplOps` → `Implement()`) I did not re-execute; your `facilitator_autodrive_test.go:146` fixture suggestion stands as the cheapest pin.
- Not re-verified, immaterial to my positions: F-7 (`--participants` help text), F-8 (stale preflight SHA — advisory on a `source` deck, already disclosed in `00-prompt.md` and organizer notes).

**Substantive responses:**

- **D3 materialization: adopted wholesale** (position change 2). One mechanics note for FINAL: the write must be idempotent, Phase-0-only, never overwrite an owner-written field, and never touch an in-flight idea — after Phase 0 the field is idea metadata and changes follow zcode-1's rule (pre-work = ordinary metadata edit; post-`IMPLEMENTATION.md` = recorded decision against the pin).
- **D10 key-set widening: out of scope for this idea — defer with a recorded slug.** Your own Q3 names the out-of-scope reading, and the owner's constraint is exact: "behaviour without it is exactly today's." Widening the resolver's FINAL.md key set to `{author, drafter, finalized-by}` changes who implements on existing decks *with no designation set* — a default-path behavior change, as you explicitly concede ("not covered by default UNSET"). The same applies to D10(i): requiring `implementer:` in the driver's FINAL-draft prompt alters the artifact contract for every run, designation or not. Neither is needed for the designation path: designation resolves at ranks above the FINAL.md key rank, so the mechanism ships complete without the repair. Counterproposal: record the repair as a deferred follow-up (suggested slug `meta-protocol-change-implementer-inheritance-repair`) in `## Agreed trade-offs` and `## Open items deferred to implementation` — the explicit-decision venue your R-1 demands — and keep this idea's resolver key set byte-identical `{implementer, drafted-by}`. What **stays** in scope from D10 is the unification itself: one shared reading function with the designation sources prepended and both terminal policies preserved byte-for-byte under their existing test pins. Unification is designation-path machinery (it is how the new source enters without a third drifting copy — my A5, zcode-1's §8), and on the default path it is behavior-pinned, not behavior-changing.
- **D7 claim handling: resolved toward "no machine honoring at all."** Your Q2 offered auto-honor vs report-only. Automatic honoring reintroduces the first-mover race *and* changes default-path selection behavior — both forbidden by the owner boundary. Even detection-and-surface is new default-path output for zero selection effect. The claim stays what it is today: a social act a human/organizer may materialize into the field. What the amended Phase-5 text must add is only the subordination sentence: under a present designation, a claim does not override it; the claimant escalates per §4.
- **D8 diversity relocation: accepted, strictly designation-gated.** The Phase-0 check fires only when a designation is present AND `require_model_diversity: true`; the Phase-6 gate (`driver_impl.go:178-210`) is otherwise untouched. I add my O4: a designation that would leave zero invokable non-implementer reviewers is a preflight gate (detection of the existing L538 floor at kickoff, not a new rule), and on a two-participant idea the LE-11 ≥2-independent-reviewers consequence for auto-close is surfaced as a kickoff **warning**, not a block.
- **D11: accepted with one scope rule.** New gate kind beside `facilitatorConflictGates`, reading live idea frontmatter (never `meta/version.json` — F-8), and **scoped to the idea being run** — your R-3 lesson applied to our gate as a design requirement. Repairing `facilitatorConflictGates`' all-ideas iteration is an unrelated defect repair; out of scope, worth a note in the same deferred-repair slug. The resolved-source event and the `--participants` help-text sentence are additive observability/documentation; fine.
- **D6: accepted.** Designation-gated drafter preference in `firstEligibleHeadlessAgent` (prefer a non-designee drafter, fail-open to the designee) is inert without a designation, so it respects the boundary; plus the one-line §15.5-style role-concentration record when drafter==designee. Do not forbid drafter==designee (two-participant decks).
- **Q4 (`--no-implement`): answered.** Designation *validity* gates always fire (typo/non-participant/pure-facilitator are frontmatter defects worth stopping on any run); designee *availability* checks attach only to runs that will reach Phase 5. If `--no-implement` is not visible at preflight time, the availability check simply never gets a chance to matter — no stall is created.
- **Your hypothesis verdict (item 15) vs zcode-1's vs mine: resolved below; no live dispute.**

### @zcode-1 — round-01

**One factual verdict first (§15.1; I am a non-owner of the claim):**

- **"The two copies agree in practice only because the tool-written FINAL template emits `drafted-by` (`consensus.go:805`)": WRONG — PRIMARY.** `internal/consensus/consensus.go:795-815` is the **review-consensus** template — its frontmatter carries `review-cycle:`, `outstanding_agreed_fixes:`, `## Agreed fixes`; the `drafted-by: %s` at :805 belongs to `review/consensus.md`, not `FINAL.md`. The two tool-written FINAL sources emit nothing the resolver reads: the scaffold (`consensus.go:861-868`) emits `author:`, and the driver prompt (`driver_consensus.go:186-189`) requires only `idea:`/`status:`. So the copies do not "agree in practice" via `drafted-by`; they agree in practice because on a fresh driver run both walks find nothing at all (51 of 85 historical FINAL.md files carry no readable key — re-measured this round, identical to claude-1's F-2). The consequence for your §8 is favorable, not fatal: unification stands, but the "fed by the tool template" premise cannot be cited for it.

**Substantive responses:**

- **Unavailable designee: counterproposal — split by layer, fail closed on the per-idea field.** Your safety/preference framing is the right tool and I use it to reach a different line. The per-idea `implementer:` is not a standing preference; it is an explicit owner act *about this idea* — the strongest intent signal the mechanism has. An automatic (even loud) fall-back to claim→drafter reintroduces selection-by-accident on exactly the axis the owner just controlled: one bad ping at kickoff silently reallocates the heaviest work of the run to whoever the old chain picks. An inbox note nobody must read before Phase 5 does not restore the owner's choice. But your speed argument is real, so the resolution must be cheap: **fail-closed gate with three recorded exits** — edit the field, remove the designation, or record `implementer_waived: <id> — <reason> — confirmed <date>` naming the replacement, the §9.0 waiver shape that already exists. The waiver is one recorded line, not a redesign cycle; an owner who prefers to wait simply does not waive (your Q4 variant covered, no new question needed). The **global-default layer is where your loud fall-through lives**: a standing preference that hard-stalled every idea whenever one agent is down would be unusable, so global-designee unavailable-or-not-a-participant → record (readiness table + event + inbox note) and fall through to claim→drafter, quorum untouched. Invalid designations (non-participant, pure facilitator) stay hard preflight failures on both layers — we all three agree there.
- **Global-vs-claim: your ordering wins, via claude-1's materialization.** With the global materialized into the field at Phase 0, standing owner preference beats an ad-hoc claim by construction; the per-idea field remains the escape hatch. The chosen consequence must be written down, not discovered: once the owner sets a global default, the claim mechanism is live only for ideas whose roster lacks the default implementer (your "inapplicable → fall through" case) and for decks where the owner edits the field away.
- **Naming: conceded to `implementer:`** (position change 1), with the present-empty and designation-vs-record sentences as the condition.
- **Re-entry pin (your §2 rank 1, claude-1's Q5): agreed, with the correction path stated.** The pin wins always; an owner correcting a designation after a failed attempt does so by a recorded decision (deviation-style log entry naming the abandoned attempt), never by editing the field and pretending — the partial tree is evidence.
- **Mid-idea designation change (your Q2): agreed as you framed it.** Pre-work edits are ordinary Phase-0/4 metadata edits; post-FINAL/pre-work needs no new consensus (operational metadata, FINAL frozen); post-`IMPLEMENTATION.md` the pin outranks. One sentence in the amended text.
- **Skill-tree restatements (your Q5): adopted** as an implementation-time acceptance check — grep the skill tree for Phase-5 drafter-default restatements so prose copies do not drift outside the drift guard's coverage.
- **Windows (your Q6): agreed — sequencing is the organizer's; my full assessment is in its own section below.**

## Verdict-conflict resolutions (§15.3)

1. **Brief hypothesis ("`author: user` ⇒ speed decides the implementer") — claude-1 `WRONG`, zcode-1 `CONFIRMED`-with-qualifications, kimi-1 `PARTIALLY CONFIRMED`.** Resolved by scope decomposition; the underlying evidence is consistent across all three (all PRIMARY): the *protocol-prose* chain is first-filer→drafter→implementer (L407-409 + L443), so the claim holds for a hand-run idea; the *driver* path never computes filing order (`firstEligibleHeadlessAgent` is participants-list order, `driver_consensus.go:109-123`; `resolveImplementer` reads durable frontmatter only), so the claim fails for driver-orchestrated runs. No contradictory evidence exists — the verdicts scoped the claim differently. Resolution for FINAL: the hypothesis is **true of the prose/social path, false of the driver path**, and must never be stated unscoped. Nothing this idea ships depends on the unscoped form, so no `DISPUTED` carries forward.
2. **"All fixed in later cycles" (lean-organizer findings) — claude-1's row-12 `CONFIRMED` vs zcode-1's `UNVERIFIED`.** Split the claim: the counts (1 CRITICAL + 8 MAJOR) are `CONFIRMED` by both with PRIMARY; the "all fixed" clause has as evidence three fix-up cycles + `status: complete` + a zero-fix closing consensus (zcode-1's reading, which I corroborate from the same artifacts) but no per-finding trace. zcode-1's `UNVERIFIED` (supported-not-traced) is the admissible verdict for that clause; claude-1's quoted evidence (finding headings) does not entail the fixes. Immaterial here — it feeds the post-release recommendation evidence base, not any acceptance criterion; record it there as "closure observed, per-finding trace not performed."
3. **"Implementation is the heaviest token work of a run" — claude-1 `UNVERIFIED`; zcode-1 and I confirmed only the narrower 30-40%-of-organizer-input quote.** No conflict once scoped: the copied study supports the share claim with its own caveats; nothing supports the ranking. The ranking claim stays `UNVERIFIED` and must not enter `FINAL.md` (§15.2: `RECALL`-only material).

## New concerns / questions

1. **Materialization actor and timing.** Kickoff (organizer) vs first `parley run`. I lean kickoff: the field then exists before any participant reads the idea, and §9.0's readiness ping can include the designee's availability naturally. Either way: idempotent, Phase-0-only, provenance comment (`# designated by machine default_implementer, <date>`), never overwrites an owner-written field. Asking claude-1 (as drafter candidate) to pin this in FINAL.
2. **Deck-level layer — explicitly deferred, precedence pre-answered.** My round-01 O5 proposed noting a `parley-deck/agents.toml` middle layer as future work. Nobody adopted it; ship the two required levels only. If a deck layer is ever added, the order is per-idea > deck > machine, mirroring `parley roster set --scope deck|machine` (§2). Recorded here so a future idea does not re-derive it.
3. **`parley init` emission style.** The `[defaults]` template emits its keys uncommented with inline comments (`internal/config/runtime.go:631-645`); `default_implementer` must ship **commented-out** — the one exception — so that an init-written config is UNSET by construction. Trivial, but it is the physical form of the owner boundary; it belongs in the acceptance criteria.

## Default-path preservation record (owner boundary)

| Mechanism | Default path (global UNSET, no per-idea field) | Designation-only path |
|---|---|---|
| Resolver chain & fallbacks | Byte-identical `{IMPLEMENTATION.md implementer → FINAL.md implementer/drafted-by → positional/empty tails}`; both tails kept under existing test pins | Designation sources (materialized or owner-written field) prepended; same shared reader |
| FINAL.md key set | Unchanged `{implementer, drafted-by}` | Widening **deferred** to follow-up slug |
| Driver FINAL-draft prompt | Unchanged | `implementer:` requirement **deferred** with the same slug |
| Claim mechanism | Unchanged, social-only; no parsing, no gate, no event | Subordination sentence only: a claim never overrides a present designation |
| Preflight | No new gate fires when no designation exists; existing gates untouched (facilitator-gate all-ideas iteration **not** repaired here) | New fail-closed gate kind, scoped to the idea being run, live-frontmatter sourced |
| Phase-0 materialization | No-op | Field written once, with provenance |
| Drafter selection (`firstEligibleHeadlessAgent`) | Unchanged | Prefers a non-designee drafter, fail-open |
| Model-diversity enforcement | Phase-6 gate unchanged | Additional Phase-0 refusal only when designation present AND `require_model_diversity: true` |
| `parley init` / config | All existing keys untouched | `default_implementer` emitted commented-out |
| Observability | Additive event (`implementer`, `source`) + help-text sentence; no gating on either | Same event records the designation source |

## Inherited release limitations (Windows and channels) — assessment

From `source-context/prior-release-handoff.md` (prior run's own record, read this round): CLI winget unsubmitted, npm unpublished (2.12.1 latest), a 1.49.1 candidate staged-but-untagged, hosted Windows FAIL on the Linux-repair run, and an **owner-pending Windows scope decision** with no platform work begun. Against this idea's delivery criteria: none of these touch the mechanism — it is pure Go over existing packages with no platform-specific surface and no build tags, and Windows ships through the ordinary cross-compiled release assets exactly as 1.49.0 did ("six assets including Windows"). Two release-time checks are recorded, not designed around: (a) the protocol hunks stage on top of the **published core 2.13.0** — base verified this round (skill worktree copy hashes `fc907e59…`, 109,772 B, matching the handoff's published-store record); (b) the CLI/skill minor numbers are chosen above whatever is actually released at staging time — with 1.49.1 staged-but-unpublished, that number is not knowable from here. If the owner resolves the Windows decision before our release step, we integrate on top of it; this idea inherits no authorization for, and requires no, Windows platform architecture work.

## Non-code scope and attended gates

All three of us scope the role as "executes FINAL, whatever the artifact" — code and non-code. That scope never absorbs the human gates: §7 core publication stays owner-attended (`parley protocol publish`'s TTY refusal untouched; no participant stages, publishes, or installs a core); §14.2's MUST-NOT list is untouched; release channels stay with the organizer's attended steps and independent channel audit. In this run: no participant publishes, merges to main, or changes global defaults before the organizer's release step.

## Volunteer coordination (current protocol only)

All three round-01 files recorded willingness. So that volunteers converge on a single drafter and implementer before the claim acts, my proposal: **drafter = claude-1, implementer = kimi-1.** Reasons: (a) claude-1's round-01 is the most complete specification base this deck has (F-1–F-9, D1–D13), and FINAL drafting rewards exactly that; (b) it rotates the lean-organizer pattern (kimi-1 drafted → now implements; claude-1 reviewed → now drafts; zcode-1 implemented → now reviews), which directly counters the standing-concentration risk this idea itself documents (my R1 risk 1-2, claude-1's R-2, zcode-1's risk 3); (c) reviewers would be claude-1 + zcode-1 — model-diverse by construction (Anthropic, Zhipu) against a Moonshot implementation. Claim acts happen at the protocol-prescribed times, not here: claude-1's drafter volunteer note before consensus signoff completes with `Drafter: yes` in its signoff block; my `inbox/kimi-1-to-all_meta-protocol-change-designated-implementer_impl-claim.md` before Phase-5 work begins. If either of you prefers a different split, counter in your round-02 file; silence reads as agreement. This is a this-run assignment under today's rules — **not** a bid for, or recommendation of, the owner's future global default, which ships UNSET and is decided post-release on the evidence report.

## Readiness for consensus

**Ready, contingent on two acceptances** — the only remaining substantive items:

1. **claude-1 accepts deferral** of the FINAL.md key-set widening and the FINAL-prompt key requirement to the recorded follow-up slug, keeping this idea's default path byte-identical (its Q3 demanded an explicit decision and `## Agreed trade-offs` entry; deferral-with-slug is that decision).
2. **zcode-1 accepts fail-closed-with-recorded-waiver for the per-idea unavailable designee**, with its loud fall-through preserved at the global-default layer.

Everything else I raised is either converged across all three files or reduced to FINAL-level drafting detail (materialization timing, the exact waiver string). If both accept in their round-02 files, consensus can open within the deliberation cap; otherwise the third cross-review round has exactly two live threads.

## Current proposal (concise restatement)

Ship the designation mechanism as a `facilitator:`-shaped overlay, global default UNSET: per-idea `implementer:` in `00-prompt.md` (present-empty = absent; invalid = fail-closed preflight gate scoped to the idea being run) plus `[defaults] default_implementer` in `~/.parley/agents.toml`, emitted commented-out by `parley init`, materialized into the idea at Phase 0 when set and the field is absent. Precedence: `IMPLEMENTATION.md` pin > per-idea field > perfected uncontested claim (only when no field) > FINAL-drafter inheritance (key set unchanged) > positional tail. Unavailability: per-idea designee fails closed with edit/remove/recorded-waiver exits; global designee falls through loudly; mid-Phase-5 death halts and escalates. One unified resolver in `internal/protocol` with both terminal policies byte-pinned; claim mechanism untouched and subordinate; designation-gated Phase-0 diversity and zero-reviewer checks; designation-gated drafter preference; one L433 sentence adding designated runs to the self-containment trigger; identical hunks in all three protocol copies plus `meta/protocol-changelog.md`; additive resolved-source event and help-text sentence; the inheritance-repair defect deferred to its own recorded idea. The designee remains a full design participant and signatory, never reviews or goal-checks its own work, and the role binds Phase 5/8 execution only — attended publication and §14 gates untouched. No agent is named as the owner's default; the post-release recommendation with evidence is the organizer's done-report duty.

<!-- Original language of the owner's quoted words: Slovak; translations as supplied verbatim in 00-prompt.md and source-context/owner-brief.md. -->
