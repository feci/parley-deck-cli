# Release preparation handoff — kimi-1 (sole implementer), 2026-09-25

Idea: `meta-protocol-change-designated-implementer`. Prepared under the organizer-authorized
RELEASE PREPARATION step after: technical goal PASS (`goal-check-zcode-1.md`, fresh
non-implementer), signed zero-fix closing consensus (cycle 4, three ACCEPT), record complete
(`status: complete` at `ba46b2a`), and organizer integration of origin/main in both worktrees.
**Nothing here is published, merged, tagged, installed or released; no global config/roster
mutation; no core publication invoked; no TTY allocated.** The accepted residual NIT
(`IMPLEMENTATION.md:325-326` lead-in) is byte-untouched. The product default ships UNSET
(`internal/config/runtime.go:654` still emits the key commented out — re-verified this session).

## Candidate commits (both `[codex-1] … (authored by kimi-1)`)

- **CLI `01fc49526ca66b771b549a01cfbbf4addeaa886b`** (worktree `designated-implementer`, on top
  of merge `eb5f586`) — `VERSION` 1.50.0, `internal/app/version.go` const 1.50.0 (the
  `app_test.go:206` VERSION↔const consistency test passes), `CHANGELOG.md` 1.50.0 — 2026-09-25
  entry. No code change beyond the version const.
- **Skill `a5664d8`** (worktree `designated-implementer-skill`, on top of `a624318`) —
  `package.json` 2.14.0, `package-lock.json` (regenerated via `npm install --package-lock-only`),
  `compatibility.json` skillVersion 2.14.0 / recommendedCli `>=1.50.0`, `CHANGELOG.md`
  2.14.0 — 2026-09-25, `skills/parley-deck/parley-addon.json` REGENERATED via
  `npm run manifest:addons` (never hand-edited). Supersedes `4835a9f`: identical tree
  (`2a7fd7d8…4cb8` both), amended only to normalize committer identity to this repo's local
  `user.name/user.email` (repo-local config; the first attempt fell back to an auto-derived
  host identity). Other five add-on manifests regenerated byte-identical.

## Version selection (live read-only recheck this session)

GitHub latest: CLI `v1.49.1`, skill `v2.13.0`. npm `parley-deck-skill` latest `2.13.0`,
integrity `sha512-EHqVIXPveetgOD/xfZZ2iy+iE0ksyXcWXOXCMLZJOUZWHCYetul17mJvTwTFMjM6Iubm9HkNl/y+voVC16zYGg==`
(matches the organizer's recorded value). Next minors 1.50.0 / 2.14.0 are unclaimed — selection
confirmed against live state, not only the inventory.

## Integration delta and inherited provenance

Organizer merge `eb5f586` integrated `origin/main` `c49b464` automatically, no conflicts. The
inherited code delta is the released 1.49.1 line: `internal/procctl/procctl_linux.go`,
`internal/procctl/procctl_cmdline_linux_test.go`, the acp stderr-reap repair, version/records —
all predecessor-reviewed and released work (hosted acceptance claude-1 PASS, run 36009912946,
Linux x86_64 + macOS; later main run 36062251339 at c49b464: Linux/macOS success, Windows test
failure — predecessor state, owner-held). This idea adds **no platform-specific code**. Diff of
the merged candidate vs `c49b464` is exactly this idea's 11-file designation delta plus the
version metadata above. Skill `origin/main` was already an ancestor; no integration changes.

## Validation (proportionate; integration justified the repeat broad run)

The merged tree had never been suite-tested as a unit (my delta + main's Linux process repairs
together) — that is the meaningful new concern warranting one broad re-run. Run in a clean
detached local-disk checkout of `01fc495` (porcelain empty, go1.27.1 darwin/arm64), serial:
`go build ./...` exit 0; `go vet ./...` exit 0; `gofmt -l internal/ cmd/` lists exactly the five
known pre-existing files; **`go test ./... -count=1 -timeout 25m` — every package ok, exit 0**
(longest: trajectory 661.7s, app 559.5s, runner 119.8s). Log:
`release-delivery/2026-09-25-designated-implementer/logs/go-merged-tree-01fc495.log`.
Prior evidence keeps its exact-commit scope: independent full suite at `d238238` (claude-1,
non-implementer), targeted behaviour evidence at `1bad263`, comment-only delta at `717f3de`,
goal-check live re-execution at `804522c`. **Named platform limitation:** the broad suite ran on
darwin/arm64, so the Linux-specific `procctl_linux.go` paths are not exercised by it; their
evidence is the predecessor hosted Linux runs above, and CI on publication assesses the merged
release (owner-held Windows labels unchanged).

## Skill release preflight (first time run; all green on the committed tree)

- `npm test` → **399/399 pass** (Node incl. manifest `--check`; Python leg 54 tests, python
  3.14). First attempt failed only in `test/design-addons.test.js`: `commonmark` missing from a
  never-installed `node_modules` (environment, not product) — `npm ci` from the regenerated lock
  (environment-only), re-run green.
- `npm pack --dry-run` → ok, `parley-deck-skill-2.14.0.tgz`, 210 files, prepack gate.
- **Actual `npm pack`** → exact tarball (below), contents verified: package.json 2.14.0,
  skillVersion 2.14.0, recommendedCli >=1.50.0, one COOPERATION.md copy.
- `npm run build:portable:current` → ok, `dist/parley-deck-skill-v2.14.0-macos-arm64`
  (sha256 `3af49e8c8f19bdf283197100e73c910c3118c521904f82deb1aa2fb1621e55a7`).
- `bin/parley-deck-skill.js install --target all --dry-run` → exit 0 (log in delivery).
- `bin/parley-deck-skill.js doctor --target all --json` → exit 0, ok, 12 targets, zero
  unhealthy (JSON in delivery).
- **Pre-existing staleness found and recorded:** the parley-deck add-on manifest was already
  STALE at `a624318` (this idea's own skill hunks never regenerated it) — baseline `--check`
  output captured before any edit; regeneration was owed regardless of the version bump.

## CLI builds (six platforms, clean committed checkout)

Script `release-delivery/2026-09-25-designated-implementer/cli/build.py` (authored by me,
following the predecessor pattern): `git clone --shared --no-checkout` + `checkout --detach
01fc49526ca66b771b549a01cfbbf4addeaa886b`, porcelain asserted empty before and after;
`CGO_ENABLED=0 go build -trimpath -ldflags "-s -w"` per target. Assets +
`sha256.json` + `build-provenance.json` + `build.log` in
`release-delivery/2026-09-25-designated-implementer/cli/assets/`:

| asset | sha256 |
|---|---|
| parley-v1.50.0-darwin-arm64 | `14f4c2c630b4de8cbf11b3110fdf0950f9dab83677e4d8fdf6678c392a7a6118` |
| parley-v1.50.0-darwin-x64 | `820590b2cb669001a1ec004f9897ae42197c1e25e6d78f3f3e15ed6cc1c6f24f` |
| parley-v1.50.0-linux-arm64 | `47b4c9185d837b406d1ad00cde86bf94f178d8daa897792817409a72d314f7e4` |
| parley-v1.50.0-linux-x64 | `d9aed7e444b90c0705905b25e286a7e3648668c6cc7c708d9409699a58418746` |
| parley-v1.50.0-windows-arm64.exe | `1cec3b374ba366ad91a35e8e11ccbddddcc0b34148f24641f7c3314568a93bac` |
| parley-v1.50.0-windows-x64.exe | `2514cb3dc4ad2390b7d36896ec4bae9a0b6a2f80a692dca8933f81fb4f78cffe` |

Own-build verification: native binary runs — `parley version` → `parley 1.50.0`;
`go version -m` on darwin-arm64 and windows-x64.exe shows
`vcs.revision=01fc49526ca66b771b549a01cfbbf4addeaa886b`, `vcs.modified=false`,
`CGO_ENABLED=0`, `-trimpath`. Independent reviewer follows per the runbook.

## Artifacts for the organizer (absolute paths)

- **npm exact tarball:**
  `/Volumes/My Shared Files/AI_WORKSPACE/parley-deck/release-delivery/2026-09-25-designated-implementer/skill/parley-deck-skill-2.14.0.tgz`
  — 389,330 B / 210 files; sha256 `681b9076a017e67935403ad6cd9779cafa09a117d168331daca300404336135b`;
  sha512 `897b1d35ed42dd3b24541541ebcf63a630d6a0302d2251f6a72e0e30df43e83ebfd153477044a7f34eff3bf2e16564f0e62a0d8942b69b3ac44f532b7c0e0be7`;
  filelist `…/skill/filelist-2.14.0.txt`. For the organizer's
  `npm publish --access public /Volumes/My Shared Files/AI_WORKSPACE/parley-deck/release-delivery/2026-09-25-designated-implementer/skill/parley-deck-skill-2.14.0.tgz`
  (exact-tarball form; never `npm publish` from the repo).
- **CLI assets/provenance/notes:** `…/2026-09-25-designated-implementer/cli/` (assets/,
  sha256.json, build-provenance.json, build.log, build.py, source/);
  release notes `…/2026-09-25-designated-implementer/cli-release-notes.md` (carries the
  Windows experimental/unvalidated labels; CLI winget held).
- **Skill notes:** `…/2026-09-25-designated-implementer/skill-release-notes.md`.
- **Core staged file:** `/Users/tomasfecko/.parley/staging/COOPERATION-2.14.0.md` — sha256
  `51476d69ed4291b77db63f6855554cd251461c0b8743a303c2a3b0eacaabf67a`, 115,010 B / 1,393 lines /
  0444. Base 2.13.0 template + EXACTLY this idea's 7 reviewed hunks (24 lines); zone integrity
  and AC-1 tail equality (`3621b7a6…f30e` all four copies) verified and documented in
  `…/2026-09-25-designated-implementer/core-staging-provenance.md`. **Owner-attended publish
  command (never executed by me):**
  `parley protocol publish --version 2.14.0 --from /Users/tomasfecko/.parley/staging/COOPERATION-2.14.0.md`
- **Logs:** `…/2026-09-25-designated-implementer/logs/` (merged-tree suite log,
  skill doctor JSON, install dry-run log, portable sha256).
- A `winget/` checkout (organizer's winget-pkgs fork) already exists inside the delivery
  directory; I did not modify it — skill manifest hashes must come from the final release
  assets after the portable workflow uploads them, never from a local build.

## Remaining channel operations (organizer/owner only — none performed by me)

1. Independent participant validation of candidates/builds/staged core (runbook requirement).
2. Merge both branches to their `main` (direct, no development PR, per owner direction);
   immutable tags `v1.50.0` / `v2.14.0`. CLI release creation command is NOT documented
   in-repo (inventory gap) — derived, organizer to confirm:
   `gh release create v1.50.0 <six assets> sha256.json --title "Parley Deck CLI 1.50.0" --notes-file cli-release-notes.md`;
   skill: `gh release create v2.14.0 --title "Parley Deck Skill 2.14.0" --notes-file skill-release-notes.md`
   (release-portable.yml then uploads `dist/*`; re-run/clobber caution — winget hashes only
   from final assets).
3. `npm publish --access public <exact tarball above>`; verify with
   `npm view parley-deck-skill version dist.integrity --json` → 2.14.0.
4. Homebrew BOTH formulae in `feci/homebrew-parley` (url/sha256 from the tag tarballs;
   `brew style`, `brew audit --strict --online`, `brew upgrade`, `brew test`).
5. Skill WinGet `Feci.ParleyDeckSkill` 2.14.0 — one application per PR from an isolated updated
   branch (never reuse the historical 2.11.0 branch); CLI WinGet stays HELD.
6. Install skill to all sessions; participant hash audit of every runtime SKILL.md
   (`doctor --target all --json`, `npm run manifest:check`).
7. Owner-attended `parley protocol publish --version 2.14.0 --from <staged file>` (G2 TTY gate;
   no bypass), then post-release owner-only machine default `default_implementer = "codex-1"`
   in `~/.parley/agents.toml` — post-release configuration, never shipped state.
8. Final completion file stating deferred/owner-only items honestly (releases
   windows-portability's sequencing hold).

## Boundaries kept

No publish/merge/tag/push/install/global-mutation by me; no broad gofmt of untouched files; no
model/effort/quorum change; no helper agents. Host: macOS arm64, go1.27.1, node v26.9.0, python
3.14 — local validation is macOS-only; Windows CI red is predecessor state with owner-held
labels; esbuild's postinstall was blocked by the host's npm install-scripts guard during
`npm ci` (warning only; the portable build succeeded regardless — recorded honestly).
