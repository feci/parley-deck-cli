---
agent: kimi-1
idea: protocol-mutation-diversity
round: 2
date: 2026-08-31
responding-to: [claude-1/round-01, codex-1/round-01, hermes-1/round-01, kimi-1/round-01]
---

## Position changes since prior round

Three changes, all forced by the facilitator's verifications, none by persuasion.

1. **V2 confirms my round-01 concern #4 and I accept the correction fully.** The kickoff's
   "2 ideas reached a fourth round" was wrong; the count is 1, and the cause was a glob matching
   the stray file `protocol-generation-bias/round-03-consensus-aborted-01.md` (PRIMARY —
   facilitator verification V2, today). My surviving numbers (29 / 44 / 6-directories / 1) are the
   ones the record supports; the "7 of 80 reached a third round" figure survives because it counts
   rounds reached, not directories at exactly that depth. I note for the record that this is the
   facilitator's second consecutive kickoff measurement error, and that both were caught by the
   measurement lens doing exactly its job. The deck's rule that a locator is a factual claim and
   must be checked like one (openviking C8) applies to kickoff prompts too.
2. **V1 breaks hermes-1's null rationale, and that moves me.** I no longer hold "no gap exists
   that instrumentation would merely confirm." The gap is real and cleanly stated (V1, PRIMARY):
   current §15.6(a) is an implementation-reuse duty about *tooling*; the removed clause was a
   deliberation duty about *competing positions*; §15.6(b) carries only a disconfirmation fragment
   and assigns nobody. So the question is no longer "is there a gap" — there is — but "does the
   gap cause measurable harm, and what is the cheapest carrier that closes it without recreating
   decay." That is a weaker version of my round-01 null, and I say so plainly.
3. **I soften "blocked on instrumentation" to "instrumentation is payable, and here is the
   price."** Round-01 me named the bar but not the payer. D3 forces the honest answer, given
   below: the bar is payable offline, at zero model-token cost, by the corpus metric I already
   specified — and codex-1's sealed benchmark pays the *other* half of it. I partially withdraw
   the framing that nothing can move until a metric exists; the correct statement is that nothing
   *protocol-shaped* can move until one of two cheap measurements lands.

## Responses to others

### @claude-1

You are the participant closest to my position and the one I disagree with most sharply, because
your proposal is substantially right on the operator and wrong on the sequencing.

- **Agreed, and I adopt it:** the drawn axis is the correct shape for the divergence — the draw is
  where randomness legitimately lives, the axis list is the semantic prior notes §5 demands, the
  recorded seed satisfies determinism, and "null result is a finding" is the release valve that
  keeps it honest. If a divergence duty ever returns to core, your form is the one I would ratify.
- **The disagreement is the carrier, not the operator — and your own concern #2 is the reason.**
  You wrote: the trigger "no substantive disagreement in round 1" was never machine-detected; it
  is a facilitator judgment call, and the facilitator has an interest in not opening extra work
  (your file, Concerns #2). That is fatal to a *core-version* restoration and you did not resolve
  it. A judgment-shaped trigger on a judgment-shaped duty is exactly the unvalidatable compound
  that decayed the first time (§15.6 preamble; `protocol-generation-bias` R3 — SECONDARY, its
  FINAL.md as cited in my round-01). My counter-proposal, per the disagreement rule: **carry your
  drawn-axis operator on codex-1's existing `roles:` field as a third arm in the sealed benchmark,
  and promote it to a core clause only if (a) the corpus metric shows premature convergence is
  separable from correct convergence and (b) your arm clears the same quality gate as the others.**
  You lose nothing but time; you gain a trigger that is measured rather than asserted, which is
  the one thing your round-01 file admitted it did not have.
- **One open hole you did not address:** local competition needs a judge. "Did it name something
  the consensus had not considered?" is judged by *whom*? If the facilitator judges, the
  facilitator is conflicted (your concern #2 again); if participants judge, the assignee is graded
  by the same quorum it was ordered to diverge from. The benchmark form dissolves this — codex-1's
  blind evaluator is the judge — which is another reason the carrier should be the benchmark first.

### @codex-1

Your file is the strongest of the four and I am adopting more of it than you probably expected.

- **Your pilot pays half my measurement bar, and it is the causal half.** My corpus metric can at
  best show that round-01 convergence *predicts* bad outcomes; your paired benchmark shows whether
  a reframe operator *causes* added valid behavioural tuples without quality loss. Both are
  needed, they are complements not rivals, and I withdraw any reading of my round-01 that made the
  corpus metric a prerequisite for your benchmark. Run them in parallel.
- **Two strengthening amendments, offered as concrete counter-proposals rather than objections.**
  First: your single arm (endorsable reframe) should become three arms over one shared baseline —
  your endorsable reframe, claude-1's drawn-axis reframe, hermes-1's lens-swap — because the
  facilitator is right that the operators have converged and only the carriers differ, and a
  shared baseline makes the marginal cost of arms two and three close to the prompts alone
  (12 prompts: 12 baseline + 36 treatment calls = 48 for batch one, not 144). Second: your
  behavioural-tuple collapse has the same judgment problem I flagged in my concern #1 — tuple
  extraction is judgment-shaped and can manufacture either diversity or convergence. You already
  require the schema fixed before outputs are seen; I ask for one more pre-registration: the
  extractor must reproduce the known split (protocol-generation-bias = bad convergence,
  openviking-context-structure = good convergence) before any batch result is believed. Two-point
  sanity check, stated in advance, same as my round-01 R1 mitigation.
- **Your production null stands, for now, and I co-sign it.** No core-version change out of this
  idea. Where I push back: your scoped null risks becoming the permanent ritual you yourself list
  as a risk ("a benchmark can become a permanent ritual even when inconclusive"). The exit
  condition must be written now: if batch one fails its gate, the operator line closes and the
  corpus metric's result decides whether the *gap* (V1) is closed by measurement alone.

### @hermes-1

V1 is a direct hit on the load-bearing premise of your null, and your round-02 position must
change because of it.

- **Your claim that §15.6(a) carries the removed steelman's function is wrong on the object**
  (V1, PRIMARY — facilitator verification today): current (a) is about what the *toolchain* ships;
  removed (a) was about the strongest rejected or unconsidered *position*. These are different
  duties with different objects, and your round-01 null rests on conflating them. Your partial
  concession on (b) is also confirmed: (b) is a disconfirmation fragment that develops no
  alternative and assigns nobody. **There is a real gap. Your null as stated does not hold.**
- **What survives of your file is the instinct, not the conclusion.** "Perturb the generation of
  proposals, not add a retrospective file" is correct, and it is exactly what codex-1's `roles:`
  carrier does — your lens-swap is a generation-side operator, and so is the drawn axis. Your
  stagnation predicate (same recommendation category across two rounds, no adopted ALT-, no
  DISPUTED) is the only *defined* convergence trigger any of us proposed, and I adopt it as a
  candidate detector — with one correction from my round-01 concern #3: stasis is not pathology.
  Your predicate would not have fired on openviking (it closed before two stasis rounds), which
  saves it there, but it would fire on any slow-but-correct three-round idea, and you offer no
  outcome label to tell those apart. That is the corpus metric's job.
- **Counter-proposal, per the disagreement rule:** withdraw the null, and enter your lens-swap as
  the third arm of codex-1's sealed benchmark (same harness, same gate, shared baseline). Your
  operator gets tested against real prompts instead of asserted as inherited infrastructure; if it
  wins, it earns the protocol gate you said would need a meta-change. "Nothing new is needed" is
  not a position V1 still permits you to hold without measurement.

### @kimi-1

Self-review, as required.

- The facilitator states my round-01 position correctly: no operator yet, no metric, no
  convergence detector, no baseline, no token telemetry; n=4 → 6 pairwise distances → corpus-level
  or nothing. I stand by all of it except the framing of the bar, amended above.
- My concern #4 is resolved by V2: the discrepancy was the facilitator's glob, the count is 1,
  and my PRIMARY re-measurement was right. Recorded, not relitigated.
- My concern #5 (statistical power) now does double duty: it is also the answer to D3. Per-idea
  measurement is noise; therefore the bar is a corpus-level metric over 80 frozen ideas, and
  corpus-level measurement is *cheap* — deterministic, offline, zero model tokens, replayable from
  git history. The bar is payable by construction. The honest risk is different: not that the bar
  is never paid, but that the metric is *shallow* (my concern #1) and pays it with counterfeit
  coin. The two-point sanity check is the anti-counterfeiting measure.

## New concerns / questions

1. **The 3-1 split is itself a shared prior and must be recorded as one (§15.6b).** Three of four
   participants recommend no new mechanism; three of four *also* answered the owner's request for
   randomness with structure. The facilitator is right to refuse to treat the split as settled.
   But note what the agreement rests on: notes §5's inefficiency finding (SECONDARY, survey
   material none of us independently verified) plus the n=4 / 2-generation arithmetic (PRIMARY,
   trivial). That is a shared *premise*, not shared evidence — if §5 is wrong, three positions
   weaken together. Recorded as a shared prior, not evidence.
2. **Who pays for the benchmark is unresolved and is the owner's decision.** 48 deep calls for
   batch one (three arms, shared baseline), up to 96 with replication, is a real bill. The
   protocol-change constraint (attended publish by owner) has a fiscal analogue: the spend needs
   the owner's approval before any call is made, and the gate thresholds are policy choices
   (codex-1's own concern). I flag it; I do not decide it.
3. **The corpus metric and the benchmark can disagree, and the tie-break must be stated now.**
   If the metric says premature convergence is separable and predictive but no arm clears the
   quality gate, the gap (V1) is real but we have no safe operator — the answer is "gap, no
   tool," not "ship the least-bad arm." If an arm clears the gate but the metric cannot separate
   good from bad convergence, we have a tool with no trigger — the answer is "tool, no trigger,"
   not "fire it everywhere," which is the unconditional-duty decay D4 warns about. Both mixed
   outcomes end the idea without a core change.
4. **V2 is the second consecutive kickoff measurement error.** One error is noise; two is a
   process signal. Proposed, cheaply: kickoff quantitative claims get a verification line in the
   kickoff itself (command + output), the same discipline §15.6(a) imposes on round-01 files.

## Current proposal

Resolving D1–D5 explicitly.

**D1 — where the mechanism lives: on the existing `roles:` field, benchmark-only, until two
measurements land; in core, if ever, as claude-1's drawn-axis duty with a measured trigger.** The
facilitator's read is correct: claude-1's drawn reframe and codex-1's endorsable reframe differ in
carrier, not operator, and hermes-1's lens-swap is the same family. So the settlement is: one
operator family, one benchmark harness (codex-1's), three arms, one shared baseline, one blind
evaluator, one pre-registered gate. Cost: 48 deep calls for batch one (owner-approved), zero live
rounds, zero core changes, zero `fast`-track impact. The corpus metric runs in parallel at zero
model-token cost on the substrate that already ships (`BuildRoundIndex`, `internal/runner/
round_index.go:96`; `BuildRoundDigest`, `internal/driver/digest.go:48` — PRIMARY, locators
verified in my round-01).

**D2 — hermes-1's null does not hold; the gap is real; whether it is correctly left open is the
empirical question.** V1 settles the object distinction (PRIMARY). A gap existing does not oblige
us to close it — the deck survived 80 ideas with it — but "nothing is needed" is no longer an
available position without the corpus measurement. The gap is correctly left open *pending* that
measurement, and incorrectly left open *forever* if the measurement is never run.

**D3 — I accept my own bar, and here is the cheapest metric that satisfies it.** Decision-unit
Jaccard over the deterministic extractors: per round, per participant, the set of named
mechanisms, ALT- ids, verdicts, and locators from `BuildRoundIndex` output; pairwise similarity
across the four round-01 artifacts; one convergence-distance number per idea; corpus-level over
the 80 frozen ideas; fully replayable, zero model tokens. Entry price: the metric must pass the
pre-registered two-point sanity check (separates protocol-generation-bias from
openviking-context-structure). If it fails the check, the report says "metric inadequate," the
idea closes on the benchmark arm alone, and no model-call extraction is substituted — that
escalation path is where the bar would become unfalsifiable, and I close it in advance.

**D4 — the 2026-08-29 removal was right in form and wrong in completeness.** Right: swapping an
unconditional, unvalidatable judgment duty for a machine-checkable one was correct, and
`protocol-generation-bias` R3's ritualisation warning has already proven out (the duty's trigger
was never machine-detected — claude-1's concern #2, which no round-01 file answered). Wrong: the
swap deleted a *function* (someone is assigned to develop a competing position) and replaced it
with fragments (V1). So restoration is justified only in trigger-gated form — claude-1's operator,
fired by a measured convergence event, never unconditionally. An unconditional restoration would
recreate the decay the preamble warns about; a gated restoration cannot ritualise because it does
not fire every round.

**D5 — the owner's randomness framing, taken seriously, survives in exactly two places.** The
strong version of the owner's intuition is not "noise makes ideas better"; it is the GA-correct
claim (notes §1, SECONDARY) that mutation's job is to preserve raw material against selection
collapse — and a *curated* axis list cannot break priors the curators do not know they have. Our
four-way agreement on structure is a shared prior and I record it as one (concern #1). But at
n=4 and ~2 generations, a stochastic *perturbation of content* spends 25% of the population on a
draw the literature measures as inefficient (notes §5, SECONDARY). What survives: **(a)
stochasticity of the draw, not the perturbation** — claude-1's seeded draw among structured axes
is already genuine randomness with a recorded seed, and it directly answers "nobody chooses who
gets the awkward job"; **(b) seeded jitter of config knobs that already ship** — per-idea draws
over effort/speed/lens assignment from `~/.parley/agents.toml`, testable as a fourth benchmark arm
at the cost of prompts alone, no protocol change (cited as an existing alternative per the
kickoff non-goals, not proposed as the primary mechanism). What does not survive: temperature or
any unrecorded randomness — it fails notes §5, the determinism constraint, and replayability
simultaneously. The owner asked for "a completely different angle"; the honest answer is that the
angle comes from the axis, and the randomness decides *which* angle — that is the owner's idea,
carried faithfully, in the only form the evidence supports.

**Bottom line:** no core-version change from this idea. Two parallel measurements (corpus metric,
three-arm sealed benchmark), both with pre-registered pass/fail gates, both reporting to the owner
before any spend. Promotion of any operator to core requires both measurements to pass and happens
in a new idea. Failure of either closes this line with "no, and here is the measurement" — which
`00-prompt.md` explicitly accepts.
