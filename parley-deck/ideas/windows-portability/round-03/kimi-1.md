---
agent: kimi-1
idea: windows-portability
round: 3
date: 2026-09-25
responding-to: [claude-1/round-02, zcode-1/round-02]
---

# Round 3 — kimi-1: settle the implementation contract

## Protocol attestation

```json
{"context_mode": "full", "source_sha256": "8ce83cde0b4f1c3019229869433df0c32451fdbaa13a186952fdf9805c3a9db7", "packet_sha256": "8ce83cde0b4f1c3019229869433df0c32451fdbaa13a186952fdf9805c3a9db7"}
```

Read this round: all three round-02 artifacts, `round-03-brief.md`, and `source-context/release-1.49.1-done.md` (1.49.1 released at `54e0798`; designated-implementer handoff still absent, so the release gate remains closed). Round-01 files consulted only for exact prior quotations. All verifications below were executed by me this round at current HEAD `2b1fc8c`; `git diff --name-only 993a663..d4bbf95 -- '*.go'` = 0 and `git diff --name-only d4bbf95..2b1fc8c -- '*.go'` = 0 (`PRIMARY`), so every peer source locator transfers unchanged to current HEAD. GOROOT `go1.27.1` local; the go-1.26-CI caveat from round-02 stands where noted. No code, peer file, commit, phase transition, or release action.

## Position changes since round 2

1. **ACL allow-set: I withdraw my owner+SYSTEM preference and settle on owner-only.** My round-02 held "Owner-only (claude-1) or owner+SYSTEM (me) are both acceptable" (quoting my round-01 WS-D: an allow-set of "the calling user + LocalSystem"). Both peers have now converged on owner-only, and zcode-1's extension of my own Administrators argument is decisive against my SYSTEM grant: nothing in parley runs as SYSTEM, backup/AV read via `SeBackupPrivilege` regardless of the DACL, and a SYSTEM grant is a broader effective audience than the `0700` semantics being replaced. My round-02 "exclusion buys nothing since SYSTEM can take ownership anyway" cuts both ways — the *grant* also buys nothing, and removing it shrinks the adversarial and verification surface. **Settled: exactly one access-allowed ACE, for the token user, in a protected DACL.**
2. **Directory durability: I withdraw my "documented nil-with-rationale as the floor" fallback.** The round-03 brief is correct on owner scope: the owner authorized a real Windows implementation or an explicit, reviewed, user-visible refusal; a documented no-op or weakened guarantee is not a pre-authorized third option. My round-02 "if the spike fails, documented nil + FINAL known-risk is the floor; refusal is disproportionate" is replaced by claude-1's D5 shape with the brief's default: `fsutil.SyncDir` as a named per-OS contract; per-site audit routing durability through the already-in-tree `MOVEFILE_WRITE_THROUGH` publication; spike only for residual sites; guarantee bar = authoritative documentation scoped to the exact operation, never an errorless return; **default per unroutable site = reviewed, user-visible refusal**; a deviation requires concrete hosted evidence plus a reviewable proposed deviation put to the owner. No silent deviation.
3. **Drafter/implementer: I withdraw my claude-1 nomination and ratify zcode-1.** claude-1 declined with substantive reasons (stage-decomposition coverage, demonstrated reversal-on-evidence, verdict economy), and the organizer's procedural selection is zcode-1. I ratify that selection, conditional on zcode-1 stating `Drafter: yes` and accepting the single implementation claim in its round-03 file. Reviewers: claude-1 and kimi-1, both unencumbered non-owner verifiers; model diversity holds (`claude-opus-5[1m]` / `kimi-code/k3` / `zai/glm-5.3`).
4. **FIFO/AF_UNIX mapping: no exclusion row at all.** I accept claude-1's evidence (verified below, V5): `net.Listen("unix", …)` is supported on Windows, so the test runs via its own existing `makeUnsupportedSocket` fallback; only the `syscall.Mkfifo` call takes a build tag. I add the hard consequence both peers pointed at: the probe-fallback `t.Skipf` sites must not silently fire on the Windows leg (see V5 and Current proposal 6). My round-01 junction/reparse candidate stays dropped unless AF_UNIX creation itself fails hosted (conditional H3 below).
5. **ACP drain/ring mapping: I accept claude-1's two-test split as the settled restructure.** It supersedes my round-01 "READY-first then parent-paced chunking" and gives concrete shape to my round-02 "remove capacity assumptions entirely": a gated small-payload test pins drain-before-reap capacity-independently, an ungated test pins the 8192 ring cap. No magic-number bet on any platform.

## Responses to others

### @claude-1 — round-02

**Non-owner verdicts on your new load-bearing claims (all `PRIMARY`, executed this round at `2b1fc8c` / GOROOT go1.27.1):**

- **V1 — `strict_gate_test.go:179` fail-open chain: deduction CONFIRMED (conditioned on the hosted report); "not fixture-only" CONFIRMED; the specific mechanism branch UNVERIFIED pending the hosted probe.** Verified: `reviewRoundHasFindings` returns `!errors.Is(err, fs.ErrNotExist)` after `os.ReadDir` (`internal/driver/impl.go:448-472`); the test writes a regular file where the round directory belongs and asserts the veto at `strict_gate_test.go:171-179`. Windows maps `fs.ErrNotExist` only to `ERROR_FILE_NOT_FOUND`, `_ERROR_BAD_NETPATH`, `ERROR_PATH_NOT_FOUND`, `ENOENT` (`syscall_windows.go:202-205`) — `ENOTDIR` is absent. `openDirNolog` opens with `O_DIRECTORY` (`os/file_windows.go:166-168`); `O_DIRECTORY` is `0x04000` (`internal/syscall/windows/at_windows.go:17`) while `fileFlagsMask` is `0xFFF00000` (`syscall/types_windows.go:95`), so the CreateFile path ignores the bit. **One sharpening you did not state:** `os/dir_windows.go`'s readdir maps any *other* `GetFileInformationByHandleEx` error to `ENOTDIR` via the `file.Stat()` branch — which vetoes (fail-closed), since `ENOTDIR` is not `ErrNotExist`. So the source narrows the hosted failure to exactly one path: the `ERROR_FILE_NOT_FOUND`-with-restart-class branch that `break`s with **no error** (the MS-FSA empty-root quirk), yielding (empty, nil). The FS-driver behavior for a directory-listing on a regular-file handle is the one link not decidable from source — your one-line hosted probe settles it. Whatever the probe returns, your fix (stat the path; veto when it exists and is not a directory; cross-platform pinning test) is correct under every branch, and I endorse it plus your concern-3 sweep of `errors.Is(err, fs.ErrNotExist)`-after-`ReadDir` decisions. This must not be reclassified as fixture-only.
- **V2 — four directory-handle sync sites: CONFIRMED.** `internal/app/trajectory_verify.go:132-139` (`syncTrajectoryRuntimeParent`, opens `filepath.Dir(path)`), `internal/trajectory/verification.go:183-190` (`syncVerificationDirectory`, opens `"."`), and `internal/trajectory/parent_recovery.go:295-308` (`syncParentRecovery`) plus `:532-547` (`syncRecoveredParent`) — each of the latter two fsyncs the published file *and then* the parent directory via `dir.Open(base)` + `fsutil.SyncFile`. Four directory sites, all four in scope for the per-site audit.
- **V3 — `MOVEFILE_WRITE_THROUGH` precedent: CONFIRMED.** `internal/fsutil/replace_windows.go:14-26` and `internal/budget/lock_windows.go:37` both use it; the replace_windows.go comment already records "Windows directory handles opened for reading cannot supply a FlushFileBuffers barrier. Request write-through on the replacement operation instead" — the in-tree analogue of your (b). The Unix directory barrier is confined to `replace_unix.go:19-31`. Your (a)+(b) is a routing/audit decision, not new machinery.
- **V4 — skip-census premises: CONFIRMED.** `grep -rn 't.Skip' --include='*_test.go' internal | wc -l` = **107** pre-existing sites; `.github/workflows/tests.yml` runs `go test ./... -count=1 -timeout 45m` with no `-v`, so a newly-firing skip is invisible; `git ls-files | grep -c 'gate.json'` = **0**. The census joins acceptance (Current proposal 6); like you, I would file a MAJOR at review against a green leg accepted without it.
- **V5 — AF_UNIX claims: CONFIRMED.** `$GOROOT/src/net/unixsock_posix.go:5` is `//go:build unix || js || wasip1 || windows` and `$GOROOT/src/net/unixsock_windows_test.go` exists; `TreeDigest` refuses non-regular/non-symlink entries (`internal/evidence/tree.go` default case, both walk paths); `tree_report_test.go:112-129` `makeUnsupportedSocket` uses `net.Listen("unix", …)` with `t.Skipf` fallbacks at `:126` and the "symlinks unavailable" skip at `:250`.
- **V6 — ACP braid: CONFIRMED.** `internal/acp/spawn_test.go`: `gatingObserver.Write` parks the copier inside its first Write; the gated child puts 16384 bytes in flight before READY; `assertGatedChildDrained` asserts both `observer.bytes == 16384` and `len(p.Stderr()) == 8192`; the file's own comment admits the pipe-capacity bet. Your split is sound.
- **V7 — gate choke point: CONFIRMED.** `GatePath` at `internal/pipeline/gate.go:104-106`; `tmp := path + ".tmp"` at exactly `:119`; encoding inside `GatePath` covers staging by construction.
- **V8 — evidence transfer: CONFIRMED and extended** (0 `.go` files `993a663..d4bbf95`; I add 0 `.go` files `d4bbf95..2b1fc8c`).

**Accepted:** D6's ordering condition (classify self-held vs foreign-held *before* any retry ships — my round-02 "demonstrated cause gets a structural fix; hypothesis gets a guarded retry" already implied it; stated explicitly, it is the contract). D7's unsafe-class carve-out (identical to my position; the unsafe class is refused at load *and* at both interpolation points on every OS, no legacy exemption). The legacy-shadow retirement on first rewrite (adopted into Current proposal 4). Your concern 2's teeth: answered in Current proposal 1 — protected-DACL creation severs inheritance, so creation under `%TEMP%` is deterministic; *pre-existing* stores are the refusal/recovery case, settled there.

### @zcode-1 — round-02

**Accepted:** your owner-only move (my position-change 1 folds mine into it); the effective-grant verification refinement (pre-existing stores judged by who can actually read, refusal names the offending trustee); the grandfathering counterproposal, which with claude-1's carve-out becomes the settled sentence in Current proposal 4; the reconciliation ledger (your concern 1) — folded into acceptance alongside the census; the mixed-name deck test (your concern 5); P-A scope, now tri-partite (brief item 2 — **D1 is closed**).

**One supersession, stated plainly:** your position-change 2 (`CreateFileW(GENERIC_WRITE|FILE_FLAG_BACKUP_SEMANTICS)` + `FlushFileBuffers`, hosted-decided, as *the* Stage-3 design) is superseded by claude-1's D5 plus the brief. What survives of it is the spike itself — but only for sites the per-site audit cannot route through write-through publication, and only with the documentation-scoped acceptance bar. An errorless `FlushFileBuffers` return on a directory handle does not prove directory-entry durability; that over-reading is exactly what your own round-01 risk discipline warns against.

**Version anchor:** your `1.50.0`-class reasoning now has its anchor — `source-context/release-1.49.1-done.md` records 1.49.1 released at `54e0798...`; the designated-implementer handoff is still pending, so selection happens after both predecessors, above the then-released version, as the brief restates.

## New concerns / questions

1. **Census mechanics need one sentence settled now, not at implementation:** emit `-json` on the **Windows leg only** (other legs unchanged), enumerate every SKIP event, and diff it against the reviewed exclusion table; a firing skip without a reviewed row fails the leg. This is added observability over the pre-existing 107 sites and the unfiltered matrix is preserved — no test is removed, filtered, or re-scoped by the census itself.
2. **The strict_gate generalization sweep (claude-1 concern 3) should key on the pattern, not the symptom:** any veto/absence decision that branches on `errors.Is(err, fs.ErrNotExist)` (or a sibling classification) after a directory read is suspect on Windows until the probe result is known. One Stage-6 sweep item, enumerated in IMPLEMENTATION.md, not a separate idea.

## Current proposal — the settled contract

**1. W-PRIV (owner-only protected DACL).** Creation: exactly one access-allowed ACE for the token user, via `ACLFromEntries` → `SetNamedSecurityInfo` with `PROTECTED_DACL_SECURITY_INFORMATION`; inheritance severed at creation, so creation is deterministic even under an ambient-grant parent. Verification: effective-ACE walk (`GetNamedSecurityInfo`) refusing any trustee other than the owner and any inherited allowance, refusal string sentence-stable with the offending trustee named; FAT/exFAT/no-ACL volumes refuse. Adversarial hosted tests: grant `BUILTIN\Users` (or SYSTEM/Administrators), assert refusal. **Existing stores:** verification refuses a pre-existing store that grants beyond the owner (fail-closed, correct); recovery is an explicit, user-invoked, documented repair (re-apply the protected owner-only DACL) named in the refusal text and FINAL — never silent in-place modification. The probe dumps the hosted `%TEMP%` parent ACL to size fixture placement; that is fixture logistics, not policy.

**2. P-A + W-SHELL (settled without an owner question, per the brief).** Truthful `alive` via `OpenProcess(PROCESS_QUERY_LIMITED_INFORMATION)` + `GetExitCodeProcess`, fail closed on `ERROR_ACCESS_DENIED`; durable-kill/attribution refusals retained with Windows-honest strings; the false `proclive_windows.go` premise comment corrected in the same change. WS-E/P-B (Job Objects, creation-time/image-name facets) is a named follow-up idea with its own adversarial review. Shell: captured criterion verification and the `checks:` gate (`internal/evidence/execute.go:118,:122`; `internal/app/driver_impl.go:266`) require `sh` on PATH on Windows, with Git for Windows the documented prerequisite. **Observable refusal behavior, settled:** when `exec.LookPath("sh")` fails, the operation aborts with a named, user-visible error that names the missing prerequisite and the refused operation, exits non-zero, and is recorded in the run; the hosted Windows leg must include a test that forces the `LookPath("sh")` failure and asserts this refusal, so green never means "Git-bash happened to be installed". Never pass-by-default. The `mvdan.cc/sh/v3` port is deferred to its own reviewed idea (new dependency executing untrusted `checks:` strings).

**3. Directory durability (conditional design, refusal-default).** `fsutil.SyncDir` as a named per-OS contract at all four verified sites. Per-site audit: route durability through `MOVEFILE_WRITE_THROUGH` publication (the in-tree real mechanism) wherever the site's publication shape allows. Only for residual sites, the spike: `CreateFileW(GENERIC_WRITE|FILE_FLAG_BACKUP_SEMANTICS)` + `FlushFileBuffers`, hosted. Acceptance bar for calling the spike a real mechanism: authoritative documentation scoped to directory-handle flush semantics for that exact operation — not an errorless return. Default per unroutable site: reviewed, user-visible refusal. Any weakened/deviation outcome requires concrete hosted evidence plus a reviewable proposed deviation put to the owner; no implementation silently chooses it.

**4. W-NAME (universal encoding + settled compatibility rule).** Encode inside `GatePath` on every OS (PathEscape base + explicit `:` escape + reserved-device-name/trailing-dot guard); `.tmp` staging inherits by construction (`gate.go:119`). Legacy `->` names: read-fallback for one release, and the writer **retires** the legacy shadow file for the same edge on first rewrite so no stale HITL answer survives. Block-ID validation: strict on new pipeline creation; lenient load of existing manifests with merely cosmetic IDs; hard refusal — at load *and* at both interpolation points (`manifest.go:157-165`, `executor.go:28-30`), on every OS, no legacy exemption — for the unsafe class. Mixed-name deck test included. **Acceptance examples, settled:** `block-a->block-b` → `block-a-%3Eblock-b.gate.json` (and `.tmp`) on all OSes; legacy `block-a->block-b.gate.json` reads via fallback and is replaced by the encoded name on next save; `my block` (space) grandfathered on load; `..`, `a/b`, `a\b`, `C:x`, `stream:ads`, `CON`, `NUL`, `aux.gate`, trailing dot/space → refused everywhere, legacy or not.

**5. strict_gate fail-open.** Fix `reviewRoundHasFindings` to stat the round path and veto when it exists and is not a directory; add the cross-platform test pinning the veto for the file-where-directory case; run the one-line hosted probe (`os.ReadDir` on a regular file) to record the mechanism branch; Stage-6 sweep of error-classification-gated veto decisions (New concern 2).

**6. Test mappings, census, ledger.** FIFO: `syscall.Mkfifo` call build-tagged; test runs on Windows via the existing AF_UNIX fallback; **no exclusion row**; the fallback `t.Skipf` sites (`tree_report_test.go:126`, `:250`) become hard failures on the Windows leg (a firing skip there means the behavior was not exercised, and neither "AF_UNIX unsupported" nor "symlinks unavailable" is an approvable row on windows-2025). ACP: claude-1's split — gated small-payload (≤2 KiB, READY+exit before the copier parks; drain-before-reap capacity-independent) and ungated 16384→8192 ring-cap. CRLF: `-c core.autocrlf=false -c core.eol=lf` pinned on parley's own git invocations (load-bearing layer, follows parley into arbitrary user repos); normalization admissible only for document-semantic comparisons (drift anchor, template block extraction), **forbidden** for evidence-integrity comparisons (snapshot bytes, `TreeDigest`). Census: per New concern 1. Ledger: the per-test failure/exclusion mapping (hosted log ↔ class ↔ stage ↔ row) is an acceptance artifact, refreshed after the Step-1 harness fixes change the denominator. Unfiltered matrix execution preserved throughout.

**7. Acceptance and release (unchanged owner gate).** Unfiltered green windows-latest/macOS/ubuntu on the released commit; per-exclusion reviewed table; suppression grep at close; census + ledger as acceptance artifacts; honest coverage envelope (hosted x64, `core.longpaths true`, no ARM64 execution, no local-native claim). Label removal iff the Windows leg is green on the released commit; winget held until then; both prerequisite handoffs (1.49.1 done; designated-implementer pending); version selected after both, above the then-released version.

**What I accept unconditionally:** items 1, 2, 4, 5, 6 as written; the zcode-1 drafter/implementer selection (conditional only on its `Drafter: yes`); the staging order both peers converged on, with Step 1 (Mkfifo tag + comma-ok assertions) first.

**Remaining blocking counterproposal:** **none.** All six brief items are settled above; I hold no ❌-class position entering consensus.

**Exact conditional acceptance for unresolved hosted facts:**

- **H1 — directory flush:** if the spike succeeds *and* authoritative documentation scoped to directory-handle flush exists, implement it for residual sites; if either fails, the default is the reviewed per-site refusal; a weakened guarantee only via owner question with the hosted evidence attached.
- **H2 — `ReadDir` on a regular file:** the fix ships under every probe outcome; the probe (empty-nil vs classified error) only records the mechanism in IMPLEMENTATION.md.
- **H3 — AF_UNIX creation on the runner:** if socket creation fails hosted, the individually-reviewed exclusion route applies with the reason recorded; otherwise the test runs and the skip sites are converted to hard failures as in item 6.
- **H4 — `wait`/`usage` first Windows execution after the comma-ok fix:** one diagnostic cycle; green is not assumed, and the ledger is refreshed from that run.
- **H5 — CRLF hosted mechanism:** probe (runner `git config` dump + hexdump of one mismatch file) lands before any parser-side normalization is finalized; the normalize-vs-byte-exact rule in item 6 is not negotiable either way.
- **H6 — hosted `%TEMP%` inherited ACL:** probe dumps it; outcome changes fixture placement only, never the owner-only policy.
- **H7 — open-site sharing violations (`ledger_test.go:57`, `review_test.go:438`):** undiagnosed; structural handle discipline and `os.Root` routing first; a bounded retry ships only after self-vs-foreign classification, loud, with full verification re-run after any successful retry.

**Scope compliance.** Verification-only reads at `2b1fc8c` (repo, GOROOT go1.27.1); wrote exactly this file. No code edits, no commits, no phase transitions, no release actions, no peer artifacts touched, no second driver/organizer launched. No secrets. No Chrome.
