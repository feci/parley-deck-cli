---
agent: kimi-1
idea: windows-portability
round: 4
date: 2026-09-25
responding-to: [claude-1/round-03, zcode-1/round-03, round-04-brief]
---

# Round 4 — kimi-1: verdicts on N1–N4, the exact conditional durability contract, closure

## Protocol attestation

```json
{"context_mode": "full", "source_sha256": "8ce83cde0b4f1c3019229869433df0c32451fdbaa13a186952fdf9805c3a9db7", "packet_sha256": "8ce83cde0b4f1c3019229869433df0c32451fdbaa13a186952fdf9805c3a9db7"}
```

Packet sha256 re-verified locally with `shasum -a 256` this session: matches. Read this round: all three round-03 artifacts, `round-04-brief.md`, `00-prompt.md` including the 2026-09-25 owner-default update. Verification executed at HEAD `ac1f0f7`; HEAD has since moved to `2659520b` (organizer metadata only: 00-prompt status line, organizer notes/usage); `git diff --name-only 2b1fc8c..2659520b -- '*.go'` = 0 files (`PRIMARY`), so every round-03 locator transfers. All checks below executed by me this round: repo source, GOROOT `go1.27.1` (darwin/arm64), **plus go1.26.8 sources fetched by me from go.googlesource.com** (the CI-pinned toolchain family: `go.mod:3` `go 1.26`, `tests.yml:42` `go-version-file: go.mod`, verified `PRIMARY`), plus three Microsoft Learn pages fetched directly (CreateFileW, FlushFileBuffers, MoveFileEx). Executed controls: the full traversal/IsLocal table, the darwin `ReadDir`-on-regular-file control, a `GOOS=windows` compile check. No code, workflow, peer artifact, commit, phase transition, publication, hosted run or driver touched. Wrote exactly this file.

**Concurrent peer round-04 files, disclosed rather than left to inference.** `round-04/zcode-1.md` (00:20 local) and `round-04/claude-1.md` (00:25 local) existed on disk when I saved at 00:35; **I did not open either**. Nothing here responds to them; my cross-review targets are the final round-03 set and the round-04 brief, as the brief instructs. Any position of theirs may already have moved in their own round-04 files.

**Owner-default update acknowledged.** The 2026-09-25 update changes new-run defaults only; this in-flight idea retains facilitator codex-1, quorum claude-1/kimi-1/zcode-1, and the zcode-1 drafter/implementer selection. Nothing in my positions changes.

## Position changes since round 3 (self-corrections first)

1. **`SELF-CORRECTION` — my round-03 V1 "sharpening" is REFUTED.** I claimed the hosted `strict_gate_test.go:179` failure was narrowed to exactly one reachable path: the `ERROR_FILE_NOT_FOUND`-with-RestartInfo-class `break` in `dir_windows.go` (empty, nil). That branch requires the *open* to succeed on a regular file, and it cannot: `syscall.Open`'s post-open `o_DIRECTORY` guard (`syscall_windows.go:455-466`, verified by me at go1.27.1 **and** go1.26.8) returns `ENOTDIR` before any readdir runs. I examined the CreateFile masking and the readdir error mapping but missed the open-time guard. What survives of V1: the product shape (`reviewRoundHasFindings` vetoes on any non-`ErrNotExist` error, `internal/driver/impl.go:448-472`) and the classification set (`ErrNotExist` = `ERROR_FILE_NOT_FOUND`, `_ERROR_BAD_NETPATH`, `ERROR_PATH_NOT_FOUND`, `ENOENT` only; `ENOTDIR` absent — `syscall_windows.go:202-205`, both toolchains). My narrowing is superseded by claude-1's N3; the hosted cause is undiagnosed, full stop.
2. **`SELF-CORRECTION` — my round-03 V2 ("four directory-handle sync sites: CONFIRMED") is superseded.** claude-1's N2 six-site census is correct (verdict below); I confirmed claude-1's round-02 undercount instead of re-running the exhaustive grep myself. zcode-1's round-03 independently caught the same undercount. Lesson recorded: a census verdict requires re-executing the census, not re-checking the cited rows.
3. **`SELF-CORRECTION` — my round-03 V4 skip count (107) was the unescaped-regex artifact.** Re-executed (`PRIMARY`): escaped `t\.Skip` in `*_test.go` under `internal` = **104 lines**, of which exactly one is a non-call (`internal/runner/launch_precheck_skip_test.go:58`, `result.Skipped`) → **103 skip call sites** = 68 `t.Skip(` + 35 `t.Skipf(`, across **55 files** with call sites (56 files match the looser pattern). The unescaped `t.Skip` count is 107. claude-1's correction is exactly right; the argument is unaffected.
4. **My round-03 H1 is narrowed: the `FlushFileBuffers`-on-directory spike can never meet the documentation bar.** I fetched the FlushFileBuffers page myself (`PRIMARY`): the subject is "a specified **file**", the handle "must have the **GENERIC_WRITE** access right", and the only other documented subjects are a communications device (transmit buffer), the server end of a named pipe, and a **volume** handle ("The caller must have administrative privileges"). Directory handles are not documented at all. The spike survives as a *diagnostic* only — never acceptance evidence — exactly as claude-1's position change 2 concludes.
5. **Existing-store recovery: I narrow my round-03 phrasing to zcode-1's shape.** My "re-apply the protected owner-only DACL" read as a product-side in-place repair. Settled: the product **refuses and instructs** (the refusal names the offending trustee and the documented user-side recovery — move aside / re-create under a protected DACL); the product never silently modifies a pre-existing store's DACL, and there is no in-flight migration code in this idea. One FINAL sentence, per zcode-1's concern 4 and claude-1's concern 3 (C3).

## Non-owner verdicts on claude-1's N1–N4

**N1 (O_SYNC → Windows write-through stdlib mapping) — CONFIRMED (`PRIMARY`, both toolchains).** Verified line-by-line at go1.27.1 and at my own go1.26.8 fetch; the chains are identical at every load-bearing line, including line numbers: plain path `syscall/syscall_windows.go:426-427` (`if flag&O_SYNC != 0 { attrs |= _FILE_FLAG_WRITE_THROUGH }`), `O_SYNC = 0x01000` (`types_windows.go:52`); `os.Root` path `internal/syscall/windows/at_windows.go:99-100` (`options |= FILE_WRITE_THROUGH`, mapping registered at `:56`); `os.Root.OpenFile` validates only `perm` and passes `flag` through `rootOpenFileNolog` → `openat(parent, name, uint64(flag), perm)` (`root.go:117-127`, `root_windows.go:132-137`). Per-GOOS constant caveat confirmed: `syscall.O_SYNC` is `0x01000` only on windows (darwin `0x00080`) — a Windows-only build-tagged constant is mandatory; my `GOOS=windows go build` of a trivial `syscall.O_SYNC` reference compiles (executed). **Claude's C4 is discharged for N1**: the mapping holds at the CI-pinned toolchain family. The darwin open-execution claim is not load-bearing for the Windows mapping and I did not re-execute it (`SECONDARY`, unnecessary).
Guarantee side, engaging the actual Microsoft text I fetched: CreateFileW documents `FILE_FLAG_WRITE_THROUGH` as "Write operations will not go through any intermediate cache, they will go directly to disk" and "A write-through request via FILE_FLAG_WRITE_THROUGH also causes **NTFS** to flush any metadata changes, such as a time stamp update or **a rename operation**, that result from processing the request." Two honest scopings: (i) the metadata sentence is **NTFS-scoped** — ReFS is in the same page's supported-technology table and is not covered by that sentence; (ii) the examples name ancillary metadata, not directory-entry creation. I concur with claude-1's tiering: rename coverage is **documented**, create-entry coverage is a **documented-mechanism inference**. Strengthening the inference's best reading (and keeping the tier): on the create sites the write happens on the *creating* handle within the same open, so the entry creation is a metadata change "resulting from processing the request" of that very handle — but since the examples stop short of naming entry creation, FINAL must disclose the inference, and the hosted probe can corroborate only the mechanics (flag accepted, write/sync succeed, NTFS asserted), never durability itself. A green open is not a guarantee; the guarantee is the quoted documentation.

**N2 (six-site durability census) — CONFIRMED with two refinements (`PRIMARY`; I read all six sites at `ac1f0f7`).** All six exist with the claimed shapes: `internal/app/trajectory_verify.go:138` (dir sync) / `:146` (O_EXCL create); `internal/trajectory/verification.go:189` (`syncVerificationDirectory`, dir `Open(".")`) / `:202` (O_EXCL create); `internal/trajectory/unchanged.go:279` (dir sync; persist is `writeState` → `fsutil.ReplaceSyncedFile`, `state.go:214-221` — already `MOVEFILE_WRITE_THROUGH`-backed, sibling-constrained at `replace_windows.go:16-18` so copy-and-delete is impossible); `internal/trajectory/parent_recovery.go:308` / `:545` (dir syncs after `dir.Rename` at `:333` / `:570`); `internal/trajectory/reservation_recovery.go` — **locator nit: the directory-handle sync is at `:67`** (dir opened at `:63`), not `:64`; the `reservation-intents` Mkdir is at `:36` as claimed. Seventh site `internal/budget/lock_unix.go:31` confirmed `//go:build !windows`. Refinement 1 — **rename sites are not currently write-through**: `os.Root.Rename` → `renameat` → `windows.Renameat` → `NtOpenFile` + `FILE_RENAME_INFORMATION_EX` (`root_windows.go:386-388`, `at_windows.go:363+`, go1.27.1; identical at go1.26.8) — no write-through anywhere, so the two parent_recovery sites need conversion, and only 1 of 3 rename sites is already covered. Refinement 2 — **Windows reachability is per-site, not uniform**: `trajectory_verify.go:138/:146` sit behind `trajectoryRuntime`'s POSIX refusal (`:106-108`); `verification.go:189/:202` sit behind `openVerificationDirectory`'s POSIX refusal (`:251-253`) for the captured-verification path, but the same helpers are reached from the reservation-intent path (`reservation_recovery.go:47/:67/:431`), and `internal/budget` carries no Windows gates (grep, `PRIMARY`; `PrepareCycleReservation` fires from `cycle_binding.go:269` whenever the observer implements it), while the four recovery CLI commands carry no command-layer GOOS gate. Net: the census is right, zcode-1's mechanical-audit amendment (grep + compiler, no hand-lists) is required, and FINAL's per-site table must record each site's reachability disposition — sites behind an existing POSIX refusal inherit this contract when (if) that refusal is ever lifted.

**N3 (`o_DIRECTORY` refutation of claude-1's round-2 strict_gate chain) — CONFIRMED (`PRIMARY`, both toolchains + executed control).** `o_DIRECTORY = 0x04000` (`types_windows.go:54`) is masked out of CreateFile attrs (`fileFlagsMask = 0xFFF00000`, `:95`) **but** enforced post-open at `syscall_windows.go:455-466` (`GetFileInformationByHandle` → not-a-directory → `CloseHandle` + `ENOTDIR`), with the second guard at `os/dir_windows.go:147-148` (`file.Stat()` → `!IsDir()` → `ENOTDIR`); `openDirNolog` passes `O_RDONLY|O_DIRECTORY` (`os/file_windows.go:166-167`); `ENOTDIR` ∉ `ErrNotExist` (`syscall_windows.go:202-205`). Identical at go1.26.8 (my fetch) — zcode-1's round-03 1.26.8 claim independently confirmed. My executed darwin control matches claude-1's: `os.ReadDir(<regular file>)` → `entries=0`, `err="not a directory"`, `errors.Is(err, fs.ErrNotExist)=false`. **C4 is discharged for N3.** Consequence: on both toolchains the source predicts veto → pass; the hosted failure contradicts the source; the cause is undiagnosed and the hosted probe is load-bearing for the *sweep*, not for the fix.

**N4 (traversal/IsLocal table + the negative result) — CONFIRMED (`PRIMARY`, re-executed in full on darwin at go1.27.1).** My re-execution of the 26-ID table reproduces claude-1's rows exactly, including the load-bearing asymmetry: `filepath.IsLocal(slug+"__"+id)` returns **true** for `..`, `../x`, `../../x`, `../../other-idea` while the raw IDs return false — validation on the concatenated element is unsound; validation must run on the **raw ID**, with an explicit charset allowlist as the primary defence. `filepathlite`'s Windows `isLocal` (`path_windows.go:23-53`, read at 1.27.1) rejects a leading separator, **any** `:`, per-element reserved names, and post-`Clean` `..` — confirmed as the allowlist precedent, cited not reused. The `\`-is-inert-on-darwin caveat stands as disclosed (Windows column needs the hosted/GOOS check); the reject-on-all-OS design conclusion is unaffected because the raw-ID allowlist is platform-independent.

## Responses to others

### @claude-1 — round-03

Verdicts N1–N4 above. **Your blocking counterproposal is met as stated** — the contract below contains no documented-no-op default and no pre-approved weakening; deviation requires concrete hosted evidence plus a reviewable proposal before any owner question. Your C1–C3, C5 stand as the conditional-acceptance set; **C4 is closed** by my go1.26.8 fetches above (N1 and N3 both hold at the CI toolchain family; zcode-1 should cite the 1.26.8 locators in FINAL). Accepted: the FlushFileBuffers demotion (my position change 4), the refusal scoping clarification (per-publication, not whole-feature), the probe-bundle additions (O_SYNC publication probe; toolchain-pinned ReadDir probe; `git config --show-origin` dump), and your honest residual on the create-entry inference — engaged and confirmed at the same tier, with the NTFS scoping added. One correction to your N2 arithmetic phrasing: "(b) write-through publication — narrowed by me to 2 of 6" reads ambiguously against your own 3-rename/3-create split; the precise statement is 3 rename sites of which 1 (unchanged) is already write-through and 2 (parent_recovery pair) need conversion, plus 3 create sites. No substantive disagreement.

### @zcode-1 — round-03

Your round-03 condition on me was satisfied in my round-03 (documented-nil floor withdrawn); recorded as settled, not majority-resolved. Your go1.26.8 fetch discharged N3's toolchain half and I have now extended it to N1 — your conclusion holds. Your amendment 2 (write-through meets the same documentation standard) is satisfied by the MoveFileEx analysis below and adopted into the contract. Your concern 1 (a nil+empty probe result implicates *every* ReadDir-based decision on Windows) is accepted and folds into the sweep's trigger condition below. Your concern 4/5 (existing-store refuse-and-instruct; CI unaffected) — adopted, my position change 5. Your `Drafter: yes` and single-implementation claim are on record; claude-1 and I are the unencumbered reviewers, model diversity intact.

## The settled conditional durability contract (complete site inventory)

One contract, six sites, per-site disposition table in FINAL (with the reachability column from N2 refinement 2). `fsutil.SyncDir` becomes the named per-OS contract; conversion is mechanical (grep + compiler proves no directory handle reaches `SyncFile`); file-handle fsync stays mandatory everywhere; Unix behavior byte-identical.

Per site, in order, with the outcome implementation must choose in each case:

- **(i) Rename-publication sites** (`parent_recovery.go:333`, `:570`; `unchanged.go:279` already satisfied): publish via `MoveFileEx(MOVEFILE_REPLACE_EXISTING|MOVEFILE_WRITE_THROUGH)` on same-volume siblings. Applicable documented guarantee (my fetch): "The function does not return until the file is actually moved on the disk" — unqualified and operation-scoped; the stronger "guarantees… flushed to disk" sentence is scoped to copy-and-delete, which the sibling constraint makes impossible here, and FINAL must quote sentence one as the applicable text. The CreateFileW sentence naming rename as flushed metadata corroborates. **Status: documented mechanism. Implement it.** Mechanism choice at the two os.Root sites (absolute-path `fsutil.ReplaceSyncedFile`-style call vs a handle-based rename on a write-through handle) must record the os.Root-containment trade-off; if neither can be expressed without breaking the containment the design requires, that site falls to (iii).
- **(ii) Create-in-place sites** (`trajectory_verify.go:146`, `verification.go:202`, `reservation_recovery.go:75`): add Windows-only `O_SYNC` (FILE_FLAG_WRITE_THROUGH) to the O_EXCL publication open, preserving the load-bearing "a partial publication stays visible and cannot be overwritten by retry" semantics — which is why rename-reshaping these sites is **rejected as the default** (it changes replay-prevention semantics, not just plumbing) and may only be revisited if review rejects the inference. **Status of the O_SYNC proposal: accepted as the design mechanism, evidence-pending on two disclosed items** — the create-entry inference (FINAL quotes the CreateFileW sentence and labels the entry-coverage an inference) and the hosted mechanical probe (flag accepted on NTFS, write+sync succeed, volume asserted NTFS; on a non-NTFS writable volume the NTFS-scoped sentence does not apply and the site falls to (iii)). If the probe shows the flag rejected or erroring at a site, that site falls to (iii).
- **(iii) Any site where (i)/(ii) cannot be expressed or proved**: `SyncDir` returns a **named, user-visible, blocking refusal scoped to that publication operation**; the operation fails loudly and nothing else is weakened. There is no fourth branch: no implicit no-op, no silent weakening, no green-return-as-guarantee; a weaker guarantee enters only as concrete hosted evidence plus a reviewable proposed deviation before any owner question. The `FlushFileBuffers`-on-directory probe remains as a diagnostic, explicitly never acceptance evidence.

## strict_gate reconciliation

Settled shape: the source-level correction (N3) is confirmed at **both** toolchains, so both known mechanisms are excluded and the hosted phenomenon contradicts the source prediction — cause **undiagnosed**, not fixture-only, and no unexecuted branch is called confirmed. The branch-independent invariant fix lands regardless (stat the round path; veto when it exists and is not a directory; cross-platform pinning test). The hosted probe prints `runtime.Version()`, the raw `os.ReadDir(<regular file>)` error, and `errors.Is(err, fs.ErrNotExist)`; a result diverging from the 1.26.8 source prediction triggers zcode-1's sharpened consequence — every `ErrNotExist`-classification-after-directory-read decision on Windows is suspect — which is exactly my round-03 concern-2 sweep, keyed on the pattern. Probe outcome→behavior mapping: fix ships under every outcome; the probe selects the recorded mechanism and the sweep's scope, never the policy.

## Census / inapplicability policy — settled exact wording

Reconciling my round-03 Windows-only emission with zcode-1's all-legs variant: emit `-json` on **all three legs** (uniform observability; the workflow change is its own reviewed change; the unfiltered matrix is untouched — `-json` selects nothing). **Windows leg:** every firing SKIP event must match an individually reviewed exclusion-table row (reason recorded, per the owner's "truly inapplicable and reviewed") or the leg fails; the AF_UNIX fallback skips convert to hard failures rather than acquiring rows. **macOS/Ubuntu legs:** the currently-firing skips are enumerated as the reviewed baseline; any *newly* firing skip beyond baseline fails the leg — this enforces the owner's "no new suppression" on the still-green legs without re-litigating 103 pre-existing sites' justifications. Baseline numbers pinned by this round's recount: 103 call sites (68 `t.Skip(` + 35 `t.Skipf(`), 55 files.

## New concerns / questions

1. **Rename-site conversion mechanism is an open implementation choice with a containment trade-off** (absolute-path MoveFileEx pierces os.Root containment for that operation; a handle-based write-through rename preserves it but is new machinery). The drafter must pick one and record the trade-off in FINAL; neither is allowed to silently drop the write-through request.
2. **NTFS-scoping of the metadata-flush sentence** means the ACL probe and the durability probe should assert the runner volume's filesystem (one `GetVolumeInformation`/PowerShell line), so hosted evidence matches the documented scope. Fixture logistics, not policy.

## Current proposal

Unchanged from round-03 items 1, 2, 4, 5, 6, 7 (privacy owner-only protected DACL with refuse-and-instruct recovery; P-A + blocking shell refusal with zcode-1's wording template; universal gate-name encoding + raw-ID allowlist validation + cosmetic-only grandfathering; strict_gate invariant fix + probe + sweep; census as settled above; acceptance/release gates unchanged), with item 3 replaced by the conditional durability contract above.

## Consensus readiness and remaining conditions

**Consensus-ready. No blocking counterproposal from me; no ❌-class position.** Every material claim I depend on is now `PRIMARY` at the CI toolchain family or explicitly hosted-pending with a pre-agreed outcome→behavior mapping (C1–C3, C5; H2–H7 as amended by this round's contract). No unresolved verdict conflicts exist: the N1–N4 verdicts are confirming, my three self-corrections replace my stale claims, and no `DISPUTED` claim survives into drafting. What FINAL must carry, as the exact conditional-acceptance residue: the quoted doc sentences (MoveFileEx sentence one; CreateFileW NTFS metadata sentence; the FlushFileBuffers subject list as the diagnostic-only justification), the create-entry inference disclosed as inference, the NTFS scoping, the per-site durability table with reachability dispositions, the strict_gate cause recorded as undiagnosed pending the probe, and honest coverage language (hosted x64 only, ARM64 build-only, no local-native Windows claim). Release-gate status unchanged: handoff 1 present (1.49.1 at `54e0798`), designated-implementer handoff absent, gate closed.

## Material findings reported this round

1. N1 confirmed at go1.26.8 (discharging C4 for N1) — with the added NTFS scoping of the metadata-flush sentence.
2. N2 confirmed with a locator nit (reservation dir sync at `:67`, not `:64`), the os.Root.Rename no-write-through finding (`NtOpenFile`/`FILE_RENAME_INFORMATION_EX`), and the per-site Windows-reachability refinement (two sites behind existing POSIX refusals; reservation path ungated).
3. N3 confirmed at both toolchains; my V1 narrowing self-corrected as refuted; hosted cause undiagnosed.
4. N4 confirmed by full re-execution; IsLocal-on-concatenated unsoundness reproduced.
5. Skip census pinned at 103 call sites / 55 files; my V4's 107 self-corrected.
6. FlushFileBuffers-on-directory foreclosed as acceptance evidence by its own documentation (my fetch); spike demoted to diagnostic — my H1 narrowed accordingly.
7. Existing-store recovery aligned to refuse-and-instruct; my in-place phrasing narrowed.

## Scope compliance

Read-only verification (repo at `ac1f0f7`, GOROOT go1.27.1, my own go1.26.8 source fetches, three Microsoft Learn pages via direct fetch, executed throwaway programs under /tmp only, git metadata). Wrote exactly this file. No product code, workflow, peer artifact, phase transition, commit, tag, publication, hosted run or driver. No secrets. No Chrome.
