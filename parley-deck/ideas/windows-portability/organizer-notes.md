# Organizer notes

- Driver gap G1: preflight exits 3 on completed historical meta-protocol-change-devx-speed whose facilitator also appears in participants. All requested participant liveness checks passed. No historical artifact edited; scoped launch fallback is permitted by owner driver-gap instruction.
- Tool observations: agents list does not accept --json; initial invalid protocol track implementation was corrected to deliberation before any participant launch.
- Transport: owner authorizes direct linear integration and no development PRs; project header remains github-pr. No protocol rule change inferred.
- Worktree skill: existing windows-portability worktree adopted. Design participants own disjoint artifact files; implementation edits serialized to one participant. Other ideas remain isolated.

- Driver gap G2: continue refuses the prepared idea with no runs; run windows-portability instead creates timestamped 2026-09-24T23-08-45-windows-portabil with only a one-line prompt and no facilitator. Bootstrap terminated, accidental idea abandoned; its artifacts cannot satisfy quorum. Manual participant launch is the recorded fallback for initial round.

- All three real CLIs launched manually at phase 1 with the full renderer context path plus exact attestation and canonical kickoff. Claude model claude-opus-5[1m]/max; Kimi kimi-code/k3 and configured effort; Zcode zai/glm-5.3 and configured effort. Only own round artifact writable by instruction; no commits or code changes. `continue --auto windows-portability` also refuses without an associated run. Cancelled bootstrap runtime marked cancelled after termination; no run-state success fabricated.

- Round 1: all three real CLI processes exited 0 and own canonical files passed parley wait validation. Cross-review manually dispatched under the same G2 fallback; all raw disagreement remains for peer adjudication. The mechanical wait digest suggested consensus after round 1; organizer follows mandatory deliberation cross-review instead. Phase-2 facilitator packet 258e2227fcbd02ef3996b40dd09d0ab539d749befdd4be2075862ad645e247d3, unchanged source hash.

- First predecessor handoff arrived: CLI 1.49.1 release complete at 54e07981740d495f08cd90842168d9228c58c90f, independently audited by Kimi in the predecessor track; copied verbatim to source-context/release-1.49.1-done.md. Windows assets remain experimental and CLI winget held. Designated-implementer handoff still absent. This is read as predecessor testimony, not organizer code/channel verification. Its Homebrew upgrade changed the globally resolved CLI during our run; exact tooling version must be recorded/pinned.

- Tooling isolation: Homebrew removed its old 1.49.0 keg while the predecessor release installed 1.49.1. Observed global `parley --version` = 1.49.1 during round 2; phase-2 commands launched around that external upgrade cannot be represented as pinned 1.49.0. Downloaded published v1.49.0 darwin-arm64 asset from feci/parley-deck-cli to /tmp/windows-portability-organizer/tooling/parley (version output parley 1.49.0); future organizer commands and packet dispatch use this private path. No global CLI downgrade.

- 2026-09-25 owner defaults replaced the new-run roster/organizer but explicitly exempt this in-flight run. Machine now reports Claude inactive / Opus 5.5 and Codex active. This idea retains its original quorum and fixed participant launch selectors; the global roster is not changed. Zcode accepted Drafter: yes and implementation, with both peers ratifying in round 3. Round 4 addresses new N1-N4 evidentiary deltas and self-corrections before drafting, not an unresolved owner policy choice.

- Memory availability correction: MCPAnywhere/OpenViking became callable during round 4. Scoped find under viking://resources/projects/parley-deck/ returned only release-status-20260923.md (historical 1.48.0/skill 2.12.1), not current handoff evidence. No memory write claimed.

- Round 4 complete: all real CLI processes exited 0; final structural validation 3/3. Raw artifacts expose a new material durability conflict despite the mechanical digest suggesting consensus. Claude withdraws O_SYNC create-entry inference and requests a thirteen-operation/four-class disposition; Kimi/Zcode's round-4 positions retain the earlier inference and six-site contract, and do not respond to Claude's same-round correction. Organizer makes no technical verdict. Section 4.0 cross-review cap reached; blocking owner escalation filed as inbox/codex-1-to-user_windows-portability_round-cap.md, with a concrete proposed durability-followup-brief.md. No consensus, FINAL, implementation or release advanced.

- Shared-memory boundary note: write to viking://resources/projects/parley-deck/windows-portability-round4-20260925.md timed out waiting for queue processing after 60 seconds. Direct readback returned the exact saved content, confirming file persistence; scoped find did not yet return it, so search indexing remains unverified. Local canonical escalation remains authoritative.

- Resume 2026-09-25: owner round-5 authorization read verbatim. `parley continue --json windows-portability` again refuses: idea has no runs yet (G2 persists); reuse existing fixed-selector CLI dispatch. Current machine roster now shows Claude active/Opus 5.5, but no in-flight membership/model migration is inferred. Exactly one round authorized. Initial `organizer brief` required --idea; corrected. No product checks run by organizer.

- Round 5 complete: Zcode exited 0 at 06:44:15 UTC, Claude at 06:49:25 UTC, Kimi at 06:53:06 UTC (2026-09-25). First `parley wait --timeout 20m` returned 3 with Kimi outstanding; a second bounded wait returned 0 and 3/3 valid artifacts. The mechanical next-action digest again says consensus; raw artifacts override it. Claude now withdraws N5 and selects O_SYNC plus refusals that preserve rooted containment; Kimi/Zcode endorse the round-4 N5 correction and select restructuring. Kimi explicitly accepts weaker traversal resistance; Claude rejects that trade. No organizer technical verdict or code verification is supplied. Per owner instruction, renewed blocking escalation filed, no further round or phase launched.
- Answered round-cap escalation archived only after all three canonical round-05 files quoted owner direction. Release predecessor 1 remains present, predecessor 2 absent at this boundary; no release, new hosted CI, implementation or channel verification occurred. Completion handoff is an exit handoff with blocked status, not a shipped claim.

- Round 6 resume: owner authorization read in required order; exactly one sequential round opened, order Claude/Kimi/Zcode. G2 reproduced (`continue`: no runs yet); existing fixed-selector dispatch reused with one agent per invocation. Pinned 1.49.0 tooling retained. Machine roster inspected, in-flight membership and launch selectors unchanged. Scoped OpenViking find returned historical round-4 note; current local owner answer controls. No product code inspected or verified by organizer.

- Round-6 release prerequisite update: BOTH handoff files now exist. Designated-implementer handoff explicitly releases this sequencing hold and reports CLI 1.50.0 / skill 2.14.0 agent-controlled delivery, with npm/core/skill-WinGet pending states. Copied verbatim into source-context/designated-implementer-done.md. This is predecessor testimony, not an organizer channel audit; our consensus, implementation, independent review and final hosted matrix remain required. New owner AGENTS instructions confirm Claude active globally while exempting this in-flight roster/organizer.
- Wait limitation: no per-participant selector. A bounded full-round parley wait remains active while sequential process completion is observed; each completed artifact is structurally validated before dispatching the next participant. The wait reports the answered round-5 escalation as pre-existing; owner answer remains authoritative and escalation will be archived after round-6 artifacts record it.

- Round 6 finished in the authorized Claude→Kimi→Zcode order with exit 0 and validation between launches; final parley wait exit 0, 3/3 valid. All raw positions now converge on the current refusal branch; comparison provisional until signoffs. Answered round-5 escalation archived after every round-6 artifact recorded owner direction. Nonblocking explicit containment-deviation request filed per owner instruction; no weakened guarantee selected. Consensus drafting assigned to Zcode, original drafter/implementer.

- Consensus draft completed by Zcode (exit 0 at 17:22:05 UTC), with its own volunteer drafter inbox claim and empty signoffs. Pinned request-signoffs dry-run works and shows the requested original quorum. Claude signoff is manually dispatched solely to preserve the in-flight Opus 5 selector rather than inherited Opus 5.5; Kimi/Zcode will use the driver, whose current model selections match their in-flight selectors. No quorum change.

- Consensus reached: Claude manual fixed-selector signoff exited 0; pinned driver request-signoffs for Kimi/Zcode exited 0 and both signed ACCEPT. parley wait returned 0 (consensus ready), all three original participant signatures present. Claude content corrections 1–3 are accepted by both peers: N7 single round-6 verifier/layered provenance, accurate quotation, and explicit benefit-side statement that the current reachable durability rows refuse or already derive. These must be incorporated in FINAL. Owner deviation remains unapproved, nonblocking; no answer inferred.

- FINAL completed by Zcode (exit 0 at 17:51:37 UTC); participant reports 52 observable acceptance criteria, expanded H1–H7 mappings, all three signoff corrections, immutable signed consensus and no code changes in that phase. `continue` now selects the completed consensus-signoff run (advisory round-01, only its two requested signers), reports STALE/no recoverable action. Canonical idea/quorum and organizer brief remain authoritative; record G3 and use scoped manual implementation dispatch, not a fabricated driver cursor.
- Shared memory round6-consensus note: write processing timed out at 45 seconds; direct readback matched the saved text, confirming persistence. Scoped semantic retrieval still returned only historical notes; search indexing not yet verified.
- Phase 5 git serialization: after the organizer closing commit, Zcode alone may commit with the owner-required [codex-1] windows-portability: prefix and push only windows-portability for hosted CI. Organizer refrains from concurrent git writes while that invocation runs. No main integration or release before independent review closure.
