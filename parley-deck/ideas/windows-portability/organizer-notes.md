# Organizer notes

- Driver gap G1: preflight exits 3 on completed historical meta-protocol-change-devx-speed whose facilitator also appears in participants. All requested participant liveness checks passed. No historical artifact edited; scoped launch fallback is permitted by owner driver-gap instruction.
- Tool observations: agents list does not accept --json; initial invalid protocol track implementation was corrected to deliberation before any participant launch.
- Transport: owner authorizes direct linear integration and no development PRs; project header remains github-pr. No protocol rule change inferred.
- Worktree skill: existing windows-portability worktree adopted. Design participants own disjoint artifact files; implementation edits serialized to one participant. Other ideas remain isolated.

- Driver gap G2: continue refuses the prepared idea with no runs; run windows-portability instead creates timestamped 2026-09-24T23-08-45-windows-portabil with only a one-line prompt and no facilitator. Bootstrap terminated, accidental idea abandoned; its artifacts cannot satisfy quorum. Manual participant launch is the recorded fallback for initial round.

- All three real CLIs launched manually at phase 1 with the full renderer context path plus exact attestation and canonical kickoff. Claude model claude-opus-5[1m]/max; Kimi kimi-code/k3 and configured effort; Zcode zai/glm-5.3 and configured effort. Only own round artifact writable by instruction; no commits or code changes. `continue --auto windows-portability` also refuses without an associated run. Cancelled bootstrap runtime marked cancelled after termination; no run-state success fabricated.

- Round 1: all three real CLI processes exited 0 and own canonical files passed parley wait validation. Cross-review manually dispatched under the same G2 fallback; all raw disagreement remains for peer adjudication. The mechanical wait digest suggested consensus after round 1; organizer follows mandatory deliberation cross-review instead. Phase-2 facilitator packet 258e2227fcbd02ef3996b40dd09d0ab539d749befdd4be2075862ad645e247d3, unchanged source hash.

- First predecessor handoff arrived: CLI 1.49.1 release complete at 54e07981740d495f08cd90842168d9228c58c90f, independently audited by Kimi in the predecessor track; copied verbatim to source-context/release-1.49.1-done.md. Windows assets remain experimental and CLI winget held. Designated-implementer handoff still absent. This is read as predecessor testimony, not organizer code/channel verification. Its Homebrew upgrade changed the globally resolved CLI during our run; exact tooling version must be recorded/pinned.

- Tooling isolation: Homebrew removed its old 1.49.0 keg while the predecessor release installed 1.49.1. Observed global `parley --version` = 1.49.1 during round 2; phase-2 commands launched around that external upgrade cannot be represented as pinned 1.49.0. Downloaded published v1.49.0 darwin-arm64 asset from feci/parley-deck-cli to /tmp/windows-portability-organizer/tooling/parley (version output parley 1.49.0); future organizer commands and packet dispatch use this private path. No global CLI downgrade.
