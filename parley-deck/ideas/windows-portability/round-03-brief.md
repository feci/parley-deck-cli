# Round 3 — settle the implementation contract

Read all final round-02 artifacts. Several disagreements cited there refer to peers' round-01 positions that their round-02 files already withdrew. Reconcile the latest positions explicitly; no majority-based resolution.

Please settle a concise contract on:
1. Owner-only protected-DACL creation and effective verification, including existing-store refusal/recovery and adversarial tests. Kimi's willingness not to block owner-only and Zcode's withdrawal of Administrators must be reflected accurately.
2. P-A truthful liveness plus explicit durable-kill refusal; full attribution/Job Objects deferred. Kimi has now conceded this scope. Shell prerequisite plus a loud blocking missing-sh refusal is endorsed by all three latest files; settle its observable behavior without re-asking a human policy question already covered by the owner's allowed-refusal constraint.
3. Directory durability: engage Claude's per-site SyncDir/write-through-publication counterproposal against the spike/no-op alternatives. Each claimed guarantee needs authoritative documentation scoped to the exact operation; an API name or successful return does not prove semantics. The owner authorized a real Windows implementation OR an explicit reviewed user-visible refusal. A documented nil/no-op or weakened guarantee is NOT an already authorized third option. A conditional design may select a real mechanism if proved and default to reviewed refusal otherwise; if peers want a different policy, produce concrete hosted evidence and a reviewable proposed deviation before any owner question. No implementation may silently choose that deviation.
4. Universal gate encoding and compatibility, grandfathering only safe cosmetic legacy IDs, rejection of unsafe traversal/drive/ADS/device-name cases, and treatment of legacy shadow files. Settle acceptance examples.
5. Claude's newly raised strict_gate_test.go product fail-open hypothesis, its required non-owner verdict, and the empirical checks needed. Do not reclassify it as fixture-only without evidence.
6. The Windows skip census and per-test failure/exclusion mapping; a currently existing skip that newly fires is not evidence that the behavior was exercised. Preserve unfiltered matrix execution and review each true inapplicability exception. Settle AF_UNIX and ACP drain/ring test mappings, and CRLF document semantics versus byte-exact evidence integrity.

Claude declines the drafter/implementer nomination in favor of Zcode, while accepting organizer selection either way. Organizer procedural selection: zcode-1, subject to its explicit acceptance and quorum ratification; Claude and Kimi will independently review. Zcode: state Drafter: yes and acceptance of the single implementation claim if willing. No participant commits, publishes, advances phases or writes code in this round.

The complete acceptance contract must be settled before Phase 5. Do not start code under an unresolved design decision on the premise it can be decided before its stage ends. Diagnostic-first conditional branches are acceptable when the allowed outcomes and refusal behavior are explicit and ratified.

Read the copied predecessor handoff source-context/release-1.49.1-done.md. One release prerequisite is satisfied; designated-implementer is still pending. Version is selected after both predecessors, above the then-released version. This is a product idea; no protocol change has been proposed. The owner-requested Windows assets remain in scope, with only hosted x64 execution evidence claimed.
