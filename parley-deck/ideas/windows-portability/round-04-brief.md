# Round 4 — close the new evidentiary deltas

All three round-03 files pass structural validation. The policy disputes have converged: owner-only ACL, P-A with full attribution deferred, blocking missing-shell refusal, universal gate names, reviewed Windows skip census, and Zcode as drafter/implementer (Drafter: yes). Kimi explicitly withdrew the documented-no-op floor, satisfying both peers' condition. No owner question is needed for these settled policies.

Claude's round-03 introduces N1–N4 and materially corrects earlier durability and strict_gate claims after both peers wrote their round-03 responses. This last scheduled cross-review round addresses those deltas before drafting; it does not rerun the full discovery inventory.

- Kimi and Zcode: provide non-owner verdicts on N1–N4 in Claude's round-03, including the CI-pinned Go 1.26.x source requirement. Engage the actual Microsoft guarantees for the actual create/rename operation and the explicitly identified create-entry inference. Correct/supersede your earlier verdicts that depended on four sync sites or the older O_DIRECTORY chain. Keep unresolved hosted facts distinct from demonstrated source facts.
- All: settle one exact conditional durability contract covering the complete site inventory: proved scoped mechanism, else named blocking refusal for the affected operation; no implicit no-op or pre-approved weaker guarantee. State whether the O_SYNC proposal is accepted, rejected or evidence-pending, and what outcome implementation must choose in each case. Do not let a green API call stand in for a guarantee.
- All: reconcile strict_gate's source-level correction with the future hosted probe and the branch-independent invariant test; do not call an unexecuted branch confirmed. Resolve traversal/IsLocal examples, the exact inapplicability/census policy, and any remaining material conflicts revealed by the latest round.
- Zcode: ensure the contract can be drafted self-contained with existing-store recovery and partial publication/refusal recovery, without a design decision being silently delegated to implementation. You have accepted the drafter and single implementation claim; Claude and Kimi will be the independent reviewers.

The owner's 2026-09-25 global default update explicitly preserves this in-flight idea: codex-1 organizer, claude-1/kimi-1/zcode-1 quorum; no model/session/role reassignment. New-run defaults are not applied here.

This is round 4 (the third cross-review round after round 1). Report every material issue, without suppressing findings to meet a cap. Explicitly state consensus-ready or the exact remaining blocking counterproposal. No product edits, hosted mutations, commits, releases or phase transitions in this invocation.
