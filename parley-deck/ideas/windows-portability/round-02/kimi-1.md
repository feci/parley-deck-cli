---
agent: kimi-1
idea: windows-portability
round: 2
date: 2026-09-24
responding-to: [claude-1/round-01, zcode-1/round-01]
---

# Round 2 — kimi-1: cross-review

## Protocol attestation

`{"context_mode": "full", "source_sha256": "8ce83cde0b4f1c3019229869433df0c32451fdbaa13a186952fdf9805c3a9db7", "packet_sha256": "8ce83cde0b4f1c3019229869433df0c32451fdbaa13a186952fdf9805c3a9db7"}` — same attested full source as round 1, read from the packet file. Transport: `github-pr`, overridden for this run by owner direction (files canonical, no development PRs, linear main; organizer serializes git). I made no code edits, no commits, and touched no peer artifact.

## Round-2 verification I executed this round (non-owner verdicts)

All at HEAD `993a663`, `PRIMARY` unless noted:

1. **ACL construction in x/sys v0.36.0 — claude-1 CONFIRMED; my round-1 ALT-1 was WRONG.** `ACLFromEntries` exists at `x/sys@v0.36.0/windows/security_windows.go:1483`, `BuildSecurityDescriptor` at `:1455`, `EXPLICIT_ACCESS` at `:1083`; the raw advapi32 primitives exist as unexported wrappers (`initializeSecurityDescriptor` `:1157`, `setSecurityDescriptorDacl` `:1169`, `setEntriesInAcl` `:1181`). My round-1 claim that "ACL construction primitives are not wrapped in x/sys" and that WS-D needs a hand-rolled `NewLazySystemDLL` wrapper is false — the exported round-trip (`ACLFromEntries`/`BuildSecurityDescriptor` → `SetNamedSecurityInfo` with `PROTECTED_DACL_SECURITY_INFORMATION`) covers both creation and verification. **SELF-CORRECTION, effective immediately** (§15.1); consequence: zero hand-rolled DLL surface, and my round-1 "hand-wrapped advapi32" risk is deleted.
2. **claude-1's W-LOCK mechanism CONFIRMED.** `$GOROOT/src/syscall/syscall_windows.go:395` is exactly `sharemode := uint32(FILE_SHARE_READ | FILE_SHARE_WRITE)` — no `FILE_SHARE_DELETE` in `os.Open`/`os.OpenFile`; `$GOROOT/src/os/root_windows.go:176` does pass `FILE_SHARE_READ|WRITE|DELETE`. Verified at go1.27.1; the repo pins go 1.26 (CI 1.26.8) — the line is long-stable, but the CI-toolchain line check claude-1 asked for remains open. Verdict on zcode-1's W3 sizing: claude-1's "WRONG at HEAD, the lock design already exists" stands — `lock_windows.go` is real; the demonstrated defect class is share-mode/open-path, not lock acquisition.
3. **claude-1's procctl caller traces CONFIRMED** (new claims, non-owner verdict): `internal/runner/durablekill.go:29` (`Alive` gates `clearStale` → unreachable on Windows; `Attributed` fails closed → "process verification failed … not killed"), `durablekill.go:63-69` (`AgentLiveness` → "stale" for live agents), `internal/trajectory/verification.go:142` (`!Alive → continue` never taken; `KillTreeAttributed` refuses → captured-criterion cleanup fails unconditionally), `internal/runner/reviewsnapshot.go:232-244` (`ownerAlive` always true when `BootID==""` → `sweepStaleSnapshots` never reclaims). All four traces byte-match the source. Also CONFIRMED verbatim: `internal/driver/proclive_windows.go`'s premise comment ("no portable signal-0 liveness probe on Windows without golang.org/x/sys") — false at HEAD, though that function's always-true return is a *documented* conservative driver-lock choice (AF3), a different beast from procctl's stub.
4. **claude-1's `sh -c` SELF-CORRECTION CONFIRMED.** Non-test product source invokes a POSIX shell at `internal/evidence/execute.go:118`, `:122` (`criterionSupervisor`) and `internal/app/driver_impl.go:266` (the `checks:` gate). My round-1 framing of shell as test-side-only, and zcode-1's "product's only /bin/sh is build-tagged terminal_unix.go", are both wrong.
5. **claude-1's verdict on zcode-1's chmod class CONFIRMED.** `grep -c Chmod internal/driver/strict_gate_test.go` = 0; the chmod fixtures are `phase_event_test.go:152/:165/:182` and `impl_test.go:800` (all confirmed). `strict_gate_test.go:179` needs its own diagnosis — do not fold it into the chmod bucket.
6. **claude-1's W-NAME "unvalidated block ID" claim CONFIRMED.** `internal/pipeline/manifest.go:156-173` validates only non-empty, uniqueness, kind/risk/transport enums — no character set, length, or path check, while the ID is interpolated into paths at `gate.go:105` (and per claude-1, `executor.go:30`, not re-verified by me). Security-adjacent, all-OS, in scope.
7. **Gate identity flow CONFIRMED** at the primary sites: `SaveGate`/`LoadGate` (`gate.go:108-138`) always receive the edge ID from the caller and the JSON body carries `Edge`; no filename→ID reverse parse at these sites. `SaveGate` stages `path + ".tmp"` — matches the hosted `.tmp` failures; the staging name must take the same encoding.
8. **claude-1's F.1 mechanism CONFIRMED modulo toolchain**: `$GOROOT/src/os/types_windows.go:176-183` synthesizes 0444/0666 from the READONLY attribute alone, so `Perm()&0077` can never be 0 on Windows (go1.27.1; same 1.26 caveat as item 2).
9. **`FlushFileBuffers` is exported in x/sys** (`syscall_windows.go:257`). Whether it succeeds on a directory handle opened with `GENERIC_WRITE|FILE_FLAG_BACKUP_SEMANTICS` is **not decidable from source** — hosted spike required (below).

## Position changes since round 1

1. **ACL implementation path: I withdraw my ALT-1 gap claim.** Creation and verification both fit in x/sys's exported API. WS-D shrinks; the struct-layout risk I flagged is gone. What remains is policy (allow-set) and adversarial testing, not wrapping.
2. **Process scope: I concede WS-E's Job-Object/attribution half.** claude-1's P-A/P-B split is correct and zcode-1's Stage 5 matches it. My round-1 proposal bundled full attribution into this idea; claude-1's R4 argument — P-B re-opens the attribution gate the base commit's U2 repair just hardened, on a platform nobody can test locally — is decisive. This idea takes P-A (truthful liveness, fail closed on `ERROR_ACCESS_DENIED`, honest refusal strings); P-B (Job Objects, creation-time/image-name facets) becomes a named follow-up idea in FINAL's deferral list.
3. **FIFO fixture: I accept the simpler existing shape.** zcode-1's and claude-1's convergence on the file's own `makeUnsupportedSocket` (AF_UNIX) fallback behind a build tag beats my junction/reparse candidate — the test's intent (unsupported entry ⇒ digest refuses) is preserved with less machinery. My junction probe is dropped unless AF_UNIX socket creation itself fails hosted, in which case the individually-reviewed exclusion route applies.
4. **CRLF ordering: product git-pinning first, `.gitattributes` demoted.** claude-1's pin `-c core.autocrlf=false -c core.eol=lf` on parley's own git invocations (precedent `reviewsnapshot.go:138-139`) is the product fix because parley snapshots arbitrary user repos whose attributes we don't control. My round-1 `.gitattributes` becomes optional contributor ergonomics, separate reviewed commit, only if wanted at all. Parser-side CRLF tolerance (drift-guard anchor, consensus-template block) stays — embedded-vs-disk comparison on a *user's* checkout is a product requirement, and the hosted mechanism is still `RECALL`-grade for all three of us, so zcode-1's diagnostic-first demand stands.
5. **Shell is product scope, not test scope** (per claude-1's correction, verified in item 4). My WS-A fixture work is unchanged but no longer covers the defect class; see Current proposal.

## Responses to others

### @claude-1 — round-01

- **W-LOCK reduction (your R7, "attack this first"): attacked, and it survives.** The stdlib locators check out (item 2); "architecture-scale" was wrong and your focused `fsutil` change is the right size. One addition to the same change: your read-only-attribute trap (staging files written without `0200` get `FILE_ATTRIBUTE_READONLY` and can't be renamed over) — consistent with what I verified of the Chmod path (`syscall_windows.go:709/:738`) but I did not execute the rename-over-readonly failure, so I tag it plausible-and-test-hosted rather than confirmed. Where zcode-1's bounded retry still fits: only for transient *third-party* handles (Defender/indexer) if they survive the structural share-flag fixes on the hosted leg — bounded, loud, full verification re-run after success. Demonstrated cause gets a structural fix; hypothesis gets a guarded retry. That is the demonstrated-vs-hypothesis split the brief asks for.
- **ACL allow-set: partial disagreement.** Your owner-only DACL is defensible and simplest to verify; my owner+SYSTEM mirrors the Unix "owner plus root-analogue" reality (OS services run as SYSTEM; SYSTEM can take ownership anyway, so exclusion buys nothing). Either closes the idea; I will not block on owner-only. What I do block is zcode-1's Administrators inclusion (below). On verification shape we agree: walk effective ACEs, refuse inherited allowances and unknown trustees, adversarial fixtures (grant `BUILTIN\Users`, assert refusal) — your R3 and zcode-1's concern 9 converge with my readback self-check, now cheap because both halves use the same exported API.
- **Experimental-label conditions (your concern 2): I disagree, and the round-2 brief resolves it on owner scope, not on majority.** The owner-set gate is: remove the label iff hosted windows-latest is green on the released commit, with honest coverage documentation; the brief explicitly forbids silently adding an ARM64-execution gate or other conditions. Your (a) arm64, (b) longpaths, (c) sh-presence gaps are real and must all be *documented* — and (c) is genuinely disposed of by the W-SHELL refusal design (the hosted leg must prove the refusal path with `sh` forced absent, not pass by incidental Git-bash) — but none of them may become extra gates. If you still believe green-plus-disclosure is insufficient, that is a §10 escalation to the owner, not a consensus condition.
- **P-A vs P-B (your concern 4): accepted, P-A.** One scope note: `proclive_windows.go`'s stale premise comment should be corrected in the same change and may adopt the real probe, preserving its documented fail-safe posture (always-true on error, AF3) — comment correction mandatory, probe adoption reviewer's call.
- **W-NAME block-ID validation: CONFIRMED (item 6), include it.** Your ordering (validation primary, encoding defense-in-depth) is right; with a validated charset the encoding's injectivity is trivial and PathEscape's untested corners (`:`, device names, trailing dot) are excluded upstream.
- **Your new claims are now verdicted by a non-owner** (items 2, 3, 6, 8). The ones I could not fully close: go-1.26 toolchain line check, `executor.go:30` interpolation, the exact count of tests masked by the `app_test.go` abort (panic and unchecked assertions confirmed; the "27 masked" figure is inference, consistent with hosted absence of wait/usage results).

### @zcode-1 — round-01

- **Stage 1 allow-set {user, SYSTEM, Administrators}: disputed, counter-proposal: exclude Administrators.** `BUILTIN\Administrators` is a group of *users*, not the OS; on any multi-user or domain-joined box it grants other admins read access to trajectory snapshots — exactly what the Unix `0700` guard this replaces exists to prevent, and the prompt forbids weakening privacy. Your inheritance point (default `%TEMP%` ACLs carry owner+SYSTEM+Administrators) is real but argues the other way: creation must set a *protected* DACL blocking inheritance, and verification must refuse inherited/unknown ACEs rather than accommodate the ambient default. Owner-only (claude-1) or owner+SYSTEM (me) are both acceptable; Administrators is not.
- **Stage 2 per-OS gate names: disputed, counter-proposal stands (universal encoding for new writes + legacy `->` read-fallback).** Your "zero Unix migration" framing answers the backward-compat question, which the read-fallback already solves — verified this round that identity lives in the JSON body and no primary read path parses names back (item 7). The question per-OS naming fails is *forward* cross-OS deck sharing: a deck synced via git or a shared volume between a macOS and a Windows machine (this very workspace sits on such a volume) would show different on-disk names for the same gate, each invisible to tooling on the other OS. Universal encoding costs one fallback lookup and keeps names portable forever. Please engage that scenario specifically.
- **Stage 3 directory-sync no-op: conditionally accepted, re-ordered behind a spike.** Your hosted evidence (`verification_test.go:368` ACCESS_DENIED) proves the *current* open path (`os.Root` dir handle, read access, no backup semantics) cannot flush; it does not prove `FlushFileBuffers` fails on a handle opened with `GENERIC_WRITE|FILE_FLAG_BACKUP_SEMANTICS` — the wrapper is exported in x/sys (item 9) and Microsoft documents that open mode for directory handles. The brief is explicit: documentation must not silently replace a real implementation or refusal. So: one hosted spike (build-tagged probe test) first; if flush works, implement it (my WS-C(iii)); if it fails, your documented nil-with-rationale plus a FINAL known-risk entry is the floor — a *reviewed, disclosed* weakening, and file-handle fsync stays mandatory either way. Refusing the whole write path over directory-entry durability would be disproportionate, and on that point I agree with you over a refusal reading.
- **Stage 4: partially preempted by demonstrated mechanism** (item 2). Keep your bounded-retry shape only as the residual guard for third-party transient handles, post-structural-fix, and only if hosted evidence still shows violations. Your rename-root test redesign (mutate a file inside the root instead of renaming the root under an open `os.Root`) is endorsed — the guarantee stays, the assertion stays.
- **Stage 5 / Stage 6 T4 / T8: converged.** P-A-shaped procctl honesty fix; `denyRead` helper sharing the WS-D ACL module (three consumers); ACP handshake restructured to remove capacity assumptions entirely (not retuned — my round-1 concern 8 stands). Two corrections you owe: `strict_gate_test.go:179` is not a chmod failure (item 5 — re-diagnose from the log, do not fold), and your "product's only /bin/sh is build-tagged" claim is wrong (item 4) — the criterion-supervisor and `checks:` gate are product shell dependencies needing the refusal contract below.
- **Version recommendation (1.50.0-class over staged 1.49.1): endorsed as reasoning, deferred to FINAL** — the brief pins selection to "after the prerequisite releases, above the then-released version", which is compatible with your argument.

## New concerns / questions

1. **Bundle the open empirical questions into one early hosted probe commit** (build-tagged, test-only): ACL create→verify round-trip plus adversarial-trustee refusal; `FlushFileBuffers` on a backup-semantics directory handle; AF_UNIX socket creation on the runner image; runner `git config` dump + a hexdump of one CRLF-mismatch file. This resolves my round-1 concerns 2–4, zcode-1's concerns 1–3, and claude-1's R3 with one ~20-minute hosted cycle instead of discovering each mid-implementation.
2. **The Windows shell contract needs one sentence of product policy in FINAL:** captured verification and `checks:` require a POSIX shell on Windows; absent `sh` ⇒ named, user-visible refusal, and the hosted leg must exercise that refusal with `sh` forced off `PATH` (otherwise green proves only "Git for Windows is installed"). If peers cannot accept that minimal contract, it is a legitimate §10 escalation to the owner — but I consider refusal-now plus a `mvdan.cc/sh/v3` follow-up idea (new dependency executing untrusted `checks:` strings; needs its own security review) the right scope line.
3. **`wait`/`usage` first Windows execution** (zcode-1 concern 4, claude-1 §7): budget one diagnostic cycle after the `app_test.go` comma-ok fix; do not assume green.
4. **proclive premise comment** is now demonstratedly false at HEAD (item 3); leaving it would mis-document the driver lock. Folding the correction into P-A keeps one file honest.
5. **Deck-on-shared-volume sentence**: FINAL should carry the one documented sentence that this workspace itself resides on a cross-machine shared volume whose Windows-side semantics are unvalidated — my round-1 concern 7, still open.

## Current proposal

Merged design I would sign today, ordered:

1. **Probe + unblock** — the hosted probe commit (concern 1); Mkfifo build-tag onto the existing AF_UNIX fallback; `app_test.go` comma-ok assertions; re-exec-self/PATHEXT fixtures; `USERPROFILE` in env fixtures; volume-aware worktree fixture paths; ACP handshake restructure; `GOOS=windows build+vet` guard step on the ubuntu leg (compile evidence only, labeled).
2. **W-PRIV (ACL privacy)** — per-OS guard; Windows creates a protected DACL (owner + SYSTEM; **Administrators excluded**) via `ACLFromEntries`/`SetNamedSecurityInfo`, verifies by effective-ACE walk refusing unknown trustees and inherited allowances; adversarial hosted fixtures; Unix byte-identical; same module powers `denyRead` test fixtures.
3. **W-NAME** — manifest block-ID validation (charset, length, no device names, no traversal) on all OSes; universal filename encoding for new writes plus legacy `->` read-fallback for one release; staging `.tmp` name encoded identically; persisted-name decision documented in FINAL.
4. **W-LOCK/sharing** — share-flag-correct opens (`os.Root` route preferred, `windows.CreateFile` helper where needed) on ledger/lock-origin/publication paths; close-before-rename handle discipline; read-only-attribute clearing before replace; bounded retry only as the residual third-party-handle guard, loud and re-verifying.
5. **Directory durability** — real `FlushFileBuffers` (backup-semantics open) if the probe passes; else the reviewed, FINAL-documented weakening; file fsync unchanged.
6. **W-PROC (P-A)** — truthful `Alive` (`OpenProcess`+`GetExitCodeProcess`, fail closed on access-denied); honest Windows refusal strings for durable kill/attribution; `proclive` premise comment corrected; P-B deferred to a follow-up idea.
7. **W-SHELL** — explicit `sh` detection + named refusal in `execute.go`/`driver_impl.go`; hosted test forcing `LookPath("sh")` failure; contract sentence in FINAL/docs.
8. **W-CRLF** — pin `-c core.autocrlf=false -c core.eol=lf` on parley's own git invocations; CRLF-tolerant embedded-vs-disk parsers; mechanism confirmed via probe before finalizing anything further.
9. **Acceptance, unchanged from the prompt** — unfiltered green windows-latest/macOS/ubuntu on the released commit; per-exclusion reviewed table in `IMPLEMENTATION.md`; suppression grep at close; coverage envelope documented (hosted x64 Server, longpaths on, runneradmin, no ARM64 execution, no desktop/ReFS claims); label removal and winget strictly per the owner gate.

**Readiness:** proposal-ready. Open disputes requiring engagement, not majority-smoothing: Administrators in the ACL allow-set (me + claude-1 vs zcode-1 — security-policy, needs zcode-1's explicit answer), universal vs per-OS gate names (me + claude-1 vs zcode-1 — forward-sharing argument unanswered), label-gate scope (claude-1 vs the brief's owner-scope restatement — resolved by owner direction unless claude-1 escalates). Everything else has converged or been reduced to located evidence.

**Drafter/implementer nomination: claude-1** — their round-1 produced the most load-bearing corrections including against their own prior artifact (the exact discipline FINAL authorship needs), the deepest verified mechanism evidence (W-LOCK, procctl, shell), and the scope reductions this merged plan adopts. Per Phase 4, with `author: user` the fallback drafter is the first round-01 submitter (zcode-1); my nomination is for claude-1 to volunteer with `Drafter: yes` at consensus, with zcode-1 and me as the two independent reviewers.
