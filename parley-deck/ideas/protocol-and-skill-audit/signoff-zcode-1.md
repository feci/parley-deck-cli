### Signoff: zcode-1 — 2026-08-21
Status: ✅ ACCEPT
Notes: All four of my original reservations are now verified fixed, and I re-derived every corrected figure myself from `round-02/*.md` rather than taking the document's word. (1) @kimi-1's row is `36 / 6 / 0 / 0` over 42 assessed — mechanically exact from her verdict headings. (2) The two false attribution sentences are gone and their replacement is true: all nine REFUTED verdicts in the three-verifier corpus are @codex-1's (seven against my findings, one against @claude-1/F1, one against @kimi-1/F5); ~89% is right at 66 of 74. (3) @codex-1/F15, @codex-1/F23 and @kimi-1/F4 are recorded as my CONFIRMED verdicts and now carry fixes or dispositions, and §3's PARTIAL-only list is down to the true two (@codex-1/F12, @zcode-1/F4). (4) @claude-1/F2 no longer reproduces at HEAD — `roster render --yes --adopt-inherited` exits 0 and the drift guard passes afterward, where at `09e9ab3` the same sequence failed — @claude-1/F3 now has a real emission path into STATUS, and both carry dispositions; the preflight roster fix, `meta/version.json`'s `protocolRole: source`, @codex-1/F20's Fixed-table row and the kimi-1/F5 re-recording as dismissed all check out live. `go test ./...` exits 0 (27 packages; 26 with tests, `cmd/parley` has none — my round-1 "26 packages" counted `ok` lines, the correction's "27" counts `go list ./...`; both true under their own counting) and `npm test` exits 0 (391/391) in scratch copies; I mutation-checked @codex-1/F23 by reverting its two source files to the parent commit with the test left in place — both pinning tests FAIL, exit 1; restored, exit 0. The audit's substance stands and I accept it. [Superseded by the Final round below: the two reservations this paragraph recorded — the 36-vs-37 headline and the uncommitted skill-repo fix — are both discharged at `6cf39f6`/`40ae6f5`; the ACCEPT above is final.]

## Evidence

- PRIMARY — round-2 verdict tallies, from commands run this session on the shared tree (read-only): `grep -n "^### " parley-deck/ideas/protocol-and-skill-audit/round-02/kimi-1.md` → 42 verdicts: 36 CONFIRMED, 6 PARTIAL (codex-1/F12, F15, F23; zcode-1/F4, F10; claude-1/F1), 0 REFUTED, 0 UNREPRO; her own summary at `round-02/kimi-1.md:322` reads "REFUTED: 0 outright; UNREPRODUCIBLE: 0". `grep -n "^### " parley-deck/ideas/protocol-and-skill-audit/round-02/codex-1.md` → 6 CONFIRMED / 8 PARTIAL / 9 REFUTED (zcode-1 F2, F3, F5, F9, F10, F12, F13; kimi-1/F5; claude-1/F1). The consensus's codex-1 and zcode-1 rows are correct; its kimi-1 row is not.
- PRIMARY — claude-1/F2 still reproduces at HEAD `09e9ab3`, run entirely in a clone (`/tmp/pdaudit-signoff`; the shared tree was not touched): `go test ./internal/protocol -run TestEmbeddedDefaultMatchesLiveDeck` → ok; `go build -o /tmp/parley-audit-bin ./cmd/parley && /tmp/parley-audit-bin roster render --dir /tmp/pdaudit-signoff --yes --adopt-inherited` → "Regenerated §2 in /tmp/pdaudit-signoff/parley-deck/COOPERATION.md", exit 0; test re-run → `--- FAIL: TestEmbeddedDefaultMatchesLiveDeck ... anchor "| Agent ID       | Workspace dir                       | Role          |" appears 0 times, want exactly 1`.
- PRIMARY — claude-1/F3 not fixed: `grep -rn "masked-by-env" /Volumes/My Shared Files/AI_WORKSPACE/parley-deck/parley-deck-skill/skills/parley-deck/SKILL.md internal/app/roster_set.go` → `SKILL.md:284` still lists `masked-by-env` in the closed STATUS vocabulary; `internal/app/roster_set.go:83-88` still emits it only as advice text, never via `addStatus`.
- PRIMARY — my confirmed findings shipped (spot checks on the shared tree, read-only): `grep -n "end-to-end-pipeline" parley-deck/COOPERATION.md` → no matches (F6 fixed); `sed -n '36,39p' parley-deck/COOPERATION.md` → the Quickstart now names §15 and §10 (F7 fixed).
- SECONDARY — my three misrecorded verdicts: `parley-deck/ideas/protocol-and-skill-audit/round-02/zcode-1.md:234` (codex-1/F15 — CONFIRMED), `:329` (codex-1/F23 — CONFIRMED), `:404` (kimi-1/F4 — CONFIRMED); each carries a severity correction, and none was refuted by any verifier. My row matches my file: 30 CONFIRMED / 2 PARTIAL (codex-1/F12 at `:185`, claude-1/F1 at `:428`) over 32 assessed.
- SECONDARY — the disposition gaps: `parley-deck/ideas/protocol-and-skill-audit/IMPLEMENTATION.md` (line 24) fixes a "claude-1/F2 (probe path)" that matches no filed claude-1 finding — `round-01/claude-1.md` F2 (lines 39-66) is the roster-render drift guard, and `review/round-01/` contains no claude-1 file at all; codex-1/F23, kimi-1/F4, and the design claude-1/F2 and F3 appear in no Fixed table, no NOT-fixed entry, no "Still open" line, and no deferred-follow-up list in `IMPLEMENTATION.md` or `review/consensus.md`.
- SECONDARY — codex-1/F20 (listed confirmed in the consensus, absent from IMPLEMENTATION.md's tables) is in fact fixed in code: `internal/consensus/consensus.go:462-491` counts signoffs "only from the `## Signoffs` heading onward", with the measured rejection of the stricter section rule recorded in the comment.
- SECONDARY — the F14 retraction: `round-02/claude-1.md` cast doubt on my 27% figure; `IMPLEMENTATION.md` ("Correction against myself on zcode-1/F14") retracts it — "Its number stands and my objection was too narrow" — and F14 was repaired as filed. My round-01 population was the 78 FINAL.md-bearing directories (`round-01/zcode-1.md`, F14 command), not "a population that includes genuinely open ideas" as consensus §3 states.
- SECONDARY — section 4's participation account matches `round-02/claude-1.md` (hermes-1's fourth attempt, "4 tool calls used") and the files exist as described; the table's exclusion of hermes-1's round-2 verdicts is also right — `round-02/hermes-1.md` cites commands against paths that do not exist in this repository (e.g. `python -m roster render`, `docs/protocol-appendix-a.md`), consistent with the fabricated-PRIMARY history `review/consensus.md` discloses.
- RECALL — the section-5 claim that "the owner has ruled that no complete release ships until the command defects are fixed": I found no file recording that ruling; nothing in the repo contradicts it (VERSION still 1.45.0, no post-audit release), and with codex-1/F6, F8, F14 deferred the condition it describes remains unsatisfied either way.

## Re-verification after the ledger correction

Re-verified at HEAD `2a5ea3a` on 2026-08-21, in a fresh clone (`git clone --no-hardlinks <shared-tree> /tmp/pdaudit-reverify`) and a scratch copy of the skill tree; the shared tree was only read. I did not read any other reviewer's signoff file before finalizing this one.

- PRIMARY — tallies and attributions re-derived mechanically, not by eye. Command, run from `parley-deck/ideas/protocol-and-skill-audit/round-02/`: `for f in kimi-1 codex-1 zcode-1; do awk -F' — ' '/^### /{key=$1; sub(/^### /,"",key); print key, $NF}' $f.md; done | awk '{v=$2; key=$1; if(v~/^CONFIRMED/)c[key]=1; if(v~/^PARTIAL/)p[key]=1; if(v~/^REFUTED/)r[key]=1} END{for(k in c)if(!r[k])n++} ...'` (pooling per (author, finding) across the three files, counting keys with ≥1 CONFIRMED and 0 REFUTED). Results: @kimi-1 `36/6/0/0` over 42 assessed; @codex-1 `6/8/9/0` over 23; @zcode-1 `30/2/0/0` over 32 — all three §1 rows exact. All nine REFUTED keys belong to @codex-1 (zcode-1/F2, F3, F5, F9, F10, F12, F13; claude-1/F1; kimi-1/F5) — §1's corrected attribution sentence is true of the three-verifier corpus. Pooled lenient-pair rate (36+30)/(42+32) = 66/74 ≈ 89% — §1's corrected figure is right. Confirmed set under §2's criterion: 36 distinct keys in the corpus (a 48th distinct key is `round-02/codex-1.md:357`'s "Additional verification" addendum heading, which carries no verdict), decomposing exactly as @codex-1 ×23, @zcode-1 ×7, @kimi-1 ×4, @claude-1 ×2; adding `round-02/claude-1.md:12` (hermes-1/F-Q2 — CONFIRMED, no refutation anywhere) makes 37 — the number §2's corrected breakdown and §5's four-surface split actually enumerate under their "36" headline (reservation 1 in Notes).
- PRIMARY — `go test ./...`: run in the clone as `go test ./... > /tmp/gotest-head.log 2>&1; echo "go test exit code: $?"` — the exit code is read from the shell's `$?` echoed immediately after the run, not inferred from the log text. Output: `go test exit code: 0`; 26 `ok` package lines; `go list ./... | wc -l` → 27 packages, the 27th being `cmd/parley [no test files]`. The correction's "27 packages, exit 0" is accurate; my round-1 "26 packages" counted `ok` lines.
- PRIMARY — `npm test`: skill tree copied with `rsync -a --exclude '/dist/' <shared-skill-tree>/ /tmp/pdaudit-skill/` (anchored exclude — a first attempt with unanchored `--exclude dist/` also stripped `node_modules/*/dist` and failed `design-addons.test.js` with MODULE_NOT_FOUND; that exit 1 was my copy artifact, not the repo), then `npm test > /tmp/npmtest.log 2>&1; echo "npm test exit code: $?"`, exit code again read from `$?`. Output: `npm test exit code: 0`; node:test reports `tests 391 / pass 391 / fail 0`, then the python suite and the addon-manifest `--check` pass. Matches the commit's claim exactly.
- PRIMARY — mutation check on @codex-1/F23, in my clone: at HEAD, `go test ./internal/runner -run 'TestValidateImplementationArtifact' -v` → both `TestValidateImplementationArtifactRejectsAnUnknownStatus` and `TestValidateImplementationArtifactRejectsAnEmptyOrMentionedSummary` PASS (exit 0). Then `git checkout 815c93a^ -- internal/protocol/reviewartifact.go internal/runner/phase58.go` (source reverted, test file left at HEAD) → the same command shows both tests `--- FAIL`, exit 1. Then `git checkout HEAD -- <the two files>` → exit 0 again. The fix is pinned by its own test both ways (status: banana / empty-or-merely-mentioned Summary).
- PRIMARY — @claude-1/F2 fixed end to end at HEAD: `go build -o /tmp/parley-rv-bin ./cmd/parley && /tmp/parley-rv-bin roster render --dir /tmp/pdaudit-reverify --yes --adopt-inherited` → "Regenerated §2", exit 0; `go test ./internal/protocol -run TestEmbeddedDefaultMatchesLiveDeck -v` → PASS, exit 0. The exact sequence that failed at `09e9ab3` (original evidence above) is now green — anchors match on column signature.
- PRIMARY — preflight addendum and role fix: `/tmp/parley-rv-bin preflight --dir /tmp/pdaudit-reverify` probes exactly the six ROSTER IDs (claude-1, codex-1, hermes-1, kimi-1, opencode-1, zcode-1) — `agy`, an installed adapter family, is gone — and reports `role=source ... classification=source-advisory`; exit 3 comes from one live availability gate (kimi-1 `unavailable:no-pong` on this machine at run time), not a role gate. `cat parley-deck/meta/version.json` → `"protocolRole": "source"` (landed in `815c93a`).
- SECONDARY — @claude-1/F3 fix path: `internal/app/roster.go:320-326` resolves `config.RosterMaskedFields(root, id)` and emits via `row.addStatus("masked-by-env")` when a field's value actually differs across config layers; `internal/config/runtime.go:1204` onward implements the layer diff; `internal/config/runtime_test.go` (100 lines, `930c268`) pins it. The STATUS vocabulary entry now has an emitter, not just a doc.
- SECONDARY — @kimi-1/F4 fix lives in the skill repo's working tree: `git status --short` → ` M lib/installer.js`, `?? test/source-role-advice.test.js`, last commit `68deb1f`. The diff adds `protocolRole` to `projectStatus` and branches `recommendedActions` ("do NOT adopt the packaged protocol here" for source; consumer and absent-role advice unchanged); `test/source-role-advice.test.js` pins all three roles and ran as part of the 391. Functional and tested, but uncommitted — reservation 2 in Notes.
- PRIMARY — deck-wide numbers behind the new gate: `find parley-deck/ideas -name IMPLEMENTATION.md | wc -l` → 72; frontmatter-only `status:` values (awk between the leading `---` fences) → 62 `complete`, 4 `ready-for-review`, 4 `implemented`, 1 `in-progress`. The disposition's "5 live files use `ready-for-review` or `in-progress`" (4+1) is exact, all five values sit inside the closed vocabulary, so "zero live artifacts newly rejected" holds. My first grep matched `status:` in prose too (119 hits) — only the frontmatter scan is the valid measurement.
- SECONDARY — the remaining ledger entries: `IMPLEMENTATION.md:209` records @codex-1/F20 in the Fixed table; `:210` re-records kimi-1/F5 as dismissed; `:215` "Still deferred: codex-1/F6, F8, F14; kimi-1/F1" with reasons at `:49-76` — all matching the consensus's Dispositions section, including the F6/F8/F14 design-decision reasons §5 now reconciles the release rule with.
- SECONDARY — the one newly-false claim: `parley-deck/ideas/protocol-and-skill-audit/consensus.md` lines 11, 42 ("Confirmed — 36 findings"), 119 ("The 36 confirmed findings are the fix list") vs the same document's own enumerations at lines 45-49 (23+7+4+2 plus Q2 = 37) and 120-122 (24+8+4+1 = 37); commit `2a5ea3a`'s message repeats "the true count is 36". §2's body labels "(21)" (`consensus.md:51`) and "(3)" (`:64`) are the stale pre-correction counts. No finding's disposition changes with either number; the fix is a one-line edit (36 → 37, or scope 36 to the corpus and move Q2 out of §5's decomposition).

## Final round

Re-verified at HEAD `6cf39f6` (cli) and `40ae6f5` (skill) on 2026-08-21. Both reservations from my previous round are discharged, and I re-derived every number in the corrected consensus myself at this HEAD before concluding. **✅ ACCEPT — final.** I read no other reviewer's signoff file this round except the two-line Status headers of `signoff-codex-1.md` and `signoff-kimi-1.md`, after my own re-derivation had already settled my verdict, solely to fact-check the consensus's "blocked again" assertions; both read `Status: ❌ BLOCK`, as the consensus recounts.

### Reservation 1 — the 36/37 off-by-one: discharged

The two counts are now stated separately and every surface agrees. Re-derived at this HEAD, from `round-02/{kimi-1,codex-1,zcode-1}.md` on the shared tree (read-only):

- PRIMARY — per-verifier verdict tallies, via `grep '^### ' <file>.md | sed 's/^### //' | awk -F' — ' '<classify by $NF>'`: @kimi-1 36 CONFIRMED / 6 PARTIAL / 0 REFUTED / 0 UNREPRO over 42 headings; @codex-1 6/8/9/0 over 23 verdicts (the 24th heading is the `round-02/codex-1.md:357` "Additional verification" addendum, which carries no verdict); @zcode-1 30/2/0/0 over 32. All three §1 rows exact.
- PRIMARY — the pooled confirmed set (≥1 CONFIRMED, 0 REFUTED across the three files, keyed per author/finding): exactly **36 keys** — @codex-1 ×23 (F1–F11, F13–F24: every codex finding except F12), @zcode-1 ×7 (F1, F6, F7, F8, F11, F14, F15), @kimi-1 ×4 (F1, F2, F3, F4), @claude-1 ×2 (F2, F3). PARTIAL-only keys: exactly @codex-1/F12 and @zcode-1/F4 — §3's "two findings, not five" is exact. All nine REFUTED keys are @codex-1's verdicts (seven against zcode-1, one against claude-1/F1, one against kimi-1/F5). 66/74 = 0.8919 ≈ 89%.
- PRIMARY — the corpus itself: `grep -c '^### F[0-9]'` in `round-01/` → codex 24, zcode 15, kimi 5, claude 3; 24+15+5+3 = **47**, and codex's 23 assessed = exactly the non-codex 23. Contested = 7 refuted + claude-1/F1 + kimi-1/F5 + 2 PARTIAL-only = **11**, matching the title; 36 + 11 = 47 closes the ledger.
- PRIMARY — the supplemental: `round-02/claude-1.md:12` is `### hermes-1/F-Q2 … — CONFIRMED`, no refutation anywhere → fix list = 36 + 1 = **37**. §2's per-agent bullets now read (23) and (4) and list F15, F23 and kimi-1/F4; §5's surfaces `24 + 8 + 4 + 1 = 37` (codex 23 + claude-1/F3; zcode 7 + claude-1/F2; kimi 4; Q2); title, §2 header ("37 findings: 36 from the round-02 corpus, plus one supplemental measurement") and §5 opening ("the fix list (36 corpus + @hermes-1/Q2)") all agree with the derivation. No stale (21)/(3) labels remain — `git show a1926ae:…consensus.md` vs HEAD confirms the only surviving counts are the correct ones.
- PRIMARY — the "CORRECTED TWICE" history paragraph is itself accurate: at `a1926ae` the §2 header said 33 while its bullets enumerated 21+7+3+2+1 = 34 (verified from `git show a1926ae:parley-deck/ideas/protocol-and-skill-audit/consensus.md`); at `2a5ea3a` the header said 36 while the components summed to 23+7+4+2+1 = 37; at `35be2e5`/`6cf39f6` the two numbers are stated separately. The document's account of its own errors is now true in all three states.

### Reservation 2 — the kimi-1/F4 fix cited no reviewed commit: discharged

- PRIMARY — `git -C <shared-skill-tree> show --stat 40ae6f5` → exactly two files: `lib/installer.js` (+14/−1) and `test/source-role-advice.test.js` (+67, committed as blob `21ff5db`). `git rev-parse HEAD` → `40ae6f5417dd0275dec8c5384139972ebce46c5e`; `git status --short` → empty (clean — nothing of the fix remains uncommitted). The diff, read in full: `projectStatus` resolves `protocolRole` from `meta/version.json`, and `recommendedActions` branches — a `source` deck gets "do NOT adopt the packaged protocol here … Re-package from this deck when the protocol changes", while consumer and unknown-role advice keep the old wording. That is the fix the disposition describes, in the repo the disposition now names. The prior false citation is neither repeated nor hidden: `IMPLEMENTATION.md`'s Fixed-table row reads "**`parley-deck-skill@40ae6f5`** (NOT `815c93a` — separate repo; see the consensus note)", and the consensus carries a dedicated section ("A ledger error committed while correcting the ledger") recording that `815c93a` never contained the fix and the earlier 391-pass had been measured on uncommitted code. I verified that section's account against `git diff 2a5ea3a..6cf39f6` — the correction and the disclosure landed together, in `6cf39f6`.

### Both suites, re-run at this HEAD

- PRIMARY — `go test ./...` in a fresh clone (`git clone --no-hardlinks <shared-tree> /tmp/pdaudit-final`, HEAD `6cf39f6`): run as `go test ./... > /tmp/gotest-final.log 2>&1; echo "go test exit code: $?" > /tmp/gotest-final.exit` — the exit code is read from the shell's `$?` captured immediately after the run, not inferred from log text. Result: **exit code 0**; 26 `ok` package lines, 0 `FAIL`, plus `cmd/parley [no test files]` (27 packages under `go list ./...`).
- PRIMARY — the seatbelt fact, checked in my own environment rather than skipped: `sysctl kern.boottime` → real value, exit 0 (this host is not seatbelted), so I ran the suite **unmodified — no shim, no `-skip`** — and additionally `go test ./internal/runner -run 'TestDurableKillEndToEndRealProcess' -v` solo → `--- PASS`, exit 0. Same binary, same test, green where the sysctl is readable — consistent with the consensus's framing that the red suite inside Codex is an environment restriction, not a code defect. SECONDARY for the denial itself (documented, not reproducible here): `docs/agent-cli-mechanics.md:31-32` carries exactly the cited bullet; the test exists at `internal/runner/durablekill_test.go:75`; and the quoted message is assembled from `internal/runner/durablekill.go:37` ("process verification failed (" + reason + "); not killed") with `internal/procctl/procctl.go:116` supplying "no recorded boot id".
- PRIMARY — `npm test`, against the **committed** skill tree (working tree clean at `40ae6f5`, so the copy is the commit): `rsync -a --exclude '/dist/' <shared-skill-tree>/ /tmp/pdaudit-final-skill/`, then `npm test > /tmp/npmtest-final.log 2>&1; echo "npm test exit code: $?"` — exit code again read from `$?`. Result: **exit code 0**; node:test `tests 391 / pass 391 / fail 0`, python suite `54 tests OK across 7 files`, addon-manifest `--check` ok. Line 393 of the log is the kimi-1/F4 pinning test passing inside the 391: "✔ a source-role deck is never told to adopt the packaged protocol".

### Nothing newly asserted is false

- PRIMARY — §3's claude-1/F1 line: both citations byte-accurate — `round-02/kimi-1.md:295` is `### claude-1/F1 — PARTIAL` and `round-02/zcode-1.md:428` is my `### claude-1/F1 — … — PARTIAL` (checked with `sed -n '295p'` / `sed -n '428p'`).
- PRIMARY — the two new commits touch exactly what they claim: `git diff 2a5ea3a..6cf39f6 --stat` → `consensus.md` (98 lines), `IMPLEMENTATION.md` (the one kimi-1/F4 citation line), and the four signoff files (including my prior round's append); no other content changed under this idea.
- SECONDARY — the consensus's account of who blocked what is borne out by the tree: `signoff-codex-1.md` and `signoff-kimi-1.md` both carry `Status: ❌ BLOCK` headers (their prior-round verdicts, committed in `35be2e5`/`6cf39f6`), matching "blocked by @codex-1 and @kimi-1 and reserved by @opencode-1 and @zcode-1".

Both reservations are met, every figure I checked re-derives exactly, both suites are green against committed trees, and the one reproducibility hazard (the seatbelt sysctl) is now recorded in the artifact where it belongs. Nothing remains open on my side.
