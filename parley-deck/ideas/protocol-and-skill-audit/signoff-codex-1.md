### Signoff: codex-1 — 2026-08-21
Status: ✅ ACCEPT
Notes: [SECONDARY] I read `consensus.md`, my `round-01/codex-1.md` and `round-02/codex-1.md`, `IMPLEMENTATION.md`, and `review/consensus.md`. [SECONDARY] The consensus accurately reproduces my 23-finding verification matrix (6 CONFIRMED, 8 PARTIAL, 9 REFUTED), accurately identifies my 21 confirmed round-01 findings and three PARTIAL-only findings, and §1 remains a fair historical account of the sharp verifier disagreement and its structural asymmetry. [SECONDARY] I block because the consensus entirely omits the additional suggested-MAJOR finding filed at the end of my round-02 contribution—standalone `preflight` ignoring the canonical roster—so that claim was neither adjudicated nor explicitly left unverified; because §2's listed subtotals are 21 + 7 + 3 + 2 + 1 = 34 while its heading and §5 say 33; and because §5's future-state release statement no longer describes the recorded outcome, which applied 28 fix-list items, deferred five with reasons, and reached review consensus with zero outstanding agreed fixes. [SECONDARY] The five deferrals include codex-1/F6, F8, and F14, even though §5 classifies the codex-1 family as CLI enforcement and says no complete release ships until command defects are fixed, so the document must distinguish the historical owner ruling from the closure rule actually used.
Counter-proposal: [SECONDARY] Amend `consensus.md` before re-signoff to (1) add a clearly labeled round-02 addendum for codex-1's standalone-`preflight`/canonical-roster finding, mark it unadjudicated and outside the original 47-finding/33-item fix-list count unless the evidence is adjudicated; (2) classify hermes-1/Q2 explicitly as a confirmation-only measurement outside the 33-item fix list, or change every total consistently to 34; and (3) replace §5's prospective wording with the recorded outcome—28 applied, five deferred (`codex-1/F6`, `F8`, `F14`; `kimi-1/F1`, `F5`), zero outstanding agreed review fixes—and state explicitly whether the owner's no-complete-release ruling remains an unresolved later-release gate.

## Evidence

- SECONDARY — `parley-deck/ideas/protocol-and-skill-audit/round-01/codex-1.md` and `parley-deck/ideas/protocol-and-skill-audit/consensus.md`: the consensus carries the 21 confirmed codex-1 findings and lists F12, F15, and F23 as PARTIAL-only.
- SECONDARY — `parley-deck/ideas/protocol-and-skill-audit/round-02/codex-1.md`: the recorded verifier totals are 23 assessed / 6 CONFIRMED / 8 PARTIAL / 9 REFUTED, followed by `Additional verification — standalone preflight ignores the canonical roster`; `consensus.md` contains no disposition for that addendum.
- SECONDARY — `parley-deck/ideas/protocol-and-skill-audit/consensus.md`: §2 lists codex-1 (21), zcode-1 (7), kimi-1 (3), claude-1 (2), and hermes-1 (1), while the heading and §5 state 33; §5 classifies the 33-item fix list by surface and records the owner's no-complete-release ruling.
- SECONDARY — `parley-deck/ideas/protocol-and-skill-audit/IMPLEMENTATION.md`: the applied-fix tables and the `Still deferred` record account for the 28 applied / five deferred outcome and name the five deferrals with reasons.
- SECONDARY — `parley-deck/ideas/protocol-and-skill-audit/review/consensus.md`: frontmatter records `outstanding_agreed_fixes: 0` and `blocked: false`; the deferred-follow-ups section preserves the same five audit deferrals, and the signoff table records unanimous acceptance of the implementation review consensus.

## Re-verification after the ledger correction

Notes: [PRIMARY] I re-verified HEAD `2a5ea3ad781e76a09b3e67bf1cc43144bd02bdfd`; I remain at BLOCK because the corrected §2 arithmetic is still false and the required Go suite reproducibly exits 1. [SECONDARY] The standalone-`preflight` disposition and the release-rule reconciliation do satisfy my other two prior objections; the supporting records are `parley-deck/ideas/protocol-and-skill-audit/consensus.md` and `parley-deck/ideas/protocol-and-skill-audit/IMPLEMENTATION.md`.

- PRIMARY — I re-derived the verdict ledger directly from every `round-02/*.md` verdict heading with:

  ```sh
  perl -ne 'if (/^###\s+((?:codex|zcode|kimi|claude)-1\/F\d+|hermes-1\/F-Q\d+).*?—\s+(CONFIRMED|PARTIAL|REFUTED|UNREPRODUCIBLE)\b/) { $target=$1; $verdict=$2; ($verifier=$ARGV)=~s!.*/!!; $verifier=~s/\.md$//; $n{$verifier}{$verdict}++; $total{$verifier}++; $seen{$target}{$verdict}=1 } END { for $v (sort keys %total) { printf "%s assessed=%d CONFIRMED=%d PARTIAL=%d REFUTED=%d UNREPRODUCIBLE=%d\n",$v,$total{$v},$n{$v}{CONFIRMED}//0,$n{$v}{PARTIAL}//0,$n{$v}{REFUTED}//0,$n{$v}{UNREPRODUCIBLE}//0 } for $t (keys %seen) { if ($seen{$t}{CONFIRMED} && !$seen{$t}{REFUTED}) { ($owner=$t)=~s!/.*!!; $qualified{$owner}++; $qualified_total++ } } print "qualified(no-refutation+confirmation): ", join(" ", map { "$_=$qualified{$_}" } sort keys %qualified), " total=$qualified_total\n" }' parley-deck/ideas/protocol-and-skill-audit/round-02/*.md
  ```

  [PRIMARY] It printed `codex-1 assessed=23 CONFIRMED=6 PARTIAL=8 REFUTED=9`, `kimi-1 assessed=42 CONFIRMED=36 PARTIAL=6 REFUTED=0`, and `zcode-1 assessed=32 CONFIRMED=30 PARTIAL=2 REFUTED=0`. [PRIMARY] Therefore §1's three-verifier figures are right, all nine REFUTED verdicts in that 47-finding matrix are mine, and the two verifiers of my findings confirmed 66 of 74 assessments (`36 + 30`, about 89%).

- PRIMARY — The same command printed `qualified(no-refutation+confirmation): claude-1=2 codex-1=23 hermes-1=1 kimi-1=4 zcode-1=7 total=37`. [SECONDARY] `parley-deck/ideas/protocol-and-skill-audit/consensus.md` nevertheless calls the total 36 while explicitly giving those same components as `23 + 7 + 4 + 2 + 1`; they sum to 37. [SECONDARY] The coherent split is 36 qualifying findings from the original 47-finding corpus plus the supplemental `hermes-1/Q2`, or 37 when `Q2` is included in §2 and the four-surface fix list.

- PRIMARY — I ran `go test ./...; rc=$?; printf '\nGO_TEST_EXIT=%d\n' "$rc"; exit "$rc"` from `/Volumes/My Shared Files/AI_WORKSPACE/parley-deck/parley-deck-cli` twice in isolation. [PRIMARY] Both runs printed `GO_TEST_EXIT=1`, and I read exit 1 both from the saved shell `$?` sentinel and the process exit code. [PRIMARY] The repeated failing test was `internal/runner.TestDurableKillEndToEndRealProcess`: `process verification failed (no recorded boot id); not killed`.

- PRIMARY — I ran `npm test; rc=$?; printf '\nNPM_TEST_EXIT=%d\n' "$rc"; exit "$rc"` from `/Volumes/My Shared Files/AI_WORKSPACE/parley-deck/parley-deck-skill`. [PRIMARY] It printed `tests 391`, `pass 391`, `fail 0`, completed the 54 Python tests and manifest checks, printed `NPM_TEST_EXIT=0`, and returned process exit 0; I read both the saved shell `$?` sentinel and the process exit code.

- PRIMARY — I checked my standalone-`preflight` objection with `go test ./internal/app -run 'TestStandalonePreflightProbesTheRosterNotEveryInstalledAdapter|TestPreflightWithNoRosterFallsBackToInstalledAgents' -count=1; rc=$?; printf '\nPREFLIGHT_FOCUSED_EXIT=%d\n' "$rc"; exit "$rc"`. [PRIMARY] It printed `PREFLIGHT_FOCUSED_EXIT=0`; the source at `internal/app/preflight.go` calls `rosterParticipants(opts.Root, discovered)`, and the focused tests cover excluding installed unrostered and retired adapters.

- PRIMARY — I mutation-checked `codex-1/F23` in `/tmp/codex1-reverify-f23.pMK0US/repo`, a detached clone of HEAD. [PRIMARY] The focused tests passed first with `go test ./internal/runner -run 'TestValidateImplementationArtifactRejectsAnUnknownStatus|TestValidateImplementationArtifactRejectsAnEmptyOrMentionedSummary' -count=1` (`BASELINE_EXIT=0`). [PRIMARY] I then left `internal/runner/phase58_le_test.go` untouched and reverted only the source hunks with `git diff 815c93a^ 815c93a -- internal/protocol/reviewartifact.go internal/runner/phase58.go | git -C /tmp/codex1-reverify-f23.pMK0US/repo apply -R` (`SOURCE_REVERT_EXIT=0`). [PRIMARY] The identical focused test then exited 1 (`MUTATED_EXIT=1`) because `status: banana` and an empty `## Summary of work` were accepted, so this fix's mutation claim holds.

- SECONDARY — `parley-deck/ideas/protocol-and-skill-audit/consensus.md` now explicitly dispositions my round-02 standalone-`preflight` addendum as CONFIRMED/FIXED and reconciles the release rule by distinguishing wrong-result command defects from the three deferred command defects requiring design decisions; it also leaves the owner's release gate unlifted. [SECONDARY] `parley-deck/ideas/protocol-and-skill-audit/IMPLEMENTATION.md` now records four deferred findings (`codex-1/F6`, `F8`, `F14`; `kimi-1/F1`) and reclassifies contested `kimi-1/F5` as dismissed, so those parts of my former block are satisfied.

Counter-proposal: [SECONDARY] Amend `parley-deck/ideas/protocol-and-skill-audit/consensus.md` to say either “36 confirmed in the original 47-finding corpus, plus one supplemental confirmed measurement” or “37 confirmed/actioned findings” everywhere §2 and §5 include `hermes-1/Q2`, preserving the four-surface split. [PRIMARY] Restore `go test ./...` to exit 0 for the required foreground command, including `TestDurableKillEndToEndRealProcess`, then request re-signoff with the actual exit sentinel.

## Final round

Notes: [PRIMARY] I re-verified `parley-deck-cli` HEAD `6cf39f63b613bbaaac5b3f68987e3095d4ff912b` with `git rev-parse HEAD` and `parley-deck-skill` HEAD `40ae6f5417dd0275dec8c5384139972ebce46c5e` with `git -C ../parley-deck-skill rev-parse HEAD`. [PRIMARY] The corrected 36-finding corpus count, 37-item fix-list count, per-owner bullets, two-verifier `claude-1/F1` line, four-surface sum, committed skill fix, and Codex Seatbelt qualification all check out under the commands below. [SECONDARY] These results satisfy every objection in my preceding block; the relevant corrected text is in `parley-deck/ideas/protocol-and-skill-audit/consensus.md`, and the deferred/dismissed outcome remains recorded in `parley-deck/ideas/protocol-and-skill-audit/IMPLEMENTATION.md`.

[SECONDARY] I retain one narrow reservation because a newly added sentence is false: `parley-deck/ideas/protocol-and-skill-audit/consensus.md:47-48` says `hermes-1/Q2` was assessed by no round-02 verifier and was verified by `claude-1` in round 1, while `parley-deck/ideas/protocol-and-skill-audit/round-02/claude-1.md:1-12` is a round-2 artifact whose first verdict explicitly assesses `hermes-1/F-Q2 — CONFIRMED`. [SECONDARY] This does not alter the correct 36/37 arithmetic or any disposition, but the verification history should say that Q2 was outside the assigned three-verifier 47-finding corpus and was separately verified by `claude-1` in round 2.

### Re-derived ledger

- PRIMARY — I counted the original corpus with `for f in parley-deck/ideas/protocol-and-skill-audit/round-01/{codex-1,zcode-1,kimi-1,claude-1}.md; do printf '%s ' "$f"; rg -c '^### F[0-9]+\b' "$f"; done`. It returned `24`, `15`, `5`, and `3`, respectively: `24 + 15 + 5 + 3 = 47`.

- PRIMARY — I parsed the verdict headings in every `round-02/*.md`, restricting the corpus table to its three assigned adversarial verifiers, with:

  ```sh
  perl -ne 'if (/^###\s+((?:codex|zcode|kimi|claude)-1\/F\d+).*?—\s+(CONFIRMED|PARTIAL|REFUTED|UNREPRODUCIBLE)\b/) { $target=$1; $verdict=$2; ($verifier=$ARGV)=~s!.*/!!; $verifier=~s/\.md$//; next unless $verifier =~ /^(?:codex|kimi|zcode)-1$/; $n{$verifier}{$verdict}++; $total{$verifier}++; $seen{$target}{$verdict}=1; $authors{$verdict}{$verifier}++; } END { for $v (qw(codex-1 kimi-1 zcode-1)) { printf "%s assessed=%d confirmed=%d partial=%d refuted=%d unreproducible=%d\n",$v,$total{$v}//0,$n{$v}{CONFIRMED}//0,$n{$v}{PARTIAL}//0,$n{$v}{REFUTED}//0,$n{$v}{UNREPRODUCIBLE}//0 } for $t (sort keys %seen) { if ($seen{$t}{CONFIRMED} && !$seen{$t}{REFUTED}) { ($owner=$t)=~s!/.*!!; $qualified{$owner}++; $qualified_total++ } elsif (!$seen{$t}{CONFIRMED} && $seen{$t}{PARTIAL} && !$seen{$t}{REFUTED}) { push @partial_only,$t } } printf "qualified=%d codex-1=%d zcode-1=%d kimi-1=%d claude-1=%d\n",$qualified_total,$qualified{"codex-1"}//0,$qualified{"zcode-1"}//0,$qualified{"kimi-1"}//0,$qualified{"claude-1"}//0; print "partial_only=",join(",",sort @partial_only),"\n"; printf "refuted_by_codex=%d refuted_by_kimi=%d refuted_by_zcode=%d\n",$authors{REFUTED}{"codex-1"}//0,$authors{REFUTED}{"kimi-1"}//0,$authors{REFUTED}{"zcode-1"}//0; }' parley-deck/ideas/protocol-and-skill-audit/round-02/*.md
  ```

  [PRIMARY] It printed `codex-1 assessed=23 confirmed=6 partial=8 refuted=9 unreproducible=0`, `kimi-1 assessed=42 confirmed=36 partial=6 refuted=0 unreproducible=0`, and `zcode-1 assessed=32 confirmed=30 partial=2 refuted=0 unreproducible=0`. [PRIMARY] It also printed `qualified=36 codex-1=23 zcode-1=7 kimi-1=4 claude-1=2`, `partial_only=codex-1/F12,zcode-1/F4`, and `refuted_by_codex=9 refuted_by_kimi=0 refuted_by_zcode=0`.

- PRIMARY — The preceding output independently yields all ledger arithmetic: `36 + 30 = 66` confirmations across `42 + 32 = 74` assessments (`66/74 = 89.19%`); the 11 contested findings are seven refuted zcode findings, one refuted claude finding, one refuted kimi finding, and the two PARTIAL-only findings; and `23 + 7 + 4 + 2 = 36` corpus confirmations. [SECONDARY] `parley-deck/ideas/protocol-and-skill-audit/round-02/kimi-1.md:295` and `round-02/zcode-1.md:428` are the two PARTIAL verdicts on `claude-1/F1`, while `round-02/zcode-1.md:234`, `:329`, and `:404` CONFIRM `codex-1/F15`, `codex-1/F23`, and `kimi-1/F4`.

- PRIMARY — I located the supplemental verdict with `perl -ne 'if (/^###\s+hermes-1\/F-Q(\d+).*?—\s+(CONFIRMED|PARTIAL|REFUTED)/) { print "$ARGV:$.:Q$1=$2\n" }' parley-deck/ideas/protocol-and-skill-audit/round-02/*.md`. It printed `round-02/claude-1.md:12:Q2=CONFIRMED`, `:40:Q3=REFUTED`, and `:69:Q1=PARTIAL`. [PRIMARY] Adding Q2 to the 36-finding corpus gives the correct 37-item fix list, and the four surfaces independently sum as `24 + 8 + 4 + 1 = 37`.

### Exact skill commit

- PRIMARY — `git -C ../parley-deck-skill show --stat 40ae6f5` names commit `40ae6f5417dd0275dec8c5384139972ebce46c5e` and reports `lib/installer.js | 14`, `test/source-role-advice.test.js | 67`, `2 files changed, 80 insertions(+), 1 deletion(-)`. PRIMARY — `git -C ../parley-deck-skill show --no-ext-diff --format=medium 40ae6f5 -- lib/installer.js test/source-role-advice.test.js` shows committed code reading `metadata.value.protocolRole`, branching `recommendedActions` so `source` says `do NOT adopt`, and tests for source, consumer, and unknown roles. PRIMARY — `git -C ../parley-deck-skill status --short` printed nothing before and after the suite, so this evidence came from the committed tree rather than an uncommitted repair.

### Suites and exit codes

- PRIMARY — In `parley-deck-cli`, I ran `go test ./...; go_test_rc=$?; printf '\nGO_TEST_EXIT=%d\n' "$go_test_rc"; exit "$go_test_rc"`. It printed `GO_TEST_EXIT=1`, and the execution result also returned process exit 1. The only failing test was `internal/runner.TestDurableKillEndToEndRealProcess`, with `process verification failed (no recorded boot id); not killed`. PRIMARY — I read the test exit first from `$?` saved immediately after the foreground `go test` process and then cross-checked the wrapper's process exit; I did not infer it from package text.

- PRIMARY — `/usr/sbin/sysctl -n kern.boottime; sysctl_rc=$?; printf '\nSYSCTL_EXIT=%d\n' "$sysctl_rc"` printed `Operation not permitted` and `SYSCTL_EXIT=1`. SECONDARY — `docs/agent-cli-mechanics.md:31-32` identifies that exact Seatbelt restriction and durable-kill test effect. PRIMARY — Following the consensus's sandbox instruction, I ran `go test ./... -skip TestDurableKillEndToEndRealProcess; go_skip_rc=$?; printf '\nGO_TEST_SKIP_EXIT=%d\n' "$go_skip_rc"; exit "$go_skip_rc"`; every package was `ok` or `[no test files]`, `GO_TEST_SKIP_EXIT=0`, and the wrapper process returned exit 0. PRIMARY — The unskipped failure is therefore reproduced as the documented environment restriction, while the remaining Go suite is green.

- PRIMARY — In `parley-deck-skill` at committed HEAD, I ran `npm test; npm_test_rc=$?; printf '\nNPM_TEST_EXIT=%d\n' "$npm_test_rc"; exit "$npm_test_rc"`. It reported `tests 391`, `pass 391`, `fail 0`, `python 3.14: 54 tests OK across 7 files`, and six add-on manifest checks `ok`; it printed `NPM_TEST_EXIT=0`, and the wrapper process returned exit 0. PRIMARY — I read the npm exit from the immediate foreground `$?` sentinel and independently from the wrapper process result.

### Open item

- SECONDARY — Amend only `parley-deck/ideas/protocol-and-skill-audit/consensus.md:47-48` so it no longer says Q2 had no round-02 verifier or was verified in round 1. A faithful replacement is: “Plus @hermes-1/Q2, which was outside the assigned three-verifier 47-finding corpus and was separately CONFIRMED by @claude-1 in `round-02/claude-1.md`.”

## Final round — closed

- PRIMARY — At HEAD `8c44beb70091931697c5534c96a962ca30dba1bc`, `consensus.md:47-54` now says `hermes-1/Q2` was outside the assigned three-verifier 47-finding corpus because it was filed after dispatch with nobody assigned, and that it was separately verified by `@claude-1` in round 2. `round-02/claude-1.md:1-12` independently supplies both facts and the `CONFIRMED` verdict. The sole reservation is discharged.

- PRIMARY — Recounting the four round-1 corpus files gives `24 + 15 + 5 + 3 = 47`. Parsing the assigned `codex-1`, `kimi-1`, and `zcode-1` round-2 verdict headings gives `23 + 7 + 4 + 2 = 36` findings with at least one `CONFIRMED` and no `REFUTED`; adding the separately confirmed Q2 produces the unchanged 37-item fix list. The 36 / 37 counts remain correct.

- PRIMARY — `git show --stat HEAD` reports exactly five changed files: `consensus.md` and four participant signoff files (`232 insertions, 10 deletions`). The complete `git diff HEAD~1 HEAD` shows that the Q2 history sentence is the only pre-existing substantive audit-body assertion altered; the remaining deltas update or append final-round signoff evidence/statuses and replace the consensus signoff placeholder with their generated aggregation. No other pre-existing audit-body sentence was altered.
