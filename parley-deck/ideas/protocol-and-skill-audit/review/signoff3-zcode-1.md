### Signoff: zcode-1 — 2026-08-21
Status: ✅ ACCEPT
Notes: My cycle-2 block is satisfied and I am flipping to accept. Both conditions are dead: (1) `go test ./...` now completes with exit code 0 — I ran it myself in a foreground background-job with the exit code captured from the `go test` process into a dedicated file, not from any wrapper — and IMPLEMENTATION.md no longer carries the stale green claim but records that it was false between `4903b47` and `7112e03`; (2) all four previously-silent MAJORs are dispositioned in the consensus, and I verified each fix at runtime through the CLI with positive controls, not just by reading code: manual `consensus finalize` refuses a FINAL declaring another idea (exit 1, names the mismatch) or a non-final status (exit 1, names status), `consensus draft --review` refuses an artifact with no `reviewed-commit` and leaves no half-written consensus, the deliberation review quorum awaits the implementer again (`addon-manifest-coverage`: partial, missing=[claude-1]) while standard/fast do not, and `preflight --yes` clears the `unknown-freshness` gate on a freshly `parley init`ed deck (classification `freshness-confirmed`, hash persisted). The corrected consensus claims reproduce exactly: my own three-binary sweep over all 66 review consensuses counts 30 flips at `0bb9903` vs base `a1926ae` (6 partial→ready, 24 →malformed) and 5 at HEAD, all partial→ready; the anti-drift claim is now carried by `TestAFinalBuiltFromThePromptSatisfiesTheProductionGate`, which I broke successfully by reintroducing a hardcoded prompt list. I mutation-checked five of the fixes: reverting the `ValidateFinal` status/slug checks, the `reviewed-commit` binding, the deliberation quorum split, the `--yes` branch, or the prompt's generated section list each fails its specific test. My one reservation — that reverting ONLY the manual finalize call site (`consensus.go` passing `idea.Slug` into `protocol.ValidateFinal`) back to content-only `FinalIsScaffold` passed the entire suite, leaving the manual binding unpinned — is now DISCHARGED: the test I suggested landed as commit `745ead5` (`TestFinalizeRejectsASubstantiveFinalWithTheWrongSlugOrStatus`), and I re-verified it is tracked, green at HEAD, and red under exactly that mutation. See "Reservation discharged" below. The suggested follow-up test from my draft signoff is exactly what landed, so there is no counter-proposal.
Counter-proposal (required if ❌): —

## Evidence
All runs in clones/copies under /tmp (`git clone --no-hardlinks` at 39dbc77, verified `git rev-parse HEAD` = 39dbc778ba56ec20f531a9da249e0cb018e0a381); the shared tree was written only in this file. Exit codes for suites were read from the test process's own status, captured immediately (`ec=$?` written to a file), never from a pipeline's last command — I caught myself reading `$?` after a `| tail` once mid-session and redid that measurement.

**PRIMARY — suites.**
- `go test -count=1 -timeout 15m ./...` in /tmp/pd3-zcode1: exit code **0**, read from `/tmp/pd3-zcode1-gotest.exit` (written from `go test`'s own `$?`); log shows 26 `ok` packages (internal/app 83.9s, no hangs) + 1 `[no test files]` (cmd/parley) = the consensus's "27 packages, exit 0".
- `npm test` in a full copy of `../parley-deck-skill` (top-level `dist/` excluded): exit **0**, 388/388 pass, plus python tests and manifest checks ok. A first run failed with exit 1 — my copy's fault, not the code's: my `rsync --exclude dist/` also matched `node_modules/commonmark/dist/`; re-syncing with an anchored `--exclude /dist/` fixed it. Noted so nobody chases a phantom failure.

**PRIMARY — the four MAJOR fixes, runtime, each with a positive control (HEAD binary /tmp/pd3-bin-39dbc77 on scratch decks).**
- Manual finalize: FINAL with complete sections but `idea: OTHER-IDEA` → exit 1, "frontmatter idea=\"OTHER-IDEA\" but it closes idea \"t1\""; `status: accepted` → exit 1, "frontmatter status=\"accepted\", want final"; valid FINAL → "Finalized consensus and closed the idea", 00-prompt.md flips to `status: final`.
- `consensus draft --review`: artifact with Findings + Refutation attempts but no `reviewed-commit` → exit 1 naming "frontmatter has no reviewed-commit", and `review/consensus.md` was not left behind; same artifact with `reviewed-commit: abc1234` → exit 0, consensus written.
- Quorum by track: `addon-manifest-coverage` (track: deliberation, implementer claude-1) → triage=partial, missing=[claude-1]; `named-roster-presets` (standard, implementer `user` unsigned) → ready, missing=None; `tui-editor-composer` (fast, unsigned implementer) → ready; every standard-track partial's missing list contains only reviewers.
- `preflight --yes` on a fresh `parley init` deck: plain run raises `unknown-freshness`; `--yes` → zero gates, classification `freshness-confirmed`, exit 0, `meta/version.json` gains `protocolSha256`. A later plain run re-raises the gate because no packaged skill hash exists in that environment — that is F24's honesty ("never claims in sync without a packaged hash"), not a regression; `--yes` re-clears.

**PRIMARY — mutation battery (fix reverted in my clone, tests at HEAD, then restored; `git status` clean after).**
- A: `protocol.ValidateFinal` reduced to `FinalIsScaffold` → internal/driver exit 1 (`TestFinalDeclaringAnotherIdeaIsRejected`, `TestFinalWithNoSlugIsRejected` FAIL).
- B: only the manual binding reverted (`consensus.go:343` → `FinalIsScaffold(...)`) → at the time of the original signoff, **entire internal/consensus package passed (exit 0)** and internal/app had no manual-finalize test at all — the coverage gap behind my reservation. **Superseded:** once `TestFinalizeRejectsASubstantiveFinalWithTheWrongSlugOrStatus` landed (745ead5), this same mutation fails the suite — re-verified from scratch this session, see "Reservation discharged".
- C: `validateReviewRound`'s loop emptied → exit 1, both `TestManualReviewDraftRejectsAnArtifactWithNoReviewedCommit` and `...NoRefutationSection` FAIL.
- D: `reviewConsensusVoters` deliberation branch removed → exit 1, exactly `TestDeliberationReviewConsensusStillAwaitsTheImplementersSignoff` FAILS (standard/absent tests still pass, as they should).
- E: the `if opts.Yes { return confirmProtocolHashes(...) }` block deleted → exit 1, exactly `TestConfirmedFreshnessClearsTheGateOnAFreshDeck` FAILS.
- F: `buildFinalDraftPrompt` given a hardcoded six-section list (dropping "Known risks / de-risking") → exit 1, both `TestFinalDraftPromptDescribesWhatTheGateRequires` and `TestAFinalBuiltFromThePromptSatisfiesTheProductionGate` FAIL — the anti-drift claim is genuinely test-carried, in both directions (FINAL-from-prompt fed to `protocol.ValidateFinal`, and prompt ⊇ gate list).

**PRIMARY — the corrected consensus claims, reproduced to the digit.** Binaries built from worktrees at `a1926ae` (base) and `0bb9903` (reviewed) plus HEAD; `consensus status --review --json` over all 66 review consensuses of the HEAD deck: `0bb9903` vs base = **30 flips** (6 partial→ready, 7 partial→malformed, 17 ready→malformed = the claimed 24 →malformed); HEAD vs base = **5 flips, all partial→ready** (automation-outer-loop, close-integrity, loop-budgets, skills-cli-install-path, verification-honesty), 0 malformed. `addon-manifest-coverage` is in 0bb9903's six but not HEAD's five — it reports `partial` again, which is what the pre-fix binary said.

**SECONDARY — single-gate structure (read in /tmp/pd3-zcode1).** `RequiredFinalSections`/`MissingFinalSections` are defined only in `internal/protocol/finalsections.go:18,30`; every other non-test reference is `protocol.`-qualified (`internal/driver` has none — `finalScaffoldReason` at driver/consensus.go:177 delegates to `protocol.ValidateFinal`, as does manual `Finalize` at consensus.go:343). `ValidateReviewArtifact` lives in `internal/protocol/reviewartifact.go`, bound at consensus.go:220 (manual draft) and via the thin wrapper in runner/phase58.go:419 (driver). New tests read: the three track tests (roundgate_test.go:238,401,448), the two manual-review-draft rejections (:344,:379), preflight_hash_test.go, finalprompt_test.go. IMPLEMENTATION.md's amendment (git show 39dbc77) records the false "26 packages, green" history and the foreground-measured "27 packages, exit 0".

**RECALL** — my cycle-2 block conditions and sweep numbers (signoff2-zcode-1.md, verified there in /tmp clones at the time).

## Reservation discharged

The single reservation in this signoff — nothing pinned the manual finalize binding itself (mutation B passed the whole suite) — is discharged by commit `745ead5` ("fix-up cycle 3: pin the manual finalize binding; codex-1 and zcode-1's cycle-3 finding"), which landed the exact test my addendum anticipated. Verdict re-issued as `✅ ACCEPT`; it no longer depends on the addendum's conditional. Everything below was re-run from scratch this session, in a fresh clone; the shared tree was written only in this file.

**PRIMARY — the test is tracked at 745ead5, not just on disk.** In the shared repo: `git ls-tree 745ead5 -- internal/consensus/consensus_test.go` → `100644 blob c409a0e090a256f87b413c1a7abc0b0f602430d4`; `git show 745ead5:internal/consensus/consensus_test.go | grep -n TestFinalizeRejectsASubstantiveFinalWithTheWrongSlugOrStatus` → line 399; `git show 745ead5 --stat` → 1 file changed, 58 insertions.

**PRIMARY — nothing else moved.** `git diff 39dbc77 745ead5 --stat` → exactly `internal/consensus/consensus_test.go | 58 insertions`, nothing else. Scope note: the shared repo's HEAD had advanced one doc-only commit past the briefed b4cca54, to `c1818ac` ("generate canonical signoff blocks into review/consensus.md"); `git diff b4cca54 c1818ac --stat` touches only `review/consensus.md` (37 insertions), and `git diff 745ead5 HEAD -- internal/ cmd/` is empty — the Go tree at c1818ac is identical to 745ead5's, so all runs below at c1818ac answer for the briefed HEAD too.

**PRIMARY — green at HEAD.** Fresh `git clone --no-hardlinks` of the shared repo into /tmp/pd4-zcode1, `git rev-parse HEAD` = c1818ac7efb01d3be6e5ef450575b8c22d263cc7, `git status --porcelain` empty. Focused run `go test -count=1 -run 'TestFinalizeRejectsASubstantiveFinalWithTheWrongSlugOrStatus' -v ./internal/consensus/` → all three subtests PASS ("another idea's FINAL", "status is not final", "no idea slug at all"), exit 0 read from /tmp/pd4-zcode1-focus.exit. Full `go test -count=1 -timeout 15m ./...` → exit **0** read from /tmp/pd4-zcode1-gotest.exit; log: 26 `ok` packages + 1 `[no test files]`, zero FAIL lines — the "27 packages, exit 0" claim reproduces at the new HEAD as well.

**SECONDARY — how I read the code (in /tmp/pd4-zcode1).** The test (`internal/consensus/consensus_test.go:399-447`) drives the exported `Finalize` end-to-end, not a helper: the first call on a draft+signed-off idea takes the no-FILE branch (`internal/consensus/consensus.go:346-355`) and writes the scaffold; the test then overwrites FINAL.md with a fully substantive FINAL (bodies from `writtenFinal`, consensus_test.go:170) whose frontmatter either names another idea, carries `status: final-design-for-review`, or has no `idea:` line at all — defects a content-only scaffold check is blind to, per the test's own comment at :391-398. The second `Finalize` must hit the exists-branch gate at `internal/consensus/consensus.go:343` — `protocol.ValidateFinal(string(existing), idea.Slug)` — return an error naming the cause, and leave `00-prompt.md` at `status: consensus` (idea not closed, asserted at :444). That is the manual binding pinned through the public API, the exact shape I asked for.

**PRIMARY — mutation B re-applied in my own clone, red as required.** Single-token revert at `internal/consensus/consensus.go:343`: `protocol.ValidateFinal(string(existing), idea.Slug)` → `protocol.FinalIsScaffold(string(existing))` (content-only; `FinalIsScaffold(body string) string` per `internal/protocol/finalsections.go:47`) — the precise hole my reservation described. `git diff --stat` confirmed 1 file, 1 line changed. Focused run → `--- FAIL: TestFinalizeRejectsASubstantiveFinalWithTheWrongSlugOrStatus`, exit 1 from /tmp/pd4-zcode1-mutB.exit. Full `go test -count=1 -timeout 15m ./...` → exit **1** from /tmp/pd4-zcode1-mutB-full.exit: `FAIL parley-deck-cli/internal/consensus`, 25 `ok` packages (one fewer than clean HEAD). Reverted with `git checkout -- internal/consensus/consensus.go`; `git status --porcelain` empty and the `ValidateFinal(string(existing), idea.Slug)` binding back at :343.

— zcode-1, 2026-08-21 (verdict re-issued after 745ead5). Verified in /tmp clones and copies; shared tree untouched beyond this file; no browser; no secrets; no other reviewer's signoff3 file read before finalizing.
