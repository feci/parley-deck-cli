### Signoff: codex-1 — 2026-08-21
Status: ✅ ACCEPT
Notes: PRIMARY — Commands: `GOCACHE=/tmp/codex1-cycle3-addendum.xiW5cs/gocache GOMODCACHE=/tmp/codex1-cycle3-addendum.xiW5cs/gomodcache go test -count=1 -v -run '^TestFinalizeRejectsASubstantiveFinalWithTheWrongSlugOrStatus$' ./internal/consensus` at unmutated HEAD; the same focused command after the exact mutation; and `PATH=/tmp/codex1-cycle3-addendum.xiW5cs/probe-shim:$PATH GOCACHE=/tmp/codex1-cycle3-addendum.xiW5cs/gocache go test -count=1 ./...` in both copies. The outputs and structured exit codes recorded in `## Cycle-3 addendum — block condition re-checked` satisfy my sole remaining cycle-3 block. SECONDARY — I read the prior evidence at `parley-deck/ideas/protocol-and-skill-audit/review/signoff3-codex-1.md` and retained it below.
Prior counter-proposal (retained for audit continuity): Add one tracked test in `internal/consensus/consensus_test.go` with two subtests. Each should create a ready consensus, run the first finalize to create the scaffold, replace it with an otherwise complete seven-section FINAL, then set (a) `idea: other-idea` with `status: final`, and (b) the correct idea with `status: draft`; each must assert that `Finalize` returns an error and `00-prompt.md` remains `status: consensus`. Mutation-check it by changing only `consensus.Finalize` from `protocol.ValidateFinal(string(existing), idea.Slug)` back to `protocol.FinalIsScaffold(string(existing))` and require that test (and therefore `go test ./...`) to fail. Amend the mutation-coverage claim in `review/consensus.md`/`IMPLEMENTATION.md` only after that check is recorded.

## Evidence

- PRIMARY — Reviewed revision. Command: `git -C "/Volumes/My Shared Files/AI_WORKSPACE/parley-deck/parley-deck-cli" rev-parse HEAD`. Output: `39dbc778ba56ec20f531a9da249e0cb018e0a381`.

- PRIMARY — Go suite at pristine HEAD in `/tmp/codex1-signoff3.yfK4YA/pristine`. Command: `PATH=/tmp/codex1-signoff3.yfK4YA/probe-shim:$PATH GOCACHE=/tmp/codex1-signoff3.yfK4YA/gocache GOMODCACHE=/tmp/codex1-signoff3.yfK4YA/gomodcache go test ./...`. Relevant output: all 27 package rows were `ok`/`[no test files]`, including `ok parley-deck-cli/internal/app 49.218s`, `ok parley-deck-cli/internal/consensus 1.728s`, and `ok parley-deck-cli/internal/runner 10.258s`; the foreground process ended with exit code `0`. I read `exit_code: 0` from the completed execution result after foreground session `73974` ended, not from `$?`, an `echo`, or a background pipeline.

- PRIMARY — The first unadapted isolated run, `GOCACHE=/tmp/codex1-signoff3.yfK4YA/gocache GOMODCACHE=/tmp/codex1-signoff3.yfK4YA/gomodcache go test ./...`, reached the suite but exited `1` only at `TestDurableKillEndToEndRealProcess`: `process verification failed (no recorded boot id); not killed`. PRIMARY — `/usr/sbin/sysctl kern.boottime` exited `1` with `Operation not permitted`. SECONDARY — `docs/agent-cli-mechanics.md` identifies this exact Codex Seatbelt artifact, and `internal/procctl/procctl_darwin.go` obtains the boot id through `sysctl` and the command through `ps`. PRIMARY — The temporary PATH shim supplied stable values only for those two blocked OS identity probes; it did not edit the pristine HEAD worktree.

- PRIMARY — Skill-package suite in the isolated sibling-repository copy. Command: `(cd /tmp/codex1-signoff3.yfK4YA/parley-deck-skill && npm test)`. Relevant output: `tests 388`, `pass 388`, `fail 0`; `python 3.14: 54 tests OK across 7 files`; all six manifest checks reported `ok`; the foreground process exit code was `0`.

- PRIMARY — Functional checks for the four newly dispositioned MAJORs. Command in the isolated HEAD copy: `GOCACHE=/tmp/codex1-signoff3.yfK4YA/gocache GOMODCACHE=/tmp/codex1-signoff3.yfK4YA/gomodcache go test -count=1 -v ./internal/consensus -run '^(TestSignoff3ManualFinalizeRejectsWrongIdeaAndStatus|TestManualReviewDraftRejectsAnArtifactWithNoReviewedCommit|TestStandardReviewConsensusDoesNotAwaitTheImplementersSignoff|TestDeliberationReviewConsensusStillAwaitsTheImplementersSignoff|TestAbsentTrackReviewConsensusUsesTheStandardQuorum)$'`. Output: all named tests passed, including both scratch subtests `wrong_idea` and `wrong_status`; exit code `0`. The scratch test existed only at `/tmp/codex1-signoff3.yfK4YA/repo/internal/consensus/signoff3_audit_test.go` and exercised the production `consensus.Finalize` function.

- PRIMARY — Explicit fast-track check. Command: `GOCACHE=/tmp/codex1-signoff3.yfK4YA/gocache GOMODCACHE=/tmp/codex1-signoff3.yfK4YA/gomodcache go test -count=1 -v ./internal/consensus -run '^(TestSignoff3FastReviewConsensusDoesNotAwaitImplementer|TestStandardReviewConsensusDoesNotAwaitTheImplementersSignoff|TestDeliberationReviewConsensusStillAwaitsTheImplementersSignoff)$'`. Output: all three tests passed; exit code `0`.

- PRIMARY — Fresh-deck CLI path. Commands: `/tmp/codex1-signoff3.yfK4YA/parley-head init --dir /tmp/codex1-freshdeck.WJhZud` followed by `/tmp/codex1-signoff3.yfK4YA/parley-head preflight --dir /tmp/codex1-freshdeck.WJhZud --no-ping --yes --json`. Output: both exited `0`; freshness was `"classification": "freshness-confirmed"`, the summary said the live deck hash was recorded with `--yes`, and `"gates": null`.

- PRIMARY — Focused freshness and prompt/gate contract tests. Command: `GOCACHE=/tmp/codex1-signoff3.yfK4YA/gocache GOMODCACHE=/tmp/codex1-signoff3.yfK4YA/gomodcache go test -count=1 -v ./internal/app -run '^(TestConfirmedFreshnessClearsTheGateOnAFreshDeck|TestInitRecordsTheLiveProtocolHashAndNoPackagedHash|TestAFinalBuiltFromThePromptSatisfiesTheProductionGate)$'`. Output: all three passed; exit code `0`.

- PRIMARY — Positive mutation check for the manual review-draft fix. In `/tmp/codex1-signoff3.yfK4YA/mutation-review`, `git diff -- internal/consensus/consensus.go` showed only removal of the `if opts.Review { validateReviewRound(...) }` call. Command: `GOCACHE=/tmp/codex1-signoff3.yfK4YA/gocache GOMODCACHE=/tmp/codex1-signoff3.yfK4YA/gomodcache go test -count=1 -v ./internal/consensus -run '^TestManualReviewDraftRejectsAnArtifactWithNoReviewedCommit$'`. Output: `manual review draft accepted a review with no reviewed-commit`, `FAIL`; exit code `1`.

- PRIMARY — Blocking mutation result for the manual finalizer. In `/tmp/codex1-signoff3.yfK4YA/mutation-final`, `git diff -- internal/consensus/consensus.go` showed exactly one source-line reversion: `protocol.ValidateFinal(string(existing), idea.Slug)` became `protocol.FinalIsScaffold(string(existing))`; tests remained at HEAD. Command: `PATH=/tmp/codex1-signoff3.yfK4YA/probe-shim:$PATH GOCACHE=/tmp/codex1-signoff3.yfK4YA/gocache GOMODCACHE=/tmp/codex1-signoff3.yfK4YA/gomodcache go test -count=1 ./...`. Output: every package passed, including `ok parley-deck-cli/internal/consensus 1.104s` and `ok parley-deck-cli/internal/app 47.721s`; foreground exit code `0`. This directly falsifies the claim that this fix has a committed test that fails when the fix alone is reverted.

- PRIMARY — Single FINAL authority. Command: `rg -n 'requiredFinalSections|missingFinalSections' internal/driver` at HEAD. Output: no matches; exit code `1` (ripgrep's no-match status). Command: `rg -n 'RequiredFinalSections|MissingFinalSections' internal/protocol internal/driver internal/app`. Output placed both definitions only in `internal/protocol/finalsections.go`; driver/app references use `protocol.RequiredFinalSections`. Command: `rg -n 'TestAFinalBuiltFromThePromptSatisfiesTheProductionGate|protocol.ValidateFinal' internal/app/finalprompt_test.go internal/driver/consensus.go internal/consensus/consensus.go`. Output showed the named contract test and both production entry points calling `protocol.ValidateFinal`.

- PRIMARY — Corrected review-consensus triage claim. I built binaries from `a1926ae2`, `0bb9903`, and HEAD, then for each path returned by `find parley-deck/ideas -mindepth 3 -maxdepth 3 -path '*/review/consensus.md' -type f | sort` ran `<binary> consensus status --review --json --dir /tmp/codex1-signoff3.yfK4YA/repo <slug>` and counted `.triage` with `jq`. Output: `total=66`, `reviewed_flips=30`, `reviewed_partial_to_ready=6`, `reviewed_to_malformed=24`, `head_flips=5`, `head_partial_to_ready=5`, `head_other=0`. The five HEAD flips printed as `automation-outer-loop`, `close-integrity`, `loop-budgets`, `skills-cli-install-path`, and `verification-honesty`, each `partial->ready`.

- PRIMARY — `addon-manifest-coverage` at HEAD. Command: `/tmp/codex1-signoff3.yfK4YA/parley-head consensus status --review --json --dir /tmp/codex1-signoff3.yfK4YA/repo addon-manifest-coverage`. Output: `"triage": "partial"`, participants included `claude-1`, and `"missing": ["claude-1"]`; exit code `0`.

- SECONDARY — I read the four requested commits and their changed files: `7112e03` updates `internal/app/pipeline_cmd_test.go`; `4c43200` contains the four functional fixes across `internal/app/preflight.go`, `internal/consensus/consensus.go`, `internal/protocol/finalsections.go`, `internal/protocol/reviewartifact.go`, and related files; `1f3d971` adds `internal/app/finalprompt_test.go::TestAFinalBuiltFromThePromptSatisfiesTheProductionGate`; `39dbc77` amends `parley-deck/ideas/protocol-and-skill-audit/review/consensus.md` and `IMPLEMENTATION.md`. SECONDARY — The two corrected statements in `parley-deck/ideas/protocol-and-skill-audit/review/consensus.md` match the PRIMARY measurements above, except its broader statement that all four fixes were mutation-tested is contradicted by the manual-finalizer mutation result.

## Cycle-3 addendum — block condition re-checked

- PRIMARY — HEAD and trackedness. Commands: `git rev-parse HEAD`; `git log -1 --format='%H%n%ad%n%s' --date=iso-strict 745ead5b3cae8436aac3de1bf2b8dd0f046bdb5b -- internal/consensus/consensus_test.go`; `git ls-tree -r --name-only 745ead5b3cae8436aac3de1bf2b8dd0f046bdb5b -- internal/consensus/consensus_test.go`; and `git show --stat --oneline --decorate=short 745ead5b3cae8436aac3de1bf2b8dd0f046bdb5b -- internal/consensus/consensus_test.go`. Output identified HEAD as `745ead5b3cae8436aac3de1bf2b8dd0f046bdb5b`, listed `internal/consensus/consensus_test.go` in that commit's tree, and showed commit `745ead5` adding 58 lines to that tracked file.

- SECONDARY — I read the committed blob at `745ead5b3cae8436aac3de1bf2b8dd0f046bdb5b:internal/consensus/consensus_test.go`. `TestFinalizeRejectsASubstantiveFinalWithTheWrongSlugOrStatus` contains the three subtests `another idea's FINAL`, `status is not final`, and `no idea slug at all`; each calls `Finalize`, requires a non-nil error, and calls `assertPromptStatus(..., "consensus")`.

- PRIMARY — Focused unmutated test. Command in `/tmp/codex1-cycle3-addendum.xiW5cs/head`: `GOCACHE=/tmp/codex1-cycle3-addendum.xiW5cs/gocache GOMODCACHE=/tmp/codex1-cycle3-addendum.xiW5cs/gomodcache go test -count=1 -v -run '^TestFinalizeRejectsASubstantiveFinalWithTheWrongSlugOrStatus$' ./internal/consensus`. Output showed `PASS` for the parent and all three subtests; the completed command result reported `exit_code: 0`.

- PRIMARY — The unmutated test tree was the exact requested revision and remained clean. Command in `/tmp/codex1-cycle3-addendum.xiW5cs/head`: `git rev-parse HEAD; git status --porcelain; git diff --exit-code --; git ls-files --error-unmatch internal/consensus/consensus_test.go`. Output was the requested full SHA plus `internal/consensus/consensus_test.go`, with no porcelain-status or diff output; the completed command result reported `exit_code: 0`.

- PRIMARY — Exact mutation. Commands: `git clone --quiet --no-hardlinks /tmp/codex1-cycle3-addendum.xiW5cs/head /tmp/codex1-cycle3-addendum.xiW5cs/mutation`; `git -C /tmp/codex1-cycle3-addendum.xiW5cs/mutation checkout --quiet --detach 745ead5b3cae8436aac3de1bf2b8dd0f046bdb5b`; then `git status --short; git diff --stat; git diff -- internal/consensus/consensus.go` in the mutation copy. Output showed exactly one modified file, `internal/consensus/consensus.go`, with one insertion and one deletion: `protocol.ValidateFinal(string(existing), idea.Slug)` became `protocol.FinalIsScaffold(string(existing))`.

- PRIMARY — Focused mutation result. Command in `/tmp/codex1-cycle3-addendum.xiW5cs/mutation`: `GOCACHE=/tmp/codex1-cycle3-addendum.xiW5cs/gocache go test -count=1 -v -run '^TestFinalizeRejectsASubstantiveFinalWithTheWrongSlugOrStatus$' ./internal/consensus`. Output showed all three subtests failing at `consensus_test.go:440` with `finalize closed the idea around a FINAL that is not this idea's final artifact`; the completed command result reported `exit_code: 1`.

- PRIMARY — Full mutated suite. Command in `/tmp/codex1-cycle3-addendum.xiW5cs/mutation`: `PATH=/tmp/codex1-cycle3-addendum.xiW5cs/probe-shim:$PATH GOCACHE=/tmp/codex1-cycle3-addendum.xiW5cs/gocache go test -count=1 ./...`. Output showed `internal/consensus` failing only through `TestFinalizeRejectsASubstantiveFinalWithTheWrongSlugOrStatus`, with all three named subtests failing; the completed process result reported `exit_code: 1`, so `go test ./...` was non-zero under the exact mutation.

- PRIMARY — Full unmutated suite. Command in `/tmp/codex1-cycle3-addendum.xiW5cs/head`: `PATH=/tmp/codex1-cycle3-addendum.xiW5cs/probe-shim:$PATH GOCACHE=/tmp/codex1-cycle3-addendum.xiW5cs/gocache go test -count=1 ./...`. Output showed every package row `ok` or `[no test files]`, including `ok parley-deck-cli/internal/consensus 0.705s` and `ok parley-deck-cli/internal/runner 10.566s`. I read exit code `0` from the structured `exit_code: 0` field in the completed process result after polling execution session `26993`; I did not infer it from `$?`, an `echo`, or the displayed package rows.

- PRIMARY — Sandbox qualification for both full-suite commands. Command: `sed -n '1,80p' /tmp/codex1-cycle3-addendum.xiW5cs/probe-shim/sysctl; sed -n '1,80p' /tmp/codex1-cycle3-addendum.xiW5cs/probe-shim/ps`. Output showed that the PATH shim supplies stable values only for `sysctl -n kern.boottime` and `ps -p <pid> -o command=` and delegates every other invocation to `/usr/sbin/sysctl` or `/bin/ps`; this avoids the known macOS sandbox denial without editing either test tree.
