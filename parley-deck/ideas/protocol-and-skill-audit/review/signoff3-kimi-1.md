### Signoff: kimi-1 — 2026-08-21
Status: ✅ ACCEPT
Notes: My cycle-2 block is satisfied and flips. That block was exactly one condition: the fix-up had changed `isFinalized` without moving the pipeline fixtures that encoded the old frontmatter-only finality, so `go test ./...` could not finish at HEAD. At 39dbc77 the suite completes with exit code 0 (26 packages ok + 1 no-test-files, `internal/app` in 81s), the four previously-hanging pipeline tests pass individually in 0.2s, and the fixture now builds its FINAL from `protocol.RequiredFinalSections` — the shape my counter-proposal asked for. I then re-verified the rest of the amended consensus instead of taking its word: all four newly-dispositioned MAJORs hold end-to-end through the real CLI (manual `finalize` rejects a wrong `status:` and a wrong `idea:` slug and names each cause; `draft --review` rejects an artifact with no `reviewed-commit` and leaves no half-written consensus; the deliberation review quorum awaits the implementer while the standard/absent-track quorum does not; `preflight --yes` turns a freshly `init`ed deck from exit 3 / `unknown-freshness` gate to exit 0 / no pending gates). Three of the fixes I mutation-checked myself — revert the source, leave the test at HEAD, watch it fail — and each failed as advertised. The two corrected measurement claims in the consensus are exact under my own three-binary sweep: reviewed 0bb9903 flips 30 review consensuses vs base a1926ae (24 of them to `malformed`); HEAD flips 5, all `partial → ready`; `addon-manifest-coverage` (track: deliberation) is `partial` again, matching the pre-fix binary. `internal/driver` owns no second section list; `finalScaffoldReason` is a thin wrapper over `protocol.ValidateFinal`, and the prompt-to-gate contract test exists and passes. One sentence I refused to sign in cycle 2 — "Every reviewer built from source and ran both suites" — was not true of my round-01 record then; it is true of this signoff, because this session I built three binaries from source and ran both suites (go: exit 0; npm: 388 pass / 0 fail). Two small observations, neither blocking, recorded in the evidence: the consensus's "66 review consensuses" counts files on disk (65 are resolvable ideas; `launch-orphan-hardening` has a review/consensus.md but no 00-prompt.md and errors "idea not found" identically under all three binaries), and on a deck whose installed skill exposes no packaged protocol body the `unknown-freshness` gate re-raises on the next plain `preflight` after `--yes` clears it — consistent with "never claims in-sync without a packaged hash", but a reader should know `--yes` clears the gate per confirmation, not permanently.

## Evidence

All PRIMARY work ran in isolated copies (`git clone` of the shared tree to `/tmp/kimi1-signoff3/repo` at HEAD 39dbc77, verified with `git rev-parse HEAD`; `rsync -a` of the sibling `parley-deck-skill` to `/tmp/kimi1-signoff3/skill`; `git archive` extractions of a1926ae and 0bb9903 for the pre-fix binaries; a scratch deck at `/tmp/kimi1-signoff3/freshdeck`). The shared tree was not modified.

**1. `go test ./...` completes, exit 0 — PRIMARY.**
Command: `cd /tmp/kimi1-signoff3/repo && go test ./... > gotest.log 2>&1; echo $? > gotest.exit`.
How I read the exit code: `$?` captured in the same shell immediately after the `go test` process, written to a file, then read back — `gotest.exit` contains `0`. Not a background job's last-command status, not a pipeline's tail: the test process's own status. Log tail: 26 × `ok` (incl. `ok parley-deck-cli/internal/app 80.981s`) + 1 × `[no test files]`, zero `FAIL`/`panic` lines (`grep -c "^ok"` → 26; `grep -E "FAIL|panic"` → empty). This is my cycle-2 block condition, resolved.

**2. `npm test` in the skill copy — PRIMARY.**
Command: `cd /tmp/kimi1-signoff3/skill && npm test > npmtest.log 2>&1; echo $? > npmtest.exit`. Exit file: `0`. Log: `ℹ tests 388 / ℹ pass 388 / ℹ fail 0`; no `not ok` lines (the `✖`-word hits are test *names* about reporting failing fixtures, each marked `✔`).

**3. The four MAJORs, verified through the real CLI in the scratch deck — PRIMARY.**
Binary: `/tmp/kimi1-signoff3/parley` (`go build ./cmd/parley` at HEAD → `parley 1.45.0`). Fixtures mirror the unit-test shapes (00-prompt.md with participants/track, round files, IMPLEMENTATION.md naming `implementer: impl`).

- **Manual `consensus finalize` gate.** Idea `t-final`, round-01 files for both participants, drafted, both signed. (a) FINAL.md with `status: accepted` + full sections → `consensus finalize` exit 1: `cannot close this idea: frontmatter status="accepted", want final`; idea stays `status: consensus`. (b) FINAL.md with `status: final` but `idea: some-other-idea` → exit 1: `frontmatter idea="some-other-idea" but it closes idea "t-final"`; idea still open. (c) correct FINAL → exit 0, 00-prompt.md becomes `status: final`.
- **`consensus draft --review` gate.** Idea `t-review` with a review artifact whose `reviewed-commit:` line I deleted → exit 1: `cannot be drafted; 1 review artifact(s) do not meet the review contract: ... frontmatter has no reviewed-commit: a review must name the tree it reviewed`; `review/consensus.md` confirmed absent afterward. Restored artifact → draft exit 0.
- **Quorum split.** Deliberation idea `t-delib` (`track: deliberation`, participants [impl, rev-a]): after rev-a's accept → `Review consensus: partial`, `Missing signoffs: impl`; after impl signs → `ready`. Absent-track `t-review` (same participants, no `track:` key): `ready` after the reviewer alone signs. Standard track takes the same non-deliberation branch by construction in `reviewConsensusVoters` (internal/consensus/consensus.go:136-144, SECONDARY) and is pinned by `TestStandardReviewConsensusDoesNotAwaitTheImplementersSignoff`, which passed in the suite run.
- **`preflight --yes` on a fresh deck.** `parley init -dir freshdeck`, then (each command's own `$?` captured directly, no pipes): plain `preflight -no-ping` → exit 3, gate `[unknown-freshness]`; `preflight -no-ping --yes` → exit 0, `Ready: no pending gates.`, classification `freshness-confirmed`, summary states exactly what was compared ("no packaged protocol available to compare against; recorded the live deck protocol hash as confirmed"); `meta/version.json` gained the live `protocolSha256` and correctly did **not** gain an invented `packagedProtocolSha256`. Observation: a subsequent plain preflight re-raises `unknown-freshness` (exit 3) because the installed skill 2.9.0 exposes no packaged body, so the OR-condition in `classifyAndSyncFreshness` (internal/app/preflight.go:427, SECONDARY) fires again. The blocked trap — a confirmation no flag could give — is gone; the per-run re-raise is the honest behavior for a deck with no packaged reference, and the consensus's own wording ("never reports 'in sync' without a packaged hash") covers it. Not a block; noted for the record.

**4. Revert-checks (fix reverted, tests at HEAD) — PRIMARY, three of them, each restored afterward with `git checkout` (final `git status --porcelain` clean).**
- Removed the deliberation branch from `reviewConsensusVoters` → `go test ./internal/consensus/ -run TestDeliberationReviewConsensusStillAwaitsTheImplementersSignoff` → FAIL (`deliberation review consensus does not await the implementer: missing=[] triage=ready`), exit 1.
- Replaced the `validateReviewRound(...)` call in `Draft` with a no-op → `-run 'TestManualReviewDraftRejectsAnArtifactWithNoReviewedCommit|TestManualReviewDraftRejectsAnArtifactWithNoRefutationSection'` → both FAIL, exit 1.
- Disabled the `opts.Yes` branch in the unknown-freshness path → `go test ./internal/app/ -run TestConfirmedFreshnessClearsTheGateOnAFreshDeck` → FAIL (`--yes left a gate standing`), exit 1.
For the finalize gate (MAJOR 4) the binding evidence is the CLI rejection in §3(a)/(b) plus `protocol.ValidateFinal`'s slug/status checks (internal/protocol/finalsections.go:92-106, SECONDARY); I did not run a fourth mutation.

**5. One authority for the FINAL gate — PRIMARY (grep) + SECONDARY.**
`grep -rn "requiredFinalSections|missingFinalSections" internal/` → no matches anywhere (the only list is exported `protocol.RequiredFinalSections` / `protocol.MissingFinalSections`). internal/driver/consensus.go:167-181: `finalScaffoldReason` = file read + 250-byte floor + `protocol.ValidateFinal(...)`; the driver-local list and checker are deleted.

**6. The two corrected consensus claims — PRIMARY sweep + PRIMARY test run.**
Built `parley-base` (a1926ae) and `parley-reviewed` (0bb9903) from `git archive` extractions; swept all 87 idea dirs with `<bin> consensus status -dir <HEAD tree> -review -json <slug>`, triage extracted with `jq`.
- base→reviewed: **30 flips** — 17 `ready → malformed`, 7 `partial → malformed` (= **24 → malformed**), 6 `partial → ready`. Matches "30 flips vs base (24 → malformed)" exactly.
- base→HEAD: **5 flips, all `partial → ready`** (automation-outer-loop, close-integrity, loop-budgets, skills-cli-install-path, verification-honesty). Matches "HEAD has 5, all partial → ready" exactly.
- Denominator note: 66 `review/consensus.md` files exist on disk; 65 ideas are resolvable — `launch-orphan-hardening` has the file but no 00-prompt.md, so all three binaries report "idea not found" identically (no flip either way). "All 66 review consensuses" is accurate as a file count; the flip counts above are over the 65 sweepable ones.
- Anti-drift claim: `TestAFinalBuiltFromThePromptSatisfiesTheProductionGate` (internal/app/finalprompt_test.go:41-77, SECONDARY for what it does) builds a FINAL from nothing but the prompt's own `## ` headings plus frontmatter and hands it to `protocol.ValidateFinal`, and checks the reverse direction too. PRIMARY: `go test ./internal/app/ -run 'TestAFinalBuiltFromThePromptSatisfiesTheProductionGate|TestFinalDraftPromptDescribesWhatTheGateRequires' -v` → both PASS, exit 0.

**7. `addon-manifest-coverage` — PRIMARY.** Same sweep row: `base=partial reviewed=ready head=partial`. It reports `partial` again at HEAD, exactly what the pre-fix binary said; the reviewed commit's `ready` was the quorum-weakening regression @codex-1 named.

**8. My cycle-2 block's ground, specifically — PRIMARY.** `go test ./internal/app/ -run 'TestPipelineAutoWalksToDoneUnderAutoLeft|TestPipelineAutoStopsAtActionBlockNeedsHumanGate|TestPipelineAutoPausesAtSupervisedGate|TestActionBlockCompleteNeedsSucceededEffectNotJustPlan' -v -timeout 120s` → all four PASS in 0.198s (they hung to the 10-minute timeout at a27a3b6). Fixture: pipeline_cmd_test.go:199-205 builds the stub FINAL from `protocol.RequiredFinalSections` with three content lines per section (SECONDARY).
