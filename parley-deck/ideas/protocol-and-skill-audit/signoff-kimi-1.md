### Signoff: kimi-1 — 2026-08-21
Status: ✅ ACCEPT
Notes: I read `consensus.md` in full, re-read my own `round-01/kimi-1.md` and `round-02/kimi-1.md`, re-derived all three verifiers' round-02 tallies myself from the verdict headings this session, and checked §5 against `IMPLEMENTATION.md`, `review/consensus.md`, and the code at HEAD. The document's architecture is sound and my contribution is accurately represented where it matters most: §2's summaries of my three confirmed findings (F1 `doctor` not byte-verifying the managed core, F2 `sync-project --yes` deleting `protocolRole`, F3 the "fourteen runtimes" README) match what I filed, §1's structural account of the verification split is real (codex-1 excluded from its own 24 findings; the two verifiers who assessed them confirmed 44 of 48), §4's participation account matches the files, and §5's three-surface split matches what shipped (28 applied, 5 deferred with recorded reasons, review unanimous at `745ead5`, VERSION still 1.45.0). I block because the document's verification ledger — the thing this gate exists to certify — is false in checkable ways, and two of the errors had consequences. (1) My §1 table row reads 37 CONFIRMED / 8 PARTIAL / 2 REFUTED / 1 UNREPRO; I filed 36 / 6 / 0 / 0, the row sums to 48 against the 42 assessed, and it attributes to me two REFUTED verdicts and one UNREPRODUCIBLE I never issued — in an audit whose one unforgivable sin is reporting unrun checks. (2) §1's "every REFUTED verdict but one came from @codex-1" and §3's "@kimi-1/F5 — the only refutation not authored by @codex-1" are both false: all nine REFUTED verdicts in the corpus are codex-1's, including the one on my F5. (3) §3's "PARTIAL-only, no confirmation" list misrecords three findings @zcode-1 actually CONFIRMED (codex-1/F15, codex-1/F23, and my kimi-1/F4), and that error dropped codex-1/F23 and kimi-1/F4 from the fix list with no disposition anywhere — not fixed, not deferred with a reason. (4) Two confirmed findings have no disposition either: claude-1/F2 (a MAJOR I verified in round-02; both generators and the drift-guard anchor are unchanged at HEAD, so following the bootstrap instruction still breaks the suite) and claude-1/F3 (`masked-by-env` still never reaches STATUS) — the review phase's own rule was "silence is not a disposition", and the design ledger violates it for four findings. (5) Smaller ledger errors: §2's enumerated list sums to 34 under a "33" header; §3 still carries the zcode-1/F14 "number struck" doubt the drafter himself retracted in `IMPLEMENTATION.md`; codex-1/F20 is fixed in code but absent from every Fixed table; codex-1/F15 was applied though the consensus excluded it and kimi-1/F5 deferred though the consensus lists it as REFUTED; §5's "no complete release ships until the command defects are fixed" is never reconciled with codex-1/F6, F8, F14 being deferred command defects; and codex-1's round-02 addendum (standalone `preflight` ignoring the canonical roster) has no adjudication. The substance of the audit and the shipped work are not in doubt — the closing ledger of a verification-integrity audit cannot itself fail the evidence standard, so amend and re-sign.
Counter-proposal: Amend `consensus.md` and re-collect signoffs: (1) §1 table — replace the kimi-1 row with 42 assessed / 36 CONFIRMED / 6 PARTIAL / 0 REFUTED / 0 UNREPRO per `round-02/kimi-1.md:316-323`, correct the "37" prose, and restate the leniency figure (pooled 66/74 ≈ 89%; on codex-1's 24 findings 44/48 ≈ 92%, which stands); (2) §1/§3 attribution — state that all nine REFUTED verdicts in the 47-finding corpus are codex-1's (zcode-1 ×7; kimi-1/F5 at `round-02/codex-1.md:272`; claude-1/F1), that kimi-1/F5 was contested by zcode-1's CONFIRMED (`round-02/zcode-1.md:417`), and that the only non-codex-1 refutation in the wider record is claude-1's of hermes-1/Q3; (3) §3 — strike codex-1/F15, codex-1/F23 and kimi-1/F4 from "PARTIAL-only, no confirmation" (CONFIRMED by zcode-1 at `round-02/zcode-1.md:234,329,404`, refuted by none), correct claude-1/F1 to PARTIAL from two verifiers, state the confirmed-set inclusion rule explicitly, and reconcile the count (the §2 enumeration sums to 34; count hermes-1/Q2 in or out, consistently); (4) add an explicit disposition for each of the four findings that currently have none — claude-1/F2 (still live at HEAD: `internal/app/roster_render.go:73` compact four-column vs `internal/protocol/drift_test.go:28` padded three-column anchor; fix or defer with a reason), claude-1/F3 (open at HEAD), codex-1/F23, kimi-1/F4; (5) ledger repairs — list codex-1/F20 as applied (`internal/consensus/consensus.go:462-491`; `IMPLEMENTATION.md:132,162`), record codex-1/F15 as applied-though-unlisted and kimi-1/F5 as deferred-though-contested, and correct the zcode-1/F14 sentence per the drafter's own retraction ("Its number stands and my objection was too narrow"); (6) §5 — restate it as the recorded outcome (28 applied, five deferred: codex-1/F6, F8, F14; kimi-1/F1, F5; review unanimous at `745ead5`, outstanding agreed fixes 0) and state whether the owner's no-complete-release ruling still binds given three deferred command defects; (7) adjudicate codex-1's round-02 addendum (`round-02/codex-1.md:357`, standalone `preflight` ignoring the canonical roster) or mark it explicitly unadjudicated and outside the 47-finding corpus.

## Evidence

- PRIMARY — verifier tallies, run this session: `cd parley-deck/ideas/protocol-and-skill-audit/round-02 && for f in kimi-1 codex-1 zcode-1; do grep -oE '— (CONFIRMED|PARTIAL|REFUTED|UNREPRO[A-Z]*)$' $f.md | sort | uniq -c; done` → kimi-1: **36 CONFIRMED / 6 PARTIAL** (0 REFUTED, 0 UNREPRO — no such lines exist); codex-1: 6 / 8 / 9; zcode-1: 30 / 2 / 0. The consensus's codex-1 and zcode-1 rows match; its kimi-1 row (37/8/2/1) matches nothing in my file and sums to 48 ≠ 42.
- PRIMARY — authorship of the kimi-1/F5 refutation: `grep -n 'kimi-1/F5' round-02/codex-1.md round-02/zcode-1.md` → `codex-1.md:272: ### kimi-1/F5 — REFUTED`; `zcode-1.md:417: ### kimi-1/F5 — … — CONFIRMED`. So §3's "the only refutation not authored by @codex-1" is false (codex-1 authored it), and §1's "every finding @codex-1 refuted belongs to @zcode-1 or @claude-1" is false (one belongs to me).
- PRIMARY — the three misrecorded "PARTIAL-only" verdicts: heading grep over `round-02/zcode-1.md` this session shows `:234` codex-1/F15 — CONFIRMED, `:329` codex-1/F23 — CONFIRMED, `:404` kimi-1/F4 — CONFIRMED. §3's list is wrong for all three; the true PARTIAL-only set is codex-1/F12 and zcode-1/F4.
- PRIMARY — claude-1/F2 unfixed at HEAD `09e9ab3`: `grep -n "Agent ID" internal/app/roster_render.go` → `:73` still writes the compact four-column header; `grep -n "anchor\|Agent ID" internal/protocol/drift_test.go` → `:28` still anchors the padded three-column header; `grep -n "Agent ID" internal/protocol/defaults/COOPERATION.md` → `:139` unchanged; `grep -n "roster render" parley-deck/COOPERATION.md` → `:59` still instructs the bootstrap to regenerate §2 with `roster render`. (The end-to-end break in a clone at HEAD is @zcode-1's PRIMARY — `signoff-zcode-1.md:8`; I did not re-run it.)
- PRIMARY — claude-1/F3 unfixed at HEAD: `grep -rn "masked-by-env" internal/ cmd/ | grep -v _test` → only `internal/app/roster_set.go:83` (comment) and `:88` (stderr advice string); no `addStatus("masked-by-env")` exists. `CHANGELOG.md:358`'s "`masked-by-env` has an emitter" describes exactly that advice string — the half-fix the finding already recorded — not a STATUS-column emission. (The SKILL.md half, `:284` still listing it in the closed vocabulary, is SECONDARY via `signoff-zcode-1.md:9`; the sibling repo is outside my read scope this session.)
- PRIMARY — release state: `cat VERSION` → `1.45.0`; no post-audit release exists, so §5's owner ruling was not violated, but the document never reconciles it with the three deferred command defects.
- PRIMARY — git relationship: `git log --oneline -8` and `git merge-base --is-ancestor 745ead5b3cae8436aac3de1bf2b8dd0f046bdb5b HEAD` → the reviewed commit `745ead5` is an ancestor of HEAD `09e9ab3`; the shipped tree contains the reviewed work.
- PRIMARY — the design consensus's own Signoffs section: `grep -n "Signoff:" parley-deck/ideas/protocol-and-skill-audit/consensus.md` → no matches; the section is empty as my brief stated.
- SECONDARY — `parley-deck/ideas/protocol-and-skill-audit/round-02/kimi-1.md:316-323` — my filed tally (36/6/0/0; "REFUTED: 0 outright; UNREPRODUCIBLE: 0") and the per-finding verdicts behind the grep counts; `:281-286` independently confirmed zcode-1/F14's 21-of-78 figure with the corrected breakdown (18× round-01 + open + kickoff + empty), which is why §3's "27% figure is in doubt" was never right and is now retracted by the drafter.
- SECONDARY — `parley-deck/ideas/protocol-and-skill-audit/round-02/kimi-1.md:295-300` and `round-02/zcode-1.md:428` — claude-1/F1 received PARTIAL from TWO verifiers; §3 says one.
- SECONDARY — `parley-deck/ideas/protocol-and-skill-audit/consensus.md` — §2's enumerated confirmed list sums to 34 (21+7+3+2+1) under a "33 findings" header; §2's three kimi-1 summaries accurately represent `round-01/kimi-1.md` F1/F2/F3; §4's participation account matches `round-01/hermes-1.md` (budget line "4 tool calls used", line 44) and `round-01/opencode-1.md` (167-byte skeleton, no findings); §3's "codex-1 refuted seven of zcode-1's (F2, F3, F5, F9, F10, F12, F13)" matches codex-1's file exactly (my PARTIAL on zcode-1/F10, not a confirmation, is a small imprecision in §3's "kimi-1 confirmed them" clause).
- SECONDARY — `parley-deck/ideas/protocol-and-skill-audit/IMPLEMENTATION.md` — 28 applied rows and five deferrals with recorded reasons (codex-1/F6, F8, F14; kimi-1/F1, F5); the "Correction against myself on zcode-1/F14" retracts the doubt §3 still carries; codex-1/F20 appears only at lines 132 and 162 (fixture mistake + reversion check), never in a Fixed table; the `:24` "claude-1/F2 (probe path)" row matches no filed claude-1 finding — `round-01/claude-1.md:39-66` is the roster-render/drift-guard finding; codex-1/F23 and kimi-1/F4 appear nowhere in it.
- SECONDARY — `parley-deck/ideas/protocol-and-skill-audit/review/consensus.md` — triage `ready`, `outstanding_agreed_fixes: 0`, unanimous ✅ at `745ead5`; the deferred-follow-ups list carries the same five deferrals with no reviewer challenge (codex-1 re-upheld all five in cycle 2); lines 86-87 set the standard this design ledger fails: "silence is not a disposition".
- SECONDARY — `parley-deck/ideas/protocol-and-skill-audit/round-02/codex-1.md:357` — the standalone-`preflight`/canonical-roster addendum; a full read of `consensus.md` confirms no disposition of it.
- RECALL — my own authorship of `round-01/kimi-1.md` and `round-02/kimi-1.md` (the verdicts the tally counts are mine); I make no claim about §1's "told to default to REFUTED" wording — no round-02 prompt is stored in this idea directory, so I cannot verify or refute it.

## Re-verification after the ledger correction

Status: 🟡 ACCEPT-WITH-RESERVATIONS
Notes: Every substantive objection from my block is resolved and re-verified by me this session, not taken on trust: §1's kimi-1 row now reads my filed 36/6/0/0, all nine REFUTED verdicts are correctly attributed to @codex-1, the leniency figure is the true pooled 66/74 ≈ 89%, §3's PARTIAL-only list is the correct two (codex-1/F12, zcode-1/F4), the three mis-recorded findings are reclassified and every finding that had no disposition now has one I checked, zcode-1/F14's doubt is retracted, kimi-1/F5 is dismissed-not-deferred, codex-1/F20 is recorded, §5 has its fourth surface, and the release-rule reconciliation matches review/signoff2-codex-1.md. I reserve rather than accept cleanly because the corrected ledger still carries three checkable defects — none fabricates a verdict, none drops a finding, none changes a disposition, but each is exactly the class of unchecked entry my block was about: (1) §3 still says claude-1/F1 received "PARTIAL from one verifier" — TWO verifiers returned PARTIAL (round-02/kimi-1.md:295, round-02/zcode-1.md:428), an explicit sub-demand of my counter-proposal left byte-identical between draft a1926ae and HEAD 2a5ea3a; (2) hermes-1/Q2 is still counted neither in nor out consistently: §2's CORRECTED note derives "36" from addends summing to 37 (23+7+4+2+Q2), the §2 bullets still read (21)/(3) and omit codex-1/F15, codex-1/F23 and kimi-1/F4, and §5's four surfaces enumerate 37 slots under "The 36 confirmed findings" (@opencode-1's re-verification found the same independently); (3) the kimi-1/F4 disposition records commit 815c93a, which contains no recommendedActions change — the actual fix and its test are uncommitted working-tree state in parley-deck-skill (M lib/installer.js, ?? test/source-role-advice.test.js), so the ledger certifies a commit that does not contain the fix it lists, and npm test's 391 passes run on uncommitted code. Not a BLOCK because my block's stated standard was errors with consequences — fabricated verdicts, dropped findings — and none of those remain; both suites are green and I mutation-checked the F4 fix myself in my own copy.

### My counter-proposal vs the correction

1. §1 kimi-1 row 37/8/2/1 → 36/6/0/0, "37" prose, leniency restated as pooled 66/74 ≈ 89% — **SATISFIED** (PRIMARY, §1 below; the 44/48-on-codex-findings figure I said stands is no longer claimed, the pooled one is and it is true).
2. REFUTED attribution: all nine are @codex-1's, kimi-1/F5's refutation included; the only non-codex-1 refutation in the wider record (claude-1's of hermes-1/Q3) sits outside the 47-finding corpus the sentence is scoped to ("in the corpus") — **SATISFIED** (PRIMARY, §1 below).
3. §3: the three mis-recorded findings struck from PARTIAL-only; the inclusion rule ("no refutation from any verifier") now stated — **SATISFIED** (PRIMARY). Two sub-demands **NOT met**: claude-1/F1 still "PARTIAL from one verifier" (reservation 1) and the count still not reconciled with Q2 in or out (reservation 2).
4. Dispositions for claude-1/F2, claude-1/F3, codex-1/F23, kimi-1/F4 — **SATISFIED in substance** (all four fixed with tests; PRIMARY checks in §3 and §4 below), with the F4 commit-attribution defect recorded as reservation 3.
5. Ledger repairs: codex-1/F20 recorded (`IMPLEMENTATION.md:209`), codex-1/F15 recorded as applied-though-unlisted, kimi-1/F5 re-recorded as **dismissed** (`IMPLEMENTATION.md:210`), zcode-1/F14 doubt retracted ("Its number stands", `IMPLEMENTATION.md:94`) — **SATISFIED** (PRIMARY greps).
6. §5 restated with the fourth surface and the release rule reconciled to the three deferred command defects, each deferral reason recorded and independently upheld by @codex-1 — **SATISFIED** (PRIMARY: `grep -n "F6\|F8\|F14" review/signoff2-codex-1.md` → :33, :45-49; `git merge-base --is-ancestor 745ead5b3cae8436aac3de1bf2b8dd0f046bdb5b HEAD` → ancestor).
7. Adjudicate codex-1's round-02 addendum — **SATISFIED**: dispositioned CONFIRMED, FIXED (`consensus.md` dispositions table); PRIMARY: a fresh HEAD binary probes exactly the six roster IDs, no agy (§4 below).

### 1. Figures re-derived from `round-02/*.md` (PRIMARY)

Command (run this session, at HEAD `2a5ea3ad781e76a09b3e67bf1cc43144bd02bdfd`):

```bash
cd parley-deck/ideas/protocol-and-skill-audit/round-02 && for f in *.md; do
  grep -oE '^### .* — (CONFIRMED|PARTIAL|REFUTED|UNREPRO[A-Z]*)' "$f" \
  | grep -oE '(CONFIRMED|PARTIAL|REFUTED|UNREPRO[A-Z]*)$' | sort | uniq -c
done
```

Result: codex-1 **6/8/9/0** over 23 verdict headings (its 24th `###` heading, `:357`, is the no-verdict preflight addendum — matches "assessed 23"); kimi-1 **36/6/0/0** over 42; zcode-1 **30/2/0/0** over 32. The corrected §1 table matches all three rows exactly.

- PRIMARY — REFUTED authorship: `grep -nE '^### .* — REFUTED' round-02/codex-1.md` → nine headings: zcode-1/F2, F3, F5, F9, F10, F12, F13 (`:39-178`), kimi-1/F5 (`:272`), claude-1/F1 (`:285`). All nine are @codex-1's; kimi-1/F5 also drew zcode-1's CONFIRMED (`round-02/zcode-1.md:417`), so §3's corrected F5 line is accurate.
- PRIMARY — census: `for f in round-01/*.md; do grep -oE '^#{2,4} (F[0-9]+|Q[0-9]+|F-Q[0-9]+)' "$f"; done` → codex-1 F1–F24, zcode-1 F1–F15, kimi-1 F1–F5, claude-1 F1–F3 = **47** findings; hermes-1's three landed late and were verified by claude-1 (`round-02/claude-1.md:12,40,69`), outside the 47.
- Derived confirmed set under the document's own criterion: codex-1 24−F12 = 23; zcode-1 15−(7 refuted +F4 PARTIAL-only) = 7; kimi-1 5−F5 = 4; claude-1 3−F1 = 2 → **36 = 47−11** ✓ (contested: 7 + claude-1/F1 + kimi-1/F5 + 2 PARTIAL-only = 11 ✓). Leniency: (36+30)/(42+32) = 66/74 = 89.2% ✓.
- PRIMARY — PARTIAL-only set: codex-1/F12 (zcode-1.md:185 + kimi-1.md:99, both PARTIAL) and zcode-1/F4 (kimi-1.md:211 + codex-1.md:69, both PARTIAL) — exactly the corrected §3 list of two; codex-1/F15, F23 and kimi-1/F4 all carry zcode-1 CONFIRMEDs (`zcode-1.md:234,329,404`) and no refutation, so their reclassification is correct.
- PRIMARY — draft comparison: `git show a1926ae:.../consensus.md` confirms the corrected note's account of the old errors (kimi-1 row 37/8/2/1; "Every REFUTED verdict but one"; title "33 confirmed, 14 contested").
- PRIMARY — the three corrected-count defects: §2's note (`consensus.md:44-49`) derives 36 from addends summing to 37; the §2 bullets (`:51`, `:64`) still enumerate (21)/(3) without F15/F23/F4; §5 (`:119-121`) splits 24+8+4+1 = 37 slots under "The 36 confirmed findings". The number 36 is true for the assessed corpus; the fix list it labels has 37 entries including Q2.

### 2. Test suites (PRIMARY)

- `git archive HEAD | tar -x -C /tmp/kimi-1-verify` (the shared tree is read-only to me; this tests HEAD's exact content), then `GOCACHE=/tmp/kimi1-gocache GOMODCACHE="<repo>/.gomodcache" go test ./... > /tmp/kimi1-gotest.log 2>&1; echo "go-test-exit=$?"` → **go-test-exit=0**, 27 package result lines (26 `ok` + 1 `[no test files]`), 0 `FAIL`. Exit code read from the shell's `$?` of the `go test` process itself, echoed immediately after it in the same shell — not inferred from the log text.
- `cd ../parley-deck-skill && npm test > /tmp/kimi1-npmtest.log 2>&1; echo "npm-test-exit=$?"` → **npm-test-exit=0**; `node --test` reports `tests 391 / pass 391 / fail 0`, python 54 OK, addon-manifest checks ok. Same `$?` method. Caveat tied to reservation 3: the 391 include the F4 test, which passes because the skill repo's working tree carries the uncommitted fix.

### 3. Mutation check — kimi-1/F4 (PRIMARY, in my own copy)

Setup: `cp -R lib test bin skills package.json /tmp/kimi-1-mutation/` plus a `node_modules` symlink from `../parley-deck-skill`; both shared repos untouched (verified after: `git status --short` in the skill repo still shows exactly `M lib/installer.js` + `?? test/source-role-advice.test.js`, the state I found).

- Control (fix present): `node --test test/source-role-advice.test.js` → `tests 3 / pass 3 / fail 0`, `control-exit=0` (exit read via `${PIPESTATUS[0]}`).
- Mutated (source reverted, test kept): `git -C ../parley-deck-skill show HEAD:lib/installer.js > /tmp/kimi-1-mutation/lib/installer.js` restores the committed pre-fix source, then the same command → `tests 3 / pass 2 / fail 1`, `mutated-exit=1` — the failing subtest is "a source-role deck is never told to adopt the packaged protocol" (`ERR_ASSERTION`).
- So the F4 test fails when the F4 fix alone is reverted: the fix is load-bearing, and the "consumer and unknown-role advice unchanged" claim holds — `git diff lib/installer.js` shows the original advice string preserved verbatim in the `else` branch.

### 4. Claims newly asserted by the correction, checked

True under re-verification:

- PRIMARY — `grep -rn "rosterParticipants" internal/app/preflight.go` → `:162,195,211`; a binary built from HEAD (`GOCACHE=/tmp/kimi1-gocache go build -o /tmp/parley-head ./cmd/parley`; the repo-root `./parley` binary predates the fix commits — Aug 21 00:33 vs 10:37 — and still shows the old seven-family probe) run as `/tmp/parley-head preflight` on this deck probes exactly the six roster IDs (claude-1, codex-1, hermes-1, kimi-1, opencode-1, zcode-1), **no agy**, `classification=source-advisory`, one `[exclude-agent] kimi-1 unavailable (unavailable:no-pong)` gate, exit 3. That gate is §9.0 working correctly against an environmentally unavailable roster member (my own CLI's pong fails right now); "preflight reports source-advisory with no gates" holds when all six answer. This deck declares no `[roster.*]` in `parley-deck/agents.toml`; the six come from the central `~/.parley/agents.toml:193-222`.
- PRIMARY — `cat parley-deck/meta/version.json` → `"protocolRole": "source"`.
- PRIMARY — `grep -rn "RosterMaskedFields" internal/` → `internal/config/runtime.go:1212`, emitter `internal/app/roster.go:325`, test `internal/config/runtime_test.go:450` (claude-1/F3).
- PRIMARY — `git show 930c268 -- internal/protocol/drift_test.go` and `-- internal/app/preflight_test.go` → column-signature anchors and the roster-not-adapters test, as the dispositions describe (claude-1/F2, preflight addendum).
- PRIMARY — `git show 815c93a --stat` → `internal/protocol/reviewartifact.go`, `internal/runner/phase58.go`, `internal/runner/phase58_le_test.go`, `parley-deck/meta/version.json` (codex-1/F23). @opencode-1 independently mutation-checked F23 (`signoff-opencode-1.md:72-87`); I did not duplicate it. "Zero live artifacts newly rejected (measured across 72)" — SECONDARY, `815c93a` commit message; I did not re-measure.
- PRIMARY — ledger sentences: `grep -n "kimi-1/F5\|codex-1/F20\|Its number stands" IMPLEMENTATION.md` → `:94,209,210`; `grep -n "Still deferred" IMPLEMENTATION.md` → `:215` {codex-1/F6, F8, F14, kimi-1/F1}; `grep -H "^Status:" signoff-*.md` → codex-1 ❌, kimi-1 ❌, opencode-1 🟡, zcode-1 🟡, claude-1/hermes-1 ✅, matching the "blocked by … reserved by …" sentence; `git merge-base --is-ancestor 745ead5b3cae8436aac3de1bf2b8dd0f046bdb5b HEAD` → ancestor; reviewed amendment commit `815c93a` = HEAD~1.

False or stale in the corrected ledger (the three reservations in my Notes):

- PRIMARY — `git show a1926ae:.../consensus.md | grep -n "claude-1/F1"` → `:66`, and `:82` at HEAD: "PARTIAL from one verifier" is unchanged; two verifiers returned PARTIAL (`round-02/kimi-1.md:295`, `round-02/zcode-1.md:428`).
- PRIMARY — the 36/37 Q2 arithmetic detailed in §1 above (`consensus.md:44-49,51,64,119-121`).
- PRIMARY — `git show 815c93a --stat` (no `installer.js`; the skill is a separate repository) vs `consensus.md:161` and `IMPLEMENTATION.md:207`, both recording `815c93a` for the `recommendedActions` fix; the fix itself is uncommitted (`git -C ../parley-deck-skill status --short` → `M lib/installer.js`, `?? test/source-role-advice.test.js`).

One observation outside my objection scope, recorded because the ledger should know it: `round-02/hermes-1.md` (in HEAD's tree, added by `d9aa26d`, a commit titled for a different idea) contains seven more verdicts on the contested zcode-1 findings — CONFIRMED on F3, F5, F9, F12, F13 and PARTIAL on F2, F10 (`grep -nE '^### .* — ' round-02/hermes-1.md`) — which §3's "kimi-1 or zcode-1's own evidence confirmed them" does not mention. It changes no classification (all seven stay contested) and §1's "three agents" sentence remains true of the dispatched verification round; I make no reservation over it.

I do not re-open `review/consensus.md` or the implementation phase; this re-verification covers the corrected design ledger and the five fixes only.

## Final round

Status: ✅ ACCEPT (line 2 carries the operative token; this restates it for the record).

All three reservations from my re-verification are satisfied, re-derived by me this session at parley-deck-cli HEAD `6cf39f63b613bbaaac5b3f68987e3095d4ff912b` and parley-deck-skill HEAD `40ae6f5417dd0275dec8c5384139972ebce46c5e` — PRIMARY: `git rev-parse HEAD` in both repos matches the prompt, and `git status --short` in both is empty (nothing uncommitted this time).

### Reservation 1 — §3's claude-1/F1 verifier count: SATISFIED

- PRIMARY — `sed -n '295p' round-02/kimi-1.md` → `### claude-1/F1 — PARTIAL`; `sed -n '428p' round-02/zcode-1.md` → `### claude-1/F1 — SKILL.md never names preflight/retro/loop tick/consult/repo-map/tui — PARTIAL`. §3 (`consensus.md:97-99`) now reads "PARTIAL from two verifiers" and cites exactly those two lines. The parenthetical at `consensus.md:101-103` also honestly records that the previous correction pass missed this handed correction.

### Reservation 2 — the 36/37 off-by-one: SATISFIED

- PRIMARY — verdict matrix re-extracted this session from the heading lines of `round-02/{codex-1,kimi-1,zcode-1}.md` (loop over `^### ` lines; key = `<agent>/F<n>`; verdict = last verdict token on the line). Under the document's own criterion (≥1 CONFIRMED, 0 REFUTED): codex-1 **23** (F1–F11, F13, F14, F15, F16–F24 — only F12 excluded, PARTIAL-only), zcode-1 **7** (F1, F6, F7, F8, F11, F14, F15), kimi-1 **4** (F1–F4), claude-1 **2** (F2, F3) → **36**; contested **11** (zcode-1 F2/F3/F5/F9/F10/F12/F13 refuted by codex-1, plus claude-1/F1, kimi-1/F5, and PARTIAL-only codex-1/F12 + zcode-1/F4); 36 + 11 = 47 ✓. §2 (`consensus.md:44-49`) now states the two numbers separately — 36 corpus, 37 fix list including Q2; the §2 bullets (`consensus.md:60`, `:76`) read (23) and (4) and enumerate F15, F23 and F4 by name; §5 (`consensus.md:139-142`) reads "The 37 confirmed findings are the fix list (36 corpus + @hermes-1/Q2)" with `24 + 8 + 4 + 1 = 37`. Every figure matches my derivation.
- PRIMARY — §1 tallies re-run: `for f in *.md; do grep -oE '^### .* — (CONFIRMED|PARTIAL|REFUTED|UNREPRO[A-Z]*)' "$f" | grep -oE '(CONFIRMED|PARTIAL|REFUTED|UNREPRO[A-Z]*)$' | sort | uniq -c; done` in `round-02/` → codex-1 6/8/9/0 over 23 verdict headings, kimi-1 36/6/0/0 over 42, zcode-1 30/2/0/0 over 32; the §1 table matches all three rows. Leniency: (36+30)/(42+32) = 66/74 = 89.2% as §1 states. All nine REFUTED verdicts in the corpus are codex-1's (zcode-1 ×7; kimi-1/F5 at `round-02/codex-1.md:272`; claude-1/F1) — the matrix extraction shows REFUTED attributed to no other verifier.

### Reservation 3 — kimi-1/F4's disposition cited the wrong repo: SATISFIED

- PRIMARY — `git -C ../parley-deck-skill show --stat 40ae6f5` → `lib/installer.js | 14 ++++++++-` and `test/source-role-advice.test.js | 67 +++++`, 2 files, 80 insertions, 1 deletion. `git show 40ae6f5 -- lib/installer.js | grep -E '^\+'` shows `recommendedActions` branching on `project.protocolRole === "source"` with the do-NOT-adopt-packaged message, and the commit message itself records that `815c93a` "claimed this fix and could not contain it". Following last round's lesson, I checked the commit against its contents: it contains the fix it is cited for. `git ls-tree -r 40ae6f5 --name-only | grep source-role-advice` confirms the test file is in the committed tree, not working-tree-only as it was last round.
- SECONDARY — `consensus.md:182` now cites `parley-deck-skill@40ae6f5`, and `consensus.md:225-242` ("A ledger error committed while correcting the ledger") records the `815c93a` misattribution and the uncommitted-measurement problem openly instead of quietly repairing them — which is what I asked the ledger to do.

### Test suites (PRIMARY, both re-run this session)

- Go: `git archive HEAD | tar -x -C /tmp/kimi-1-final-verify` (shared tree is read-only to me; this tests HEAD's exact content), then `GOCACHE=/tmp/kimi1-gocache-final GOMODCACHE="<repo>/.gomodcache" go test ./... > /tmp/kimi1-final-gotest.log 2>&1; echo "go-test-exit=$?"` → **go-test-exit=0**; log has 26 `ok` lines and zero `FAIL`/`panic` lines (`grep -c '^ok'` = 26). How I read the exit code: from the shell's `$?` of the `go test` process itself, echoed immediately in the same shell — not inferred from the log text. This host is unsandboxed (`sysctl kern.boottime` → `kern.boottime: { sec = 1786453740, ... }` succeeds), so the Seatbelt section's host-half claim (`consensus.md:205-223`) holds here.
- npm: `cd ../parley-deck-skill && npm test > /tmp/kimi1-final-npmtest.log 2>&1; echo "npm-test-exit=$?"` → **npm-test-exit=0**; `node --test` reports tests 391 / pass 391 / fail 0 — including ✔ "a consumer-role deck still gets the adopt-after-review advice" and ✔ "a deck with no protocolRole keeps the conservative advice", which is the disposition's "consumer and unknown-role advice unchanged" claim passing — plus python 54 OK and addon-manifest checks ok. Same `$?` method. The working tree is clean, so unlike last round the 391 passes now run against the COMMITTED tree; the claim at `consensus.md:238-239` is true.
- SECONDARY — the sandbox half of the Seatbelt fact (exit 1 inside a Codex seatbelt) is not testable from this host; I am not in a Codex sandbox. The docs citation is exact: `docs/agent-cli-mechanics.md:31-32` reads "Known sandbox artifact: `sysctl kern.boottime` is restricted under seatbelt, which fails parley's `TestDurableKillEndToEndRealProcess` inside codex runs", and the test exists at `internal/runner/durablekill_test.go:75`. The shim-or-skip-`-skip TestDurableKillEndToEndRealProcess`-and-say-so instruction to sandboxed reviewers is the right fact to carry in the consensus.

### Newly asserted and checked — true

- PRIMARY — `git show --stat 35be2e5` and `git show --stat 6cf39f6` → only idea artifacts (consensus.md, signoff files, one IMPLEMENTATION.md line); no code moved after `815c93a`, so the fix dispositions verified last round stand.
- SECONDARY — §2's Q2 summary (6 of 71, two at `ready-for-review`, the `grep -rLl` inverse error) matches `round-02/claude-1.md:12-38` exactly, including whose counter-measurement was the error.

### One imprecision with no ledger consequence — recorded, not reserved

- PRIMARY — `grep -n "Q2" round-01/claude-1.md` → no match; the only round-01 Q2 text is the finding itself (`round-01/hermes-1.md:36`). claude-1's verification of Q2 lives at `round-02/claude-1.md:12` under `round: 2` front matter, so §2's "measured and verified by @claude-1 in round 1" (`consensus.md:47-48`) attributes the verification to the wrong round, and "no round-02 verifier assessed it" is true only of the three dispatched verifiers (§1's "three agents") — claude-1's round-02 file does assess it, as a self-assigned supplemental check its own text distinguishes from the dispatched round. Nothing hinges on it: the verifier, the verdict, the 6-of-71 number, the supplemental-not-corpus classification, and the 37 total are all correct. My stated standard for reserving was errors with consequences — fabricated verdicts, dropped findings, dispositions certifying commits that do not contain the fix — and this is none of those. Flagged so the deck can true the parenthetical whenever it next touches the file, not to hold the consensus.
- Carried from last round, unchanged and still classification-neutral: `round-02/hermes-1.md`'s seven late verdicts on the contested zcode-1 findings (re-tallied this session: 5 CONFIRMED, 2 PARTIAL) remain unmentioned in §3.

I do not re-open the implementation phase or `review/consensus.md`; this final round covers the design ledger only.
