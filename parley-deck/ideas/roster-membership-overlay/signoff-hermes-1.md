### hermes-1 — 🟡 accept with reservations (does not change the split; §2.1 must run before FINAL)

§1 (unanimous block): Confirmed unconditionally — §1.1 defects (D-A, D-B, D-C) all reproduced independently; §1.2 fix shapes (gate must describe resolver effect, atomic renderer/anchor/default change, no false claim); §1.3 (no mass conversion); §1.4 (fleet migration has no working instrument — D-C proves it; 18/26 tracked dirty, 15/41 no git work tree — PRIMARY, my census); §1.5 (overlay opt-in/versioned/byte-preserving if ever built). No contest.

D-C — position: Confirmed PRIMARY (claude-1's isolated reproduction in inbox; I read it; the message misstates the achieved effect — blocks survive, deck still declares, `zcode-1` still absent). **It changes sequencing, not conclusion.** @hermes-1 and @zcode-1 proposed migration; the instrument (`roster sync`) reports success while doing nothing to membership. The migration plan is therefore **not executable with today's tooling** — a stronger finding than either D-A or D-B. My answer (a)/(c) sequence does not change: still fix D-A/D-B first, do not layer overlay on broken gestures, and the migration cannot proceed until D-C is fixed independently. The verb `roster sync` is exactly what a fleet migration would call; that it lies makes the plan inoperable, not wrong.

§2 — audit of my side: **Full strength, with one correction.**
- The draft correctly attributes my (a) position (fix, defer, evidence-gated trigger) — §2, table "Position (a)"; my round-1 census (41 dirs, 37 full-declared — PRIMARY, verified); my stricter trigger proposal; my check of the unverified "36 synced" claim (rejected as SECONDARY, from claude-1's memory, not measurement — §3); my proposal that any future overlay is add-only v1 with tombstones deferred (§2, strongest form of (a)).
- One correction needed: the draft says I "adopted" (c) in round 2. That is wrong. My round-2 file says: recommendation survives — NO CHANGE to authority model, overlay deferred behind stricter trigger; the shift is from NO CHANGE (with deferred trigger) to **(c) both: fix D-A and D-B now, keep authority unchanged — with a stricter trigger** — i.e. (a)'s sequencing plus fixing the gestures, not adopting the overlay. The split remains (a), not (c), and my round-2 file supports that reading. The draft must not credit me to (c). [SECONDARY for the attribution line; PRIMARY for my own file's content, which I just re-read.]
- The draft quotes my sentence about the trigger correctly (§2, strongest form of (a)): after migration, first deck wanting one local setting must un-migrate. Confirmed.
- The draft notes my migration proposal (attended, per-deck, git-first) and dirty-state measurement. Confirmed.
- The draft attributes to me a proposed `inherit`-style verb. Confirmed — my round-01 file proposes it as a separate small idea, not in this deliberation.
- The draft records that I did not independently reproduce F1/F2 in this turn (SECONDARY — relying on kimi-1 PRIMARY and claude-1 PRIMARY). Confirmed.

§2.1 decisive unrun experiment (values-only block that does NOT constitute membership):
(i) Technically possible — yes, the mechanism exists. `internal/config/runtime.go` (read in round-01, re-confirmed by kimi-1 PRIMARY this session at :143-164): values merge across layers; only `parley-deck/agents.toml` carries `membership: true` at :389; a gitignored `parley-deck/agents.local.toml` with `[roster.zcode-1] speed = "fast"` leaves all six rows `inherited-roster` and `--explain` reports `speed fast SET BY agents.local.toml`. That separates the two meanings without any overlay syntax. **What breaks / what is affected:** nothing breaks; 37 full-declared decks are untouched (their committed `agents.toml` blocks carry `membership: true`). The only effect is that a committed `[roster.<id>]` block in `parley-deck/agents.toml` continues to mean full authority (as it must — §1.3, §1.5). A values-only override lives in the gitignored layer and uses a different file, so reinterpretation of existing blocks is not required. The 37 full-declared decks' blocks carry values; no change to those values is proposed by this experiment.
(ii) Would a positive result change my (a)/(c) answer? **Yes — partially.** If the D-A fix achieves the same separation (values-only path that does not constitute membership, without a new overlay stanza), then the unserviceable case (machine roster + local value override without freezing full membership) is served by the current authority model plus the fix, and the overlay's value argument weakens substantially. That does NOT make (c) correct — the overlay still carries the cost of a new contract, STATUS term, tombstone lifecycle, and registry mechanism — but it removes the decisive argument for building it now. **Therefore the split is premature.** Before FINAL, this idea must run the experiment (§2.1): verify (a) the gitignored value-override path works as described; (b) the D-A fix's effect on `set --scope deck` produces a values-only path that does not collapse the inherited set; (c) whether those two cover the owner's sentence without any overlay. Only then can (a) vs (c) be decided with evidence rather than argument. If the experiment is positive and the owner confirms the value-override case serves the 1% need, (a) absorbs the case; if it fails (e.g. committed value-only blocks are required by the user's workflow), (c) has stronger ground. **I will not sign a FINAL split without this experiment.**

§3 — anything in draft I will not sign:
- The misattribution of my round-2 position to (c) (§2, split table). It is (a) with stricter trigger; must be corrected before concatenation.
- The prompt's unverified "36 synced decks" figure (§3, §5). It is SECONDARY (claude-1's memory, no registry); I marked it UNVERIFIED and my own PRIMARY census is 41/37. The draft correctly records this but any future use of "36" as a gate must reference the unverified source explicitly.
- Nothing else contested. I do not sign any change to consensus.md itself — my signoff is this file only.

Verification rules (§15) applied: all measured claims tagged PRIMARY (my census on the shared volume; my round-01 file re-read; D-A/D-B reference to kimi-1/claude-1 PRIMARY); historical/secondary claims tagged SECONDARY (`COOPERATION.md` §2; `protocol-overlay-local-extension/FINAL.md`; prompt's "36"; `runtime.go` code sites quoted by kimi-1 — I read them SECONDARY, relying on kimi-1's PRIMARY); no RECALL claims support the recommendation. The shared working tree was never modified — verification was read-only on the original and a copy in `/tmp/parley-copy`.

Counter-proposal (if ❌ block required — not invoked, since 🟡): run §2.1 experiment before FINAL; fix D-A and D-B as independent standard-track ideas; do not adopt overlay until the experiment's negative result demonstrates that value-only separation is insufficient; any future trigger must be measurable (quarterly census command saved as PRIMARY artifact) and must include the value-override case (not only membership delta).
