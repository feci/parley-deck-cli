# Release channel verification — parley-deck-cli 1.46.0 / parley-deck-skill 2.10.0

Verifier: zcode-1, 2026-08-21. Every check below was run live against the service named in the command; all claims are PRIMARY unless marked otherwise. Overall: **all 7 channels VERIFIED.** One neutral observation on winget state at the bottom.

| # | Channel | Result | Command run and what it returned |
|---|---------|--------|----------------------------------|
| 1a | git tag v1.46.0 on origin (feci/parley-deck-cli) | **VERIFIED** (PRIMARY) | `git ls-remote --tags https://github.com/feci/parley-deck-cli.git` → `318225c… refs/tags/v1.46.0` (annotated), dereferencing to `5538796…` = the `release: parley-deck-cli 1.46.0` commit. Present on origin, not merely local. |
| 1b | git tag v2.10.0 on origin (feci/parley-deck-skill) | **VERIFIED** (PRIMARY) | `git ls-remote --tags https://github.com/feci/parley-deck-skill.git` → `cb0a0cc… refs/tags/v2.10.0` (annotated), dereferencing to `3a16a43…`. Present on origin. |
| 2a | GitHub release CLI v1.46.0, not draft, 6 assets | **VERIFIED** (PRIMARY) | `gh release view v1.46.0 --repo feci/parley-deck-cli --json …` → `isDraft: false`, `isPrerelease: false`, `assetCount: 6`: darwin-arm64, darwin-x64, linux-arm64, linux-x64, windows-arm64.exe, windows-x64.exe. Exactly the six required platforms. |
| 2b | GitHub release skill v2.10.0, not draft, 5 assets | **VERIFIED** (PRIMARY) | `gh release view v2.10.0 --repo feci/parley-deck-skill --json …` → `isDraft: false`, `isPrerelease: false`, `assetCount: 5`: linux-x64, macos-arm64, macos-x64, windows-arm64.exe, windows-x64.exe. |
| 3a | CLI windows-x64 asset is real; SHA-256 = A36C89F7…D165 | **VERIFIED** (PRIMARY) | `gh release download v1.46.0 --pattern '*windows-x64.exe'` then `shasum -a 256` → `a36c89f7f2582651fd710900edec2c4193af1d07501db28b303c13efa329d165` — exact match to the expected hash (7,495,168 bytes; `file` confirms PE32+ executable x86-64 for MS Windows, not a stub). |
| 3b | Skill windows-x64 asset is real; SHA-256 = B2F01D72…F514 | **VERIFIED** (PRIMARY) | `gh release download v2.10.0 --pattern '*windows-x64.exe'` then `shasum -a 256` → `b2f01d72ddb05b316812cd8ed5017a4473a5efbe955a716ae9f6ec80fad8f514` — exact match (91,394,564 bytes; PE32+ executable x86-64). |
| 4 | npm: parley-deck-skill = 2.10.0 | **VERIFIED** (PRIMARY) | `npm view parley-deck-skill version` → `2.10.0`; `npm view parley-deck-skill dist-tags` → `{ latest: '2.10.0' }`. |
| 5a | Homebrew tap: parley-deck-cli.rb at 1.46.0, pushed, sha256 matches tarball | **VERIFIED** (PRIMARY) | `gh api repos/feci/homebrew-parley/contents/Formula/parley-deck-cli.rb` (read from origin = pushed state) → `url …/archive/refs/tags/v1.46.0.tar.gz`, `sha256 7a7e69af…74a6`. Downloaded that exact tarball and `shasum -a 256` → `7a7e69afc60d2b273e7d00a3951766bd0bfbb5c91287c14daf2bb17fa9b974a6` — exact match. |
| 5b | Homebrew tap: parley-deck-skill.rb at 2.10.0, pushed, sha256 matches tarball | **VERIFIED** (PRIMARY) | `gh api repos/feci/homebrew-parley/contents/Formula/parley-deck-skill.rb` → `url …/archive/refs/tags/v2.10.0.tar.gz`, `sha256 4bb65dc4…ee65`. Downloaded tarball hash → `4bb65dc4d72e6e998073596ca74966a5da810428a31ea89eedcba9add5bcee65` — exact match. |
| 6a | winget PR #422111 (Cli): state, labels, single app dir | **VERIFIED** (PRIMARY) | `gh pr view 422111 --repo microsoft/winget-pkgs --json state,labels,files` → `state: OPEN`, `isDraft: false`, `labels: []` (no `PullRequest-Error`, no error/stale labels of any kind). Files: exactly 3, all under `manifests/f/Feci/ParleyDeckCli/1.46.0/` — touches only the Cli application. |
| 6b | winget PR #422113 (Skill): state, labels, single app dir | **VERIFIED** (PRIMARY) | `gh pr view 422113 --repo microsoft/winget-pkgs --json state,labels,files` → `state: OPEN`, `isDraft: false`, `labels: []`. Files: exactly 3, all under `manifests/f/Feci/ParleyDeckSkill/2.10.0/` — touches only the Skill application. |
| 6c | winget manifest hashes match published binaries (the historic failure mode) | **VERIFIED** (PRIMARY, beyond spec) | `gh pr diff` on both PRs, then hashed the actual release assets myself. All four match: Cli x64 `A36C89F7…D165` ✓, Cli arm64 `0E71A676…01E2` ✓ (computed `0e71a676…` from downloaded asset), Skill x64 `B2F01D72…F514` ✓, Skill arm64 `F9EC1EA4…24B0` ✓ (computed `f9ec1ea4…`). No manifest/file mismatch exists. |
| 6d | No other open/stale/errored winget PRs by this author | **VERIFIED** (PRIMARY) | `gh search prs --repo microsoft/winget-pkgs --author feci --state open` → exactly two results: #422111 and #422113, both updated 2026-08-21, both label-free. No stale or errored leftovers from previous attempts. |
| 7a | `parley version` reports 1.46.0 | **VERIFIED** (PRIMARY) | Cloned the v1.46.0 tag to /tmp, `go build ./cmd/parley`, ran `parley version` → `parley 1.46.0`. |
| 7b | Skill version fields all 2.10.0 (at the released tag) | **VERIFIED** (PRIMARY) | `gh api repos/feci/parley-deck-skill/contents/<f>?ref=v2.10.0` → package.json `"version": "2.10.0"`, package-lock.json `"version": "2.10.0"`, skills/parley-deck/references/compatibility.json `"skillVersion": "2.10.0"`. |
| 7c | COOPERATION.md `Protocol synced:` names both versions | **VERIFIED** (PRIMARY) | Line 7 at the v1.46.0 tag: `**Protocol synced:** 2026-08-21 — parley-deck-skill 2.10.0 / parley-deck-cli 1.46.0`. Working tree in the shared volume is identical. |

## Notes

- **Nothing FAILED and nothing was left unverified.** Every hash comparison in this report was computed by me from freshly downloaded artifacts, not copied from release notes, manifests, or other agents' reports.
- **winget PRs are OPEN, not merged.** That is the normal state for a winget-pkgs submission awaiting maintainer review (typically hours to days), and it satisfies the specified check (state + labels + one app per PR). If the claim required the packages to be *installable via winget today*, that part is not yet true — the manifests exist only in the PR branches until merge.
- Downloads and the tag build were done in `/tmp/parley-verify` (cloned, never built in the shared tree); the shared working tree was not modified. Scratch directory removed after verification.
