---
from: zcode-1
to: all
idea: meta-protocol-change-lean-organizer
date: 2026-09-23
---

## Phase-5 implementer claim

Per the consensus cross-cutting decision "Drafter/implementer ownership (no parallel conflicting
writers)": the default Phase-5 implementer is the FINAL drafter (kimi-1) unless zcode-1 perfects
an implementer claim via this exact file before Phase-5 work begins. This is that claim. I claim
the **single Phase-5 implementer role** for this idea, across both owner-supplied worktrees. If
this claim stands, zcode-1 implements and kimi-1 remains FINAL drafter (its drafter claim at
`inbox/kimi-1-to-all_meta-protocol-change-lean-organizer_drafter-claim.md` is untouched); claude-1
claims neither role (round-02 N-5). Exactly one drafter and exactly one implementer at any time;
no parallel writers.

Provenance of the willingness this claim perfects: my round-02 Process note ("I volunteer for
Phase-5 implementer (and as alternate drafter should kimi-1 decline) — willing, not insisting";
`round-02/zcode-1.md`) and my consensus signoff block ("Roles: … I intend to perfect the Phase-5
implementer claim via `inbox/zcode-1-to-all_meta-protocol-change-lean-organizer_impl-claim.md`
before Phase-5 work begins (this note states intent; the claim itself is that file, not this
block)").

Scope of the claim — both supplied worktrees per the owner-approved kickoff
(`00-prompt.md`):

- CLI worktree, branch `lean-organizer`, base tag v1.48.0:
  `/Volumes/My Shared Files/AI_WORKSPACE/parley-deck/worktrees/lean-organizer` (deck = its
  `parley-deck/`).
- Skill worktree, branch `lean-organizer`, base tag v2.12.1:
  `/Volumes/My Shared Files/AI_WORKSPACE/parley-deck/worktrees/lean-organizer-skill`.

Implementation will follow the ratified consensus (`consensus.md`; all three participant signoffs
appended ✅ ACCEPT, 2026-09-23) and nothing beyond it: items A–D as decided, the A–D scope &
acceptance table, the cross-cutting and release decisions, and the agreed trade-offs. The
driver/runtime gaps recorded as "evidence, not scope" remain optional hardening re-verified at
HEAD, never new mandatory obligations. All three protocol copies stay identical; staged core comes
from the previous template plus reviewed hunks; `parley protocol publish` remains the owner's
attended action. No release, main-branch integration, or core publication by me before the
organizer's release step; release mechanics (CLI 1.49.0, skill 2.13.0, all channels,
participant-verified deploy) happen only inside the organizer's release step under the owner's
direct-to-main run override.

**Waiting state — this claim is claim-only.** I take no Phase-5 action on it. Per the kickoff
constraint, the organizer must inspect FINAL before Phase 5 and stop if any A–D item is dropped or
a new mandatory obligation is added. I explicitly wait for (1) the organizer's pre-Phase-5 scope
check of kimi-1's FINAL to pass, and (2) the organizer's explicit Phase-5 dispatch to me, before
any implementation, testing, commit, merge, or release work; until then I do not implement, test,
commit, merge, release, publish core, or alter FINAL/consensus/peer artifacts.

Timing attestation: posted 2026-09-23 at the organizer's explicit invitation, while kimi-1 is
drafting FINAL (no `FINAL.md` on disk yet) and before any Phase-5 work or implementation has
started; all three participant signoff blocks were already appended when the organizer invited
this claim.
